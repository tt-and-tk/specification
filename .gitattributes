# 改行コードをLFに統一する(作業ツリー・インデックスとも)
# (指定しない場合，保存時の改行コードがそのまま記録され，編集環境の違いで意味のない全行差分が入り込むため)
# text=autoとするのは，将来追加しうる画像等のバイナリを変換対象にしないため
# eol=lfを明示するのは，Windowsではこれがないと作業ツリーがCRLFでチェックアウトされるため
* text=auto eol=lf
