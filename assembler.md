# Pyntaxis(アセンブリ言語)について
## 仕様
- `;`以降はコメント
- 関数ごとにブロックを作る．`main`ブロックは必ずある
- main関数を一番最初に書く．
- このプログラムで使用するすべての関数名を`.global`宣言でカンマ区切りで挙げる．`.global`宣言はインデントせず行頭から書き，`.global`と関数名の間は半角スペースで区切る．カンマの前後には必ず関数名を書く(連続・先頭・末尾のカンマは書けない)．数値表記(`fh`・`-5`等)やレジスタ表記(`r5`等)として読める綴りは即値やレジスタと，`.`で始まる綴りは局所ラベル(後述「ラベルによるジャンプ先指定」)と区別できないため，関数名にできない
- `.global`宣言より前は空行とコメント行のみ許可
- インデントは半角スペース（タブ文字は非対応）
- 全関数に`ret`が1つ以上必要（位置・個数は問わない）
- マジックナンバーは，とくに指定がなければ10進数として扱う．末尾に`b`,`o`,`h`がある場合はそれぞれ2進数，8進数，16進数として扱う．負の値は即値の位置にのみ，10進数表記で書ける
- レジスタの番地は`r10`のように表記する．特に指定がなければ10進数で，末尾に文字があれば指定された基数で扱うのはマジックナンバーと同様
- 即値の位置に関数名を書くと，その関数の先頭のPCとして扱う(関数の番地をレジスタへ載せる用途．後述「ジャンプ系(J系)」)．命令のPCは出力先によって異なる(後述「出力先」)．`main`は書けない．`jmp`・F系の飛び先には関数名を書けず，局所ラベルしか書けない
- ループ命令はない
- 関数内のジャンプ先は局所ラベル(`.L0`等)で指定する(後述「ラベルによるジャンプ先指定」)

例
```
.global main, get_num

main:
    call get_num
    ret

get_num:
    mov fh r0 r1 42  ; r1に42を代入
    ret
```

## 処理を実行しない(N系)
```
main:
    nop  ; 何もしない
    ret
```

## 演算系(P系)
rs1とrs2の演算結果をrdに格納．
```
main:
    and r1 r2 r3   ; 論理積
    or  r1 r2 r3   ; 論理和
    xor r1 r2 r3   ; 排他的論理和
    not r1 r2      ; 反転 rs2は使わない
    nand r1 r2 r3  ; 否定論理積
    add r1 r2 r3   ; 算術和
    sub r1 r2 r3   ; 算術差
    mul r1 r2 r3   ; 積
    div r1 r2 r3     ; 符号ありの商のみ必要な場合．r3に商を格納(余りは捨てる)
    div r1 r2 r3 4   ; 符号ありの商と余りの両方が必要な場合．r3に商，4番地のレジスタに余りを格納(番地だがイミディエイトデータとしてr接頭辞なしで記載)
    divu r1 r2 r3    ; 符号なしの商のみ必要な場合．r3に商を格納(余りは捨てる)
    divu r1 r2 r3 4  ; 符号なしの商と余りの両方が必要な場合．余りの格納先の指定方法はdivと同じ
    ret
```

## シフト系(S系)
rs1をrs2またはイミディエイトデータ分だけシフトしrdに格納．
```
main:
    sll r1 r2 r3  ; 左シフト命令
    srl r1 r2 r3  ; 右シフト命令
    sla r1 r2 r3  ; 算術左シフト命令
    sra r1 r2 r3  ; 算術右シフト命令
    ret
```

## 代入系(A系)
rs1またはイミディエイトデータをrdに格納．maskは使用されず，常にrdの全バイトに書き込まれる(構文上は必須引数のため値を指定する必要があるが，何を指定しても動作は変わらない)．
```
main:
    mov fh r0 r1 42  ; r1に即値42を代入（maskは未使用のため値は動作に影響しない）
    mov fh r2 r1     ; r2の値をr1へコピー（maskは未使用のため値は動作に影響しない）
    ret
```

## 分岐系(F系)
rs1とrs2を比較して，条件を満たせばPCに飛び先までの相対オフセットを足し，満たさなければインクリメントする．\
飛び先は**局所ラベルのみ**で指定する(数値での直接指定は不可)．アセンブラがラベルから相対オフセットを計算する．
```
main:
    eq   r1 r2 .L0  ; 等号
    ne   r1 r2 .L0  ; 不等号
    lt   r1 r2 .L0  ; 符号あり小なり
    gt   r1 r2 .L0  ; 符号あり大なり
    elt  r1 r2 .L0  ; 符号あり以下
    egt  r1 r2 .L0  ; 符号あり以上
    ltu  r1 r2 .L0  ; 符号なし小なり
    gtu  r1 r2 .L0  ; 符号なし大なり
    eltu r1 r2 .L0  ; 符号なし以下
    egtu r1 r2 .L0  ; 符号なし以上
.L0:
    ret
```

## ジャンプ系(J系)
`call`/`ret` は CPU の正式命令．戻り先アドレスをメモリ上のスタックへ積み下ろしする処理と SP の更新は，すべてハードウェアが行う(スタックの扱いは[memory.md](memory.md)を参照)．ネストの段数に固定の上限はなく，深さの上限はスタックの残り容量で決まる．空のスタックで`ret`を実行すると番地が不正となりCPUが停止する([halt.md](halt.md)を参照)．\
`jmp <局所ラベル>` は指定したラベルの位置のPCへ無条件ジャンプする。飛び先は**局所ラベルのみ**で指定し、レジスタ・数値による飛び先指定は持たない。\
`call <関数名>` は呼び出し先関数の先頭アドレスへ，戻り先を保存してジャンプする．呼び出し先に`main`は書けない(`main`はプログラムの開始点であり，ROMでは`main`の`ret`が復帰しないため．後述)．`call <レジスタ>`(例: `call r1`)はレジスタの値を呼び出し先の番地とする(ハードウェアのレジスタ間接ジャンプを使う)．関数の番地は，即値の位置に関数名を書いてレジスタへ載せる．\
`ret` は引数なしで，置かれている関数によって動作が異なる．
- `main`以外の関数の`ret`: 直近の `call` の戻り先へ復帰する
- `main`の`ret`: プログラムの終了を表す．終了時の動作は出力先によって異なる(後述「出力先」)
  - ROM: `main`はプログラムの開始点で戻り先を持たない．アセンブラが`main`内のすべての`ret`を自分自身へ飛ぶ`jmp`に置き換えるため，CPUは停止せず同じ命令を実行し続ける(停止との違いは[halt.md](halt.md)を参照)
  - 実行ファイル: シェルが`main`をCALLで呼び出すため，`ret`のまま残り，シェルへ戻る
```
main:
    call func1         ; func1 を呼び出す(戻り先はハードウェアが保存)
    mov fh r0 r1 func1 ; r1 = func1の先頭のPC
    call r1            ; r1の値の番地(func1)を呼び出す
    jmp .L0            ; 局所ラベル .L0 へジャンプ
.L0:
    ret                ; プログラムを終了する(引数なし)

func1:
    ret                ; 呼び出し元へ復帰する(引数なし)
```

## メモリ系(M系)
メモリの番地を指定し，読み込みの場合はその番地のデータをrdへ格納し，書き込みの場合はrs2のデータをその番地へ書き込む．同時にプログラムカウンタを更新する．番地の指定方法は命令によって異なる．

- `rm`/`wm`: rs1またはイミディエイトデータで番地を指定する．イミディエイトデータを指定した場合，rs1は無視される
- `brm`/`bwm`: 番地の指定方法は未検討([isa.md](isa.md)を参照)
- `rmr`/`wmr`: rs1にイミディエイトデータを足した番地を指定する．両方を常に使用するためイミディエイトデータは省略できず，rs1も無視されない．イミディエイトデータには負の値を書ける(rs1より小さい番地を指す．負の値は10進数表記で書く)．SP(`r16`．[register.md](register.md)を参照)をrs1に指定すれば，スタック上に確保した領域をSPからの相対位置で読み書きできる(領域の確保はSPを減らして行う．[memory.md](memory.md)を参照)

```
main:
    rm  fh r1 r3      ; r1番地からr3へ読み込む(mask=0xf，imm省略)
    wm  fh r1 r2      ; r1番地へr2を書き込む(mask=0xf，imm省略)
    brm fh r1 r2 r3   ; バースト読み込み(アセンブルは可能だがCPU側が未実装で実行しようとすると停止する)
    bwm fh r1 r2 r3   ; バースト書き込み(アセンブルは可能だがCPU側が未実装で実行しようとすると停止する)
    rmr fh r16 r3 4   ; SP+4番地からr3へ読み込む(immは省略できない)
    wmr fh r1 r2 -4   ; r1-4番地へr2を書き込む(immには負の値も書ける)
    ret
```

## 標準入出力系(IO系)
処理が完了すると同時にPCをインクリメントする．\
scanは標準入力から読み込んだデータをrdへ格納する．\
printはrs1またはイミディエイトデータを標準出力へ出力する．
```
main:
    scan r1     ; 標準入力から読み込んだデータをr1へ格納
    print r1    ; r1の値を出力
    print r0 5  ; 即値5を出力（rs1は無視される）
    ret
```

# ラベルによるジャンプ先指定(局所ラベル)

`if`/`while`/`for` などの関数内ジャンプの飛び先を指定するための**局所ラベル**を提供する．\
`.global`宣言する関数ラベルとは別物で，`call`の対象にはならず，定義した関数内のジャンプ先専用である．

## 定義

- 局所ラベルは先頭が `.` の名前で，`<ラベル名>:` を単独行に書いて定義する(例: `.L0:`)．
- 関数ラベルと異なり `.global` 宣言は不要．先頭の `.` で関数名と区別する(Pynesisの識別子は `.` で始まらないため衝突しない．識別子の字句規則は[compiler.md](compiler.md)を参照)．
- ラベルは命令を生成せず，その位置(直後の命令のindex)を表すだけ(関数ラベルと同様)．
- ラベルは，定義した行の直前にある関数ラベルの関数に属する．関数の末尾(最後の命令の後)に書いたラベルは，位置が次の関数の先頭と一致するが，定義した関数に属する．どの関数にも属さない位置(`main:`より前)には定義できない(エラー)．
- ラベル名はプログラム全体で一意とする．関数ごとに名前を分けないことで，アセンブラは全関数のラベルを1つの表で解決できる．コンパイラは連番 `.L0`，`.L1`，… で一意性を保証し，手書きのアセンブリでは書き手が一意性を守る．
- 参照されるラベルは参照元と同じ関数内に定義されていなければならない(未定義のラベル・別の関数のラベルの参照はエラー)．

## ジャンプ先としての参照

`jmp` とF系命令(`eq`/`ne`/`lt`/`gt`/`elt`/`egt`/`ltu`/`gtu`/`eltu`/`egtu`)のイミディエイト位置に，数値の代わりに局所ラベル名を書ける．アセンブラがラベルを次の値に解決する．

| 命令 | 解決される値 | 理由 |
|:-|:-|:-|
| `jmp .L0` | `.L0`の位置の**PC**(先頭の命令のPC + `.L0`のindex) | `jmp`は `PC = imm`(絶対ジャンプ) |
| `eq r1 r2 .L0` 等のF系 | `.L0のindex − このF系命令のindex`(**相対オフセット**) | F系は真のとき `PC += imm`．飛び先 `T` に着くには `imm = T − (このF系のindex)` |

- F系のオフセットは**負になりうる**(後方ジャンプ)．イミディエイトは32ビットの2の補数として扱い，ALUは `PC ← PC + imm` で後方へも進む．
- indexは，プログラムの先頭の命令を0として数えた命令の順番である．先頭の命令のPCは出力先によって異なる(後述「出力先」)．F系の相対オフセットはindexの差なので，出力先によらない．
- 解決の仕組みは，即値の位置や`call`に書いた関数名を先頭のPCに置き換える処理と同様で，ラベル表を引いて置換する．F系のときだけ「参照元の命令indexとの差」を取る点が異なる．
- **ジャンプ先は局所ラベルのみで指定する**(数値での直接指定は不可)．また `jmp` はレジスタ値による飛び先指定を持たず，ラベルのみを取る(ハードウェアのレジスタ間接ジャンプ機能を`jmp`では当面使用しない．`call`での使用は「ジャンプ系(J系)」を参照)．

## 例

前方ジャンプ(if本体のスキップ)と後方ジャンプ(do-whileのループ)の例．コメントの数値は解決後のイメージ(ROMへ出力する場合．indexとPCが一致する)．

前方(F系オフセットが正):
```
    rm  fh r0 r0 4      ; index 0: r0 = a
    rm  fh r0 r1 8      ; index 1: r1 = b
    egt r0 r1 .L0       ; index 2: a>=b なら本体スキップ → imm = 5 - 2 = 3
    mov fh r0 r0 1      ; index 3 \ x = 1 (本体)
    wm  fh r0 r0 12     ; index 4 /
.L0:                    ; → index 5
```

後方(F系オフセットが負):
```
.L1:                    ; → index 0 (ループ先頭)
    mov fh r0 r0 1      ; index 0 \ x = 1 (本体)
    wm  fh r0 r0 12     ; index 1 /
    rm  fh r0 r0 4      ; index 2: r0 = a
    rm  fh r0 r1 8      ; index 3: r1 = b
    lt  r0 r1 .L1       ; index 4: a<b なら先頭へ戻る → imm = 0 - 4 = -4 (負)
```

無条件の後方ジャンプ(`while`の条件へ戻る等)は `jmp .L1` のように書き，`.L1`の位置のPCへ飛ぶ(絶対なので符号は不要)．

# アセンブラについて
上記アセンブリ言語で書かれたコードを，ROMとして埋め込むSystem Verilogのソース，またはQosmosの実行ファイルにする．

## 出力先
出力先によって，命令を置く位置と`main`の`ret`の扱いが異なる．

| | ROM(`.sv`) | 実行ファイル |
|:-|:-|:-|
| 出力する内容 | 受け取ったPCに格納されている機械語を返すROMのモジュール．各命令は機械語を作る関数(`machine.svh`)の呼び出しとして書く | 各命令の機械語を先頭から8バイトずつ並べたもの(形式は[qosmos.md](qosmos.md)の「実行ファイル」を参照) |
| 先頭の命令のPC | `0` | `0x4000`(メモリの後半の先頭に対応するPC．[memory.md](memory.md#メモリ上の命令の実行)を参照) |
| 関数の先頭・`jmp`の飛び先のPC | 先頭の命令からのindexそのもの | `0x4000` + 先頭の命令からのindex |
| `main`の`ret` | 自分自身へ飛ぶ`jmp`に置き換える(プログラムの終了) | `ret`のまま残す(シェルへ戻る) |
| 命令数の上限 | 16384([limitations.md](limitations.md)を参照) | 4096(メモリの後半の32KBに収まる命令数) |

命令数が上限を超える場合はエラーになる．\
エラーになった場合は，出力ファイルを作らない．

## 起動引数

実行ファイル名は `asm2mc.exe`．

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pt` | 入力アセンブリファイル名(`.pt`)．必須 | - |
| `-sv` | 出力SystemVerilog ROMファイル名(`.sv`) | `-pt`の拡張子を`.sv`に変えた名前を自動的に使う |
| `-bin` | 出力する実行ファイル名．ファイル名の部分(ディレクトリを除いた部分)は，Qosmosの名前の規則([qosmos.md](qosmos.md)の「ファイル・ディレクトリの名前」を参照)に従い，かつ拡張子を持たない名前に限る(Qosmosは拡張子の無いファイルだけを実行ファイルとして探すため)．これに合わない名前はエラーになる | SystemVerilog ROMを出力する |

出力するファイルの種類は`-bin`の有無で決まる．出力ファイルを何も指定しない場合は，SystemVerilog ROMを出力する．実行ファイルを出力するには`-bin`の指定が必須である．`-sv`と`-bin`を両方指定した場合はエラーになる．

例: `asm2mc.exe -pt program.pt -sv program.sv`\
例: `asm2mc.exe -pt hello.pt -bin HELLO`

同じフラグ(`-pt`／`-sv`／`-bin`)や指定子なし引数を複数回指定した場合，エラーにはならず最後に指定した値で上書きされる(後勝ち)．
