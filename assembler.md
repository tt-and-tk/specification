# Pyntaxis(アセンブリ言語)について
## 仕様
- `;`以降はコメント
- 関数ごとにブロックを作る．`main`ブロックは必ずある
- main関数を一番最初に書く．
- このプログラムで使用するすべての関数名を一行目（`.global`宣言）で挙げる
- `.global`宣言より前は空行とコメント行のみ許可
- インデントは半角スペース（タブ文字は非対応）
- 全関数に`ret`が1つ以上必要（位置・個数は問わない）
- マジックナンバーは，とくに指定がなければ10進数として扱う．末尾に`b`,`o`,`h`がある場合はそれぞれ2進数，8進数，16進数として扱う
- レジスタの番地は`r10`のように表記する．特に指定がなければ10進数で，末尾に文字があれば指定された基数で扱うのはマジックナンバーと同様
- ループ命令はない
- 関数内のジャンプ先は局所ラベル(`.L0`等)で指定する(後述「ラベルによるジャンプ先指定」)

例
```
.global get_num, main

get_num:
    mov fh r0 r1 42  ; r1に42を代入
    ret

main:
    call get_num
    ret
```

## 処理を実行しない(N系)
```
main:
    nop  ; 何もしない
    ret
```

## 演算系(P系)
rs1とrs2の演算結果をrdに格納．
```
main:
    and r1 r2 r3   ; 論理積
    or  r1 r2 r3   ; 論理和
    xor r1 r2 r3   ; 排他的論理和
    not r1 r2      ; 反転 rs2は使わない
    nand r1 r2 r3  ; 否定論理積
    add r1 r2 r3   ; 算術和
    sub r1 r2 r3   ; 算術差
    mul r1 r2 r3   ; 積
    div r1 r2 r3     ; 商のみ必要な場合．r3に商を格納(余りは捨てる)
    div r1 r2 r3 4   ; 商と余りの両方が必要な場合．r3に商，4番地のレジスタに余りを格納
    ret
```

## シフト系(S系)
rs1をrs2またはイミディエイトデータ分だけシフトしrdに格納．
```
main:
    sll r1 r2 r3  ; 左シフト命令
    srl r1 r2 r3  ; 右シフト命令
    sla r1 r2 r3  ; 算術左シフト命令
    sra r1 r2 r3  ; 算術右シフト命令
    ret
```

## 代入系(A系)
rs1またはイミディエイトデータをrdに格納．maskは使用されず，常にrdの全バイトに書き込まれる(構文上は必須引数のため値を指定する必要があるが，何を指定しても動作は変わらない)．
```
main:
    mov fh r0 r1 42  ; r1に即値42を代入（maskは未使用のため値は動作に影響しない）
    mov fh r2 r1     ; r2の値をr1へコピー（maskは未使用のため値は動作に影響しない）
    ret
```

## 分岐系(F系)
r01とr02を比較して，条件を満たせばpcに飛び先までの相対オフセットを足し，満たさなければインクリメントする．\
飛び先は**局所ラベルのみ**で指定する(数値での直接指定は不可)．アセンブラがラベルから相対オフセットを計算する．
```
main:
    eq  r01 r02 .L0  ; 等号
    ne  r01 r02 .L0  ; 不等号
    lt  r01 r02 .L0  ; 小なり
    gt  r01 r02 .L0  ; 大なり
    elt r01 r02 .L0  ; 以下
    egt r01 r02 .L0  ; 以上
.L0:
    ret
```

## ジャンプ系(J系)
`call`/`ret` は CPU の正式命令。戻り先アドレスの保存・SP の更新・戻り先レジスタ(`6'h11`〜`6'h1a`)の管理はすべてハードウェアが行う(最大10階層)。\
`jmp <局所ラベル>` は指定したラベルの絶対indexへ無条件ジャンプする。飛び先は**局所ラベルのみ**で指定し、レジスタ・数値による飛び先指定は持たない。\
`call <関数名>` は呼び出し先関数の先頭アドレスへ、戻り先を保存してジャンプする。\
`ret` は引数なしで、直近の `call` の戻り先へ復帰する。
```
main:
    call func1 ; func1 を呼び出す(戻り先はハードウェアが保存)
    jmp .L0    ; 局所ラベル .L0 へジャンプ
.L0:
    ret        ; 呼び出し元へ復帰する(引数なし)
```

## メモリ系(M系)
r01またはイミディエイトデータでメモリの番地を指定子，その番地のデータをr03へ読み込みまたは書き込みする．\
イミディエイトデータを指定した場合，r01は無視される．
同時にプログラムカウンタや各種フラグも更新する．
```
main:
    rm  fh r1 r3      ; r1番地からr3へ読み込む（mask=0xf，imm省略）
    wm  fh r1 r2      ; r1番地へr2を書き込む（mask=0xf，imm省略）
    brm fh r1 r2 r3   ; バースト読み込み（アセンブルは可能だがCPU側が未実装で実行しようとすると停止する）
    bwm fh r1 r2 r3   ; バースト書き込み（アセンブルは可能だがCPU側が未実装で実行しようとすると停止する）
    ret
```

# ラベルによるジャンプ先指定(局所ラベル)

`if`/`while`/`for` などの関数内ジャンプの飛び先を指定するための**局所ラベル**を提供する．\
`.global`宣言する関数ラベルとは別物で，`call`の対象にはならず，関数内のジャンプ先専用である．

## 定義

- 局所ラベルは先頭が `.` の名前で，`<ラベル名>:` を単独行に書いて定義する(例: `.L0:`)．
- 関数ラベルと異なり `.global` 宣言は不要．先頭の `.` で関数名と区別する(C言語の識別子は `.` で始まらないため衝突しない)．
- ラベルは命令を生成せず，その位置(直後の命令のindex)を表すだけ(関数ラベルと同様)．
- ラベル名はプログラム全体で一意とする(コンパイラは連番 `.L0`，`.L1`，… で一意性を保証する)．
- 参照されるラベルは必ずどこかで定義されていなければならない(未定義参照はエラー)．

## ジャンプ先としての参照

`jmp` とF系命令(`eq`/`ne`/`lt`/`gt`/`elt`/`egt`)のイミディエイト位置に，数値の代わりに局所ラベル名を書ける．アセンブラがラベルを次の値に解決する．

| 命令 | 解決される値 | 理由 |
|:-|:-|:-|
| `jmp .L0` | `.L0`の**絶対index** | `jmp`は `PC = imm`(絶対ジャンプ) |
| `eq r1 r2 .L0` 等のF系 | `.L0のindex − このF系命令のindex`(**相対オフセット**) | F系は真のとき `PC += imm`．飛び先 `T` に着くには `imm = T − (このF系のindex)` |

- F系のオフセットは**負になりうる**(後方ジャンプ)．イミディエイトは32ビットの2の補数として扱い，ALUは `PC ← PC + imm` で後方へも進む．
- 解決の仕組みは既存の関数名解決(関数名→開始index)と同様で，ラベル表を引いて置換する．F系のときだけ「参照元の命令indexとの差」を取る点が異なる．
- **ジャンプ先は局所ラベルのみで指定する**(数値での直接指定は不可)．また `jmp` はレジスタ値による飛び先指定を持たず，ラベルのみを取る(ハードウェアのレジスタ間接ジャンプ機能は当面使用しない)．

## 例

前方ジャンプ(if本体のスキップ)と後方ジャンプ(do-whileのループ)の例．コメントの数値は解決後のイメージ．

前方(F系オフセットが正):
```
    rm  fh r0 r0 4      ; index 0: r0 = a
    rm  fh r0 r1 8      ; index 1: r1 = b
    egt r0 r1 .L0       ; index 2: a>=b なら本体スキップ → imm = 5 - 2 = 3
    mov fh r0 r0 1      ; index 3 \ x = 1 (本体)
    wm  fh r0 r0 12     ; index 4 /
.L0:                    ; → index 5
```

後方(F系オフセットが負):
```
.L1:                    ; → index 0 (ループ先頭)
    mov fh r0 r0 1      ; index 0 \ x = 1 (本体)
    wm  fh r0 r0 12     ; index 1 /
    rm  fh r0 r0 4      ; index 2: r0 = a
    rm  fh r0 r1 8      ; index 3: r1 = b
    lt  r0 r1 .L1       ; index 4: a<b なら先頭へ戻る → imm = 0 - 4 = -4 (負)
```

無条件の後方ジャンプ(`while`の条件へ戻る等)は `jmp .L1` のように書き，`.L1`の絶対indexへ飛ぶ(絶対なので符号は不要)．

# アセンブラについて
上記アセンブリ言語で書かれたコードを，System Verilogのソースにする．\
romとして，受け取ったpcに格納されている機械語を返すもの．

## 起動引数

実行ファイル名は `asm2bin.exe`．

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pt` | 入力アセンブリファイル名(`.pt`)．必須 | - |
| `-bin` | 出力SystemVerilog ROMファイル名(`.sv`) | `-pt`の拡張子を`.sv`に変えた名前を自動的に使う |

例: `asm2bin.exe -pt program.pt -bin program.sv`

同じフラグ(`-pt`／`-bin`)や指定子なし引数を複数回指定した場合，エラーにはならず最後に指定した値で上書きされる(後勝ち)．
