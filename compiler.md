# Pynesisの言語・コンパイラ仕様

## 概要

Pynesisの言語仕様，およびそのコンパイラの仕様．

---

# 言語仕様

## 識別子

変数名・関数名等の識別子は，アルファベットまたは`_`で始まり，以降はアルファベット・数字・`_`のみで構成される．

## データ型

| 型 | ビット幅 | 値の範囲 | 備考 |
|:-|:-|:-|:-|
| `char` | 8ビット | -128〜127 | |
| `short` | 16ビット | -32768〜32767 | |
| `int` | 32ビット | -2147483648〜2147483647 | |
| `unsigned char` | 8ビット | 0〜255 | |
| `unsigned short` | 16ビット | 0〜65535 | |
| `unsigned int` | 32ビット | 0〜4294967295 | |
| `void` | - | - | 戻り値なし関数の型としてのみ使用 |

`char`/`short`/`int`の直前に符号修飾子`signed`(符号付き)または`unsigned`(符号なし)を書ける．省略時は`signed`である．  
符号修飾子は`char`/`short`/`int`の直前にのみ書け，単独(`unsigned x`)・重ね書き(`signed unsigned int`)・`void`や構造体型への付与はコンパイルエラーになる．`const`と組み合わせる場合は`const unsigned int`の順に書く(「const変数」を参照)．  
以降，`char`/`short`/`int`とその符号なし型をまとめて整数型と呼ぶ．  
`long` は将来対応予定．`float` / `double` はISAに浮動小数点命令がないため対応しない．

## リテラル

### 整数リテラル

| 記法 | 例 |
|:-|:-|
| 10進 | `10`，`255` |
| 16進(`0x`または`0X`に続けて1桁以上の16進数字) | `0x0f`，`0XFF` |

- 書ける値は，10進が`0`〜`2147483647`，16進が`0x0`〜`0xFFFFFFFF`で，範囲外はコンパイルエラーになる
- 型は`int`．ただし`int`で表せない`0x80000000`以上の値(16進でのみ書ける)は`unsigned int`になる(C言語と同じ)
- 負の値はリテラルに単項マイナスを付けて表す
- 型を指定する接尾辞(`10u`等)は書けない．小さい値を符号なしとして扱うには，`unsigned int`のconst変数や変数に入れて使う
- 先頭が`0`でも8進ではなく10進として読む(`010`は`10`)．C言語とは解釈が異なる

### 文字リテラル

- ASCIIの1文字またはエスケープシーケンス1つを`'`で囲む(`'a'`，`'\n'`)．空(`''`)や2文字以上はコンパイルエラーになる
- 型は`char`，値はその文字の文字コード

### 文字列リテラル

- 文字とエスケープシーケンスの並びを`"`で囲む(`"hello"`)．途中で改行することはできない
- 隣接した文字列リテラルは1つに連結される(`"C" "D"`は`"CD"`)．間に空白・改行・コメントを挟んでも連結される

### エスケープシーケンス

文字リテラル・文字列リテラルで共通に使える．

| 記法 | 意味 |
|:-|:-|
| `\n` | 改行 |
| `\t` | タブ |
| `\r` | 復帰 |
| `\0` | ヌル文字 |
| `\\` | `\` |
| `\'` | `'` |
| `\"` | `"` |

上記以外は，`\`を取り除いた次の1文字そのものになる(`\a`は`a`)．`\x41`・`\101`のような数値によるエスケープには対応しない．8進表記を書いた場合，文字列リテラル中では`\0`とそれに続く数字として解釈され(`"\012"`は`\0`・`1`・`2`の3文字)，文字リテラル中(`'\012'`)ではコンパイルエラーになる．

## 変数

- グローバル変数・ローカル変数の両方に対応する
- const変数(後述の「const変数」)を除くすべての変数はメモリ上に配置する(レジスタへの割り当ては行わない)
  - スカラー変数は宣言場所・型によらず1ワード(4バイト)を占有する．`char`/`short`も値をワードの下位バイトに置き，他の変数と1ワードを共有しない
- ブロックスコープに対応する
- スコープが異なれば同名変数は別の変数として扱う(シャドーイング許容)
  - 例外として，ハードウェア変数(後述の「定義済みハードウェア変数」)と同名のローカル変数・関数パラメータは宣言できない(コンパイルエラー)．I/Oレジスタの誤上書きを防ぐための特別ルールであり，通常のグローバル変数との同名は問題ない
- 宣言と同時に初期化できる(`int x = 0;`)
  - 初期化子のない変数の値は保証されない．C言語と異なり，グローバル変数も0で初期化されない([limitations.md](limitations.md)を参照)
- グローバル変数の初期化子は**定数式のみ**(後述の「定数式」．通常の変数や関数呼び出しは初期化の実行順序が不定になるため不可)．初期化子の値を書き込む時点は「main関数」を参照
- 固定長配列に対応する(`int table[10]`，`char buf[64]`，`short arr[4]`)
  - サイズは定数式で指定する
  - 初期化子(初期値リスト)には非対応(値は保証されない)
  - 要素単位で読み書きする．変数インデックスにも対応する(`buf[i]`，`table[i + 1] = x`)．配列そのもの(`buf`・`v.array`等)は，代入・`++`/`--`の対象にできない
  - char(1ワードに4要素)・short(1ワードに2要素)はRM/WMのmaskで該当バイト位置を直接指定してサブワードアクセスする(maskのビット位置とメモリ番地・レジスタのバイト位置が直接対応するため，シフト命令は不要)
- 文字列リテラル(記法は「リテラル」を参照)を次の用途に使える
  - `char msg[] = "hello";` のようにサイズ省略の配列宣言の初期化子として使う(サイズは文字列長+1(ヌル終端)から決定する)
  - 上記以外の式中(関数呼び出しの引数等)でも使え，その場合は匿名のグローバルchar配列として扱われる(1回だけメモリへ書き込まれ，以降は固定アドレスを参照する．書き込む時点は「main関数」を参照)
  - ポインタの仕組みは持たないため，文字列を受け取れるのは配列パラメータ(`char msg[]`)のみ

## const変数

`const`を付けて宣言した整数型のスカラー変数は，値がコンパイル時に確定する定数になる．  
メモリを確保せず，参照箇所へ値を直接埋め込む(`#define`による置換と同等)．このため定数式の中でも使える．

```c
const int CAP = 4;
int table[CAP];                  // 配列サイズに使える
void main(void) {
    for (int i = 0; i < CAP; i++) {
        table[i] = 0;
    }
}
```

- グローバル・ローカル(for文の初期化部を含む)のどちらでも宣言でき，初期化子は必須
- 値が宣言した型の範囲(「データ型」を参照)を外れる場合はコンパイルエラー(通常の変数への格納と異なり，下位ビットへの切り詰めは行わない．「型変換」を参照)．初期化子の値は定数式の型に従って判定するため，`const unsigned int A = -1;`・`const unsigned int B = ~0;`(いずれも`int`の`-1`)，`const int C = 0xFFFFFFFF;`(`unsigned int`の4294967295)はエラーになる．それぞれ`0xFFFFFFFF`・`-1`と書く
- 値を書き換える操作(代入・`++`/`--`・組み込み関数の書き込み先への指定等)の対象にするとコンパイルエラー
- 式の中では，宣言した型の値として扱う(`const unsigned int U = 1;`の`U`は`unsigned int`)
- `sizeof(const変数)`は宣言した型のバイト数を返す
- 名前の重複・シャドーイング・ハードウェア変数との同名禁止は，通常の変数と同じ規則に従う
- 初期化子では，後方で宣言したconst変数の参照や，後方で宣言した変数の`sizeof`も使える(宣言どうしが循環して依存する場合は「グローバルスコープ」を参照)
- 配列・構造体(メンバを含む)・関数パラメータ・関数の戻り値型には付けられない(配列・構造体は初期値リストに非対応で，値を与える手段がないため)
- `const`は符号修飾子・型名より前にのみ書ける(`int const x`・`unsigned const int x`は不可)

## 定数式

コンパイル時に値を計算する式．整数リテラル・文字リテラル(「リテラル」を参照)・const変数・`sizeof`と，代入・`++`/`--`以外の演算子で構成する．通常の変数の参照や関数呼び出しは含められない．  
定数式を要求する箇所は，グローバル変数の初期化子・配列サイズ(構造体メンバの配列・構造体配列を含む)・`case`の値・const変数の初期化子．

定数式は，同じ式を実行時に計算した場合と同じ値になるよう，「型変換」の規則(整数昇格・演算の符号・32ビットでの折り返し)に従って計算する(`2147483647 + 1`は`-2147483648`，`0xFFFFFFF0 / 2`は`2147483640`)．シフト量も実行時と同じく下位5ビットのみを使う(`1 << 33`は`2`)．  
0での除算・剰余と，`int`の最小値を`-1`で割る除算・剰余は，実行時にCPUが停止する，または結果が定まらないためコンパイルエラーになる([halt.md](halt.md)を参照)．  
`case`の値は条件の値とビット列の一致で比較するため，ビット列が同じ値(`-1`と`0xFFFFFFFF`)を複数の`case`に書くと重複としてコンパイルエラーになる．

## 構造体

固定メンバ構成の`struct`に対応する．メンバは整数型のスカラーまたは固定長配列のみで構成する．  
ネスト構造体(メンバに別の構造体を持つこと)・構造体ポインタ・メンバの初期化子には非対応(将来対応予定はなし)．  
構造体を関数の引数・戻り値にすることにも非対応(ポインタを持たないため，対応する場合は全メンバの値コピーが必要になる)．

各メンバは(配列を含め)ワード単位で確保する．`char`/`short`型のスカラーメンバも1ワード(4バイト)を占有する(構造体に属さないスカラー変数と同じ割り当て方針．「変数」節を参照)．  
構造体配列(`struct Tag arr[N];`)は，この構造体1個分のワード数×要素数ぶんの領域を連続して確保する．

構造体定義は以下の3形式に対応する(いずれもグローバルスコープ直下でのみ記述可能)．

```c
struct Tag { int x; char c; };       // 構造体名の定義のみ(この時点では変数を作らない)
struct Tag2 { int x; } v1;           // 定義と同時に変数v1を宣言
struct { int x; } v2;                // 無名構造体(構造体名を持たない)．再利用できないため変数宣言は必須
```

変数宣言は`struct Tag v;`(単一変数)・`struct Tag arr[N];`(配列，要素数は定数式で明示する)の両方に対応する(グローバル・ローカルとも1変数につき1個の`struct`文)．  
カンマ区切りの複数変数宣言(`struct Tag v1, v2;`)には非対応(スカラー型の変数宣言と同様，カンマ区切り自体が未対応のため)．

メンバへのアクセスは`v.scalar`(スカラーメンバ)・`v.array[i]`(配列メンバの要素)・`arr[i].scalar`(構造体配列要素のスカラーメンバ)・`arr[i].array[j]`(構造体配列要素の配列メンバの要素)の形式で読み書きできる．  
構造体変数・構造体配列の要素そのもの(`v`・`arr[i]`)は，メンバアクセスを介さずに直接読み書きすることはできない．  
`++`/`--`を適用できるメンバは`v.scalar`のみである(「未対応機能」を参照)．

メンバを関数の引数に渡す場合の対応状況は次のとおり(組み込み関数に渡す場合は「組み込み関数」を参照)．

- 対応
  - スカラーパラメータへ: `v.scalar`・`v.array[i]`・`arr[i].scalar`・`arr[i].array[j]`
  - 配列パラメータへ: `v.array`
- 非対応
  - 配列パラメータへ: `arr[i].array`(配列パラメータへ書き込む先頭アドレスはコンパイル時に確定したシンボルの番地として生成しており，実行時に計算したアドレスを渡すコード生成に対応していないため)

## 演算子

| 分類 | 演算子 |
|:-|:-|
| 算術 | `+`，`-`，`*`，`/`，`%` |
| ビット | `&`，`\|`，`^`，`~`，`<<`，`>>` |
| 論理 | `&&`，`\|\|`，`!` |
| 比較 | `==`，`!=`，`<`，`>`，`<=`，`>=` |
| 代入 | `=`，`+=`，`-=`，`*=`，`/=`，`%=`，`&=`，`\|=`，`^=`，`<<=`，`>>=` |
| 単項 | `-`(単項マイナス)，`+`(単項プラス)，`++`，`--` |
| その他 | `?:`(三項演算子)，`sizeof` |

1つの式の複雑さには上限があり，超えるとコンパイルエラーになる．入れ子が浅くても，組み込み関数のように評価に多くの作業領域を使う構文を含むと上限に達しうる(limitations.mdの「その他の量的制限」を参照)．

`sizeof`は`sizeof(型名)`(整数型．`sizeof(unsigned int)`のように符号修飾子も書ける)または`sizeof(変数名)`(スカラー変数・配列変数・構造体変数(配列を含む))のみに対応する(非対応の詳細は「未対応機能」を参照)．  
結果は常に`int`型のコンパイル時定数(要素数×要素型のバイト数)であり，実行時命令は生成しない．スカラー変数・配列とも，実際の占有量ではなくワード境界を考慮しない論理サイズを返す(1ワードを占有する`char`のスカラー変数でも`1`)．配列パラメータ(`char msg[]`等，サイズ不明)には非対応．  
`sizeof(構造体変数.member)`(構造体メンバ単体のサイズ)・`sizeof(struct Tag)`(型名としての構造体名)には非対応．構造体を関数の引数として受け取ること自体が非対応のため，「引数として受け取った構造体」に対する`sizeof`は考慮の対象外(そもそも記述できない)．

## 型変換

式の値は32ビットの`int`または`unsigned int`として扱う．どちらになるかと，演算を符号付き・符号なしのどちらで行うかは，C言語と同じ整数昇格の規則で決まる．

- 整数昇格: 演算のオペランドが`char`/`short`/`unsigned char`/`unsigned short`の場合は，`int`に変換してから演算する(どの値も`int`で表せるため)．`unsigned int`だけが符号なしのまま残る
- 二項演算(算術・ビット・比較)と三項演算子の結果の値: 昇格後のオペランドのどちらかが`unsigned int`なら符号なしで演算し，結果の型は`unsigned int`になる．そうでなければ符号付きで演算し，結果の型は`int`になる
- シフト`<<`・`>>`: 左オペランドの昇格後の型だけで決まる(右オペランドの型は影響しない)
- 単項演算子`-`・`+`・`~`: オペランドの昇格後の型になる(`unsigned int`の`-u`は`unsigned int`)
- 結果が`int`で固定のもの: 比較・`!`・`&&`・`||`の結果(`0`/`1`)，`sizeof`の結果(C言語の`size_t`と異なり符号付き．`int i = -1;`のとき`i < sizeof(buf)`は真)
- 複合代入`x op= e`は`x = x op e`と同じ規則で演算する
- 代入式の値の型は左辺の型，`++`/`--`の値の型はオペランドの型，関数呼び出しの値の型は戻り値型であり，式の中で使う際は上記の整数昇格を受ける

演算の符号によって，次の演算の結果が変わる．それ以外の演算(`+`・`-`・`*`・`&`・`|`・`^`・`<<`・`==`・`!=`)は符号によらず同じ結果になる．

| 演算 | 符号付き | 符号なし |
|:-|:-|:-|
| 右シフト`>>` | 空いた上位ビットを符号ビットで埋める算術シフト(`-8 >> 1`は`-4`) | 空いた上位ビットを0で埋める論理シフト |
| 大小比較`<`・`>`・`<=`・`>=` | 2の補数として比較する | 0〜4294967295の値として比較する(`unsigned int u = 0;`のとき`u < -1`は真．`-1`が`0xFFFFFFFF`として比較されるため) |
| 除算`/`・剰余`%` | 商は0方向へ切り捨て，剰余の符号は被除数に従う | 0〜4294967295の値として除算する |

値の読み出しと格納は次のとおり．

- 演算は32ビットで行い，結果は32ビットの範囲で折り返す．オペランドが`char`/`short`でも，演算結果をその型の幅に切り詰めることはない(`char a = 100;`のとき`a + a`は`200`)
- `char`/`short`の変数・配列要素・構造体メンバは，読み出した時点で符号付きの値として`int`に変換される(符号拡張)．`unsigned char`/`unsigned short`は上位ビットを0で埋めた値になる(ゼロ拡張．`unsigned char c = 200;`の`c`は`200`)
- 8/16ビットの型の変数・配列要素・構造体メンバへ値を格納する際は，初期化・代入・`++`/`--`・スカラーパラメータへの引数の受け渡しなど操作の種類によらず，下位8/16ビットのみを格納し上位ビットは捨てる．32ビットの型どうし(`int`と`unsigned int`)の格納はビット列をそのまま格納する(`unsigned int u = -1;`の`u`は`4294967295`)．いずれもエラー・警告は出ない(`char c = 300;`の`c`は`44`)．関数の戻り値は格納に当たらず切り詰めない(「関数」を参照)
- 変数の値を型の異なるスカラーパラメータへ渡す場合も，上記の読み出しと格納の規則がそのまま適用される(`char c = -1;`を`int`のパラメータへ渡すと`-1`，`int n = 300;`を`char`のパラメータへ渡すと`44`)
- 格納した値は読み出し時に格納先の型で解釈されるため，格納前の値と一致しないことがある(`char c = 200;`のとき`c == 200`は偽になり，`case 200:`にも一致しない)
- 代入式・`++`/`--`の値は，格納時に切り詰める前の値である(`char c = 127;`のとき`x = ++c;`で`x`は`128`，`c`は`-128`)．C言語では格納後の値になるため，8/16ビットの型では結果が食い違う(`unsigned char c = 0;`のとき`--c`の値は`-1`，`(c = -1) > 0`は偽．`return -1;`する`unsigned char`の関数`f`で`f() == 255`は偽)
- キャスト演算子(`(unsigned int)x`)は対応しない(「未対応機能」を参照)．符号を付け替えるには，目的の型の変数へ代入して読み直す

## 制御構文

| 構文 | 備考 |
|:-|:-|
| `if` / `else if` / `else` | |
| `for` | |
| `while` | |
| `do`-`while` | |
| `switch` / `case` / `default` | |
| `break` | |
| `continue` | |
| `return` | `return;`(値なし)と `return expr;`(値あり)の両方に対応．どちらを書けるかは関数の戻り値型による(「関数」を参照) |

## ソースファイルの取り込み

`#include "パス"`を書くと，その位置に指定したファイルの内容が書かれていたものとして，1つのプログラムにまとめてコンパイルする．

```c
// main.pn
#include "lib/io.pn"      // main.pnがあるディレクトリのlib/io.pnを取り込む
void main(void) {
    put_line("hello");
}
```

- パスは`"`で囲む(`<...>`形式は非対応)．取り込み指令を書いたファイルがあるディレクトリからの相対パス，または絶対パスで指定する
- 取り込むファイルの拡張子は`.pn`に限る
- 指令の`#`は行の1文字目に書く(前に空白を置けない)．指令の後ろの同じ行にはコメントのみ書ける
- 1つのファイルに書いた宣言はそのファイルの中で完結させ，指令はグローバルスコープの宣言と宣言の間にのみ書ける(ファイルが宣言の途中で終わる場合や，関数本体・構造体定義の中，宣言の途中に指令を書いた場合はコンパイルエラー)．取り込んだファイルの宣言は，取り込み元の宣言と区別なくプログラム全体から参照できる(「グローバルスコープ」を参照)
- `#pragma once`(後述)が書かれていないファイルを1回のコンパイルで2回以上取り込むと，コンパイルエラーになる．同じファイルからの重複か，別のファイルを経由した間接的な重複かは問わない．`-pn`で指定したファイル自身を取り込む場合も同様
- 取り込みが循環する場合(`a.pn`が`b.pn`を，`b.pn`が`a.pn`を取り込む等)も，`#pragma once`で無視されない限りコンパイルエラーになる

### #pragma once

ファイルに`#pragma once`を書くと，そのファイルの2回目以降の取り込みはエラーにならず無視される．複数のファイルから共通に取り込むファイルに書く．

- `#include`と同じく宣言と宣言の間であればどこに書いてもよく，同じファイルに複数回書いてもよい
- 効果は`#pragma once`を読んだ時点から生じる．取り込みが循環する場合，そのファイルの字句解析の途中で再び取り込まれるため，循環のもとになる`#include`より前に書いておく必要がある．ファイルの先頭に書くことを推奨する
- `#pragma`に`once`以外を書くとコンパイルエラー

`#include`・`#pragma once`以外の指令(`#define`等)はコンパイルエラーになる．

## グローバルスコープ

ファイル直下(グローバルスコープ)に置かれた宣言はすべて，取り込んだファイル(「ソースファイルの取り込み」を参照)を含むプログラム全体のどこからでも順序問わず参照できる．  
関数定義とグローバル変数宣言(const変数を含む)の両方に適用され，定数式から参照する場合も同様である．  
ただし，宣言の値やサイズを求めるためにその宣言自身へ戻ってくる循環した依存はコンパイルエラーになる(例: `const int A = B; const int B = A;`，`int buf[sizeof(buf)];`)．

## 関数

- 戻り値型は `void` と整数型に対応する(`main`は「main関数」を参照)
- 引数は整数型のスカラーおよび配列パラメータ(`int arr[]` 等)のカンマ区切りに対応する(`main`は「main関数」を参照)
  - スカラーパラメータには型の異なるスカラー式も渡せ，値はパラメータの型で格納される(「型変換」を参照)
  - 配列パラメータには要素型が同じ配列(文字列リテラルは`char`配列)のみ渡せ，要素型が異なる配列を渡すとコンパイルエラーになる．符号も要素型に含む(`unsigned char`の配列は`char`の配列パラメータへ渡せず，文字列リテラルは`unsigned char`の配列パラメータへ渡せない)
  - 配列パラメータは呼び出し元が配列のベースアドレスを書き込み，呼び出し先が間接参照でアクセスする
- 引数なしの宣言は `(void)` と `()` の両方を許容する
- void関数は `return;` で，非void関数は `return expr;` で呼び出し元へ復帰する．戻り値はRAX(`6'h1e`)を経由する．void関数が`return`文を通らずに関数本体の終端へ達した場合は，`return;`と同じく復帰する(`main`での復帰の扱いは「main関数」を参照)
- `return`文の形式が戻り値型と食い違う場合(`void`関数の`return expr;`，非void関数の`return;`)はコンパイルエラーになる．検査するのは値を返すか返さないかの一致のみで，戻り値型と式の型の一致は検査せず，戻り値型の幅への切り詰めも行わない(`char`の関数が`return 300;`と書けば300がそのまま渡る)．すべての関数定義が対象であり，`main`から一切呼ばれない関数も検査される
- 引数の受け渡しは呼び出し元が引数を評価し，呼び出し先のパラメータのメモリアドレスに直接書き込む方式(callee-static)

### 関数に関する制限

- 関数のオーバーロードには対応しない(関数名で一意に特定する)
- デフォルト引数には対応しない
- 同一関数の入れ子呼び出し(例: `add(1, add(2, 3))`)は正しく動作しない(内側の呼び出しが外側の引数書き込み済みのパラメータを上書きするため)．異なる関数同士の入れ子(例: `add(five(), 3)`)は問題ない
- 関数呼び出しネストが**10段**(戻り先レジスタ`6'h11`〜`6'h1a`の本数)を超える場合はコンパイルエラーになる．段数は1つの呼び出し経路上で入れ子になる`call`の数であり，`call`されず戻り先レジスタを消費しない`main`自身は算入しない(`main`から呼ばれる関数が1段目となる)．呼び出しグラフ(`main`を起点に静的に辿れる呼び出し関係)から検査するため，実行時に到達しない経路も含めて判定される．組み込み関数はこの段数に算入されない(「組み込み関数」を参照)
- 再帰呼び出し(直接・間接を問わない)は，呼び出しグラフに循環が見つかった時点でコンパイルエラーになる
- 上記2つの検査は`main`を起点とした探索でのみ行われるため，`main`から一切呼ばれない関数(未使用関数)の内部でのネスト超過・再帰は検出されない

### main関数

`main`はプログラムの開始点となる関数であり，定義は必須(定義がない場合はコンパイルエラー)．  
開始点として実行が始まる`main`には値を受け渡す呼び出し元が存在しないため，戻り値型は`void`とし，引数は取らない．これに違反する定義(`int main(void)`・`void main(int a)`等)はコンパイルエラーになる．引数なしの書き方は関数一般と同じく`(void)`・`()`のいずれでもよい(「関数」を参照)．`main`での`return;`および関数本体の終端への到達は，呼び出し元への復帰ではなくプログラムの終了を表す(生成される`main`の`ret`の動作は[assembler.md](assembler.md)を参照)．

初期化子を持つグローバル変数への書き込みと，式中に現れる文字列リテラル(匿名のグローバルchar配列)へのメモリ書き込みは，`main`の本体を実行する前に1回だけ行う．`main`より前に実行されるコードは他に存在しないため，これらの書き込みがプログラム中で最初に実行される．

## 組み込み関数

標準ライブラリは提供しない．代わりに以下の組み込み関数を提供する．  
`print`/`scan`/`streq`/`strcopy`は予約語であり，同名の変数・関数を宣言することはできない(宣言した時点でコンパイルエラーになる)．  
いずれも呼び出し箇所へ命令列を直接展開する(`call`を伴わない)ため，関数呼び出しネストの段数を消費しない．

| 関数 | 説明 | 対応ISA命令 |
|:-|:-|:-|
| `print(buf)` | `char`配列`buf`の先頭からヌル終端(`'\0'`)まで1文字ずつ標準出力に出力する | PRINT |
| `scan(buf)` | 標準入力から改行(`'\n'`)までの1行を読み込み，`char`配列`buf`へヌル終端付きで格納する | SCAN |
| `streq(a, b)` | `char`配列`a`・`b`の内容が一致するか比較し，一致すれば`1`，不一致なら`0`を返す(`int`) | なし(既存の比較・メモリアクセス命令のみで実装) |
| `strcopy(dst, src)` | `char`配列`src`の内容をヌル終端付きで`char`配列`dst`へコピーする | なし(既存のメモリアクセス命令のみで実装) |

引数には`char`型または`unsigned char`型の配列を渡す(表の`char`配列はどちらも指す)．渡せる配列の種類は関数ごとに次のとおり異なる．

| 引数 | `print`/`streq`/`strcopy` | `scan` |
|:-|:-|:-|
| 固定サイズで宣言した配列変数(`buf`) | ○ | ○ |
| 単一の構造体変数のメンバ配列(`entry.name`) | ○ | ×(「未対応機能」を参照) |
| 構造体配列要素のメンバ配列(`arr[i].name`) | × | × |
| 配列パラメータ(`char buf[]`) | × | × |
| スカラー変数・上記以外の式 | × | × |

すべての関数で渡せないものの理由は次のとおり．

- 構造体配列要素のメンバ配列: アドレスを実行時に計算することになり，各関数のコード生成が前提とする「コンパイル時に確定したアドレス」と相容れない
- 配列パラメータ: 各関数が打ち切りの判定に使う宣言サイズが分からない
- スカラー変数・上記以外の式: オーバーロードに対応しないため，用途ごとに別の組み込み関数を用意する設計は取らない

各関数の動作の詳細は次のとおり．

- `print`: 配列の宣言サイズに達してもヌル終端が見つからない場合は，そこで安全に打ち切る
- `scan`: 標準入力の先頭に改行が続く場合はすべて読み飛ばし，空行として受け取らない(配列に格納されるのは実際の内容がある行のみ)．入力が配列サイズ-1文字を超える場合はそこで打ち切り，超過分(改行を含む)は次に`scan`を実行したときに読み込まれる(破棄しない)．対象配列は**2要素以上**(最低1文字+ヌル終端)が必要
- `streq`: 先頭から1文字ずつ比較し，不一致文字が出た時点，または片方だけが先にヌル終端に達した時点で不一致(`0`)．両方同時にヌル終端に達すれば一致(`1`)．どちらかの宣言サイズに達してもヌル終端が見つからない場合は，安全のため不一致(`0`)として扱う
- `strcopy`: `src`の宣言サイズに達してもヌル終端が見つからない場合は，そこで安全に打ち切る(`print`と同じ考え方)．`dst`の宣言サイズ-1文字を超える場合は，そこで打ち切ってヌル終端を書き込む(`scan`と同じ安全な打ち切り)

## 定義済みハードウェア変数

LED・DIPスイッチ等のI/Oレジスタを，あらかじめ定義された変数として提供する．  
通常の変数と同様に代入・参照でき，コンパイラが対応するレジスタ番地への読み書き命令に変換する．

```c
LED = 0x0f;      // LEDレジスタへの書き込み
int x = DIPSW;   // DIPスイッチレジスタからの読み込み
```

変数名と対応するレジスタ番地は以下の通り．読み書き可否は[register.md](register.md)に従い，違反した場合はコンパイルエラーとなる．

| 変数名 | 番地 | 内容 | 読み | 書き |
|:-|:-|:-|:-|:-|
| `BTN` | `6'h20` | タクトスイッチ×4 | 可 | 不可 |
| `DIPSW` | `6'h21` | DIPスイッチ×2 | 可 | 不可 |
| `LED` | `6'h22` | LED×4 | 不可 | 可 |
| `RGBLED` | `6'h23` | RGB LED×2 | 不可 | 可 |
| `PMOD_A` | `6'h24` | Pmod A | 可 | 可 |
| `PMOD_B` | `6'h25` | Pmod B | 可 | 可 |
| `AR8_13` | `6'h26` | Arduinoピン AR8〜AR13 | 可 | 可 |
| `AR_I2C` | `6'h27` | A(AREF)，AR_SDA，AR_SCL | 可 | 可 |
| `AR0_7` | `6'h28` | Arduinoピン AR0〜AR7 | 可 | 可 |
| `AR_RST` | `6'h29` | Arduinoリセット | 可 | 不可 |
| `AR_SPI` | `6'h2A` | AR_MISO，AR_SCK，AR_MOSI，AR_SS | 可 | 可 |
| `GPIO0_7` | `6'h2D` | GPIO0〜7 | 可 | 可 |
| `GPIO8_15` | `6'h2E` | GPIO8〜15 | 可 | 可 |
| `GPIO16_23` | `6'h2F` | GPIO16〜23 | 可 | 可 |
| `GPIO24_27` | `6'h30` | GPIO24〜27 | 可 | 可 |

型は全て`int`として扱う．

次のレジスタは変数として公開しない(各レジスタの制約は[register.md](register.md)を参照)．

- CPU内部レジスタ(`6'h00`〜`6'h1f`)．番地ごとの理由は次のとおり
  - r0〜r15・RAX: コンパイラがコード生成に内部で使用する
  - SP・戻り先レジスタ(`6'h11`〜`6'h1a`)・空き番地(`6'h1b`): ハードウェア上で読み書きが禁止されている
  - FLG・RSI: 未実装であり，意味のある値を持たない
  - PC: 書き込みが禁止されている．読み出すとその命令自身の番地が返るが，番地を扱えない本言語では使い道がない
- 標準入出力レジスタ(`6'h31`〜`6'h34`): 組み込み関数`print`/`scan`経由でのみ使用する
- アナログピン(`6'h2B`)・XADC(`6'h2C`): 現時点でCPUで使用する予定がなくオミットされている

## コメント

```c
// 行コメント

/* ブロックコメント */
```

## 未対応機能

スモールスタートの方針により，以下は当面対応しない．

| 機能 | 備考 |
|:-|:-|
| `#define` / `#ifdef` 等のプリプロセッサ指令 | 「ソースファイルの取り込み」に記載した指令以外は非対応．定数の定義にはconst変数を使う(「const変数」を参照) |
| `sizeof(計算式)` | `sizeof(1 + 2)`のようなC言語の任意の式への対応は非対応．型名・変数名のみ対応(analyzer.cppのanalyze_expr参照)．計算式の最終的な型を推論する仕組み自体がないため(analyzer.cppのTODOコメント参照)．将来対応予定 |
| ポインタ | |
| `long` | 将来対応予定 |
| `float` / `double` | ISAに浮動小数点命令なし |
| キャスト演算子(`(型)式`)・整数リテラルの接尾辞(`10u`等) | 符号の付け替えは目的の型の変数への代入で行う(「型変換」を参照) |
| `union` | |
| カンマ区切りの複数変数宣言・`typedef`・構造体を関数の引数/戻り値にすること | `struct`自体(固定メンバ構成，単一変数・配列の宣言，`v.member`／`arr[i].member`アクセス)は対応済み(「構造体」節を参照)．上記3点は個別に対応検討予定(pynesisリポジトリ issue #15(カンマ区切り)・#16(typedef)・#17(引数/戻り値)) |
| `static` 変数 | |
| `goto` / ラベル文 | |
| カンマ演算子 | 対応予定なし．カンマは関数のパラメータ宣言と関数呼び出しの引数(組み込み関数を含む)の区切りとしてのみ使用する |
| 動的メモリ管理(`malloc`等) | |
| 標準ライブラリ全般(`printf`，`scanf`等) | |
| 配列添字の範囲外アクセス検査 | コンパイル時・実行時ともに検査しない．C言語と同様，未定義動作として扱う |
| 配列要素・構造体配列要素のメンバへの`++`/`--`(`arr[i]++`・`++v.array[i]`・`arr[i].scalar++`等) | `++`/`--`はスカラー変数と構造体変数のスカラーメンバ(`v.scalar`)のみに対応．実行時に番地を計算する対象の増減はコード生成が未対応のため |
| `scan`の対象を構造体のメンバ配列にすること | `scan`は現状，識別子1個(配列変数そのもの)のみを対象とする．メンバ配列(単一の構造体変数のもの)はコンパイル時に確定したアドレスに配置されるため技術的には対象にできるが，`print`等のように任意の式を受け付ける構文へは未拡張．将来対応予定 |
| `int`の最小値(`-2147483648`)のリテラル表現 | 10進の整数リテラルに書ける値の範囲(「リテラル」を参照)に含まれないため，単項マイナスと組み合わせても`-2147483648`は表現不可．16進の`-0x80000000`は`unsigned int`の`0x80000000`になる(「型変換」を参照)．`-2147483647 - 1`等で代替可能 |

---

# コンパイラ仕様

## メモリ配置

### アドレッシング方式

メモリ上に置く変数は，グローバル変数・ローカル変数を問わず**絶対アドレス**に配置する．番地はコンパイル時(意味解析)に確定し，シンボルテーブルに保持する．アセンブリ言語の生成では，この番地をそのまま命令の即値として埋め込む．

ローカル変数もグローバル変数と同じく絶対アドレスに配置するのは，SP(スタックポインタ，`6'h10`)がハードウェア実装上読み書き禁止のためデータスタックを構築できず，呼び出しごとのフレーム確保ができないためである．この制約により**再帰呼び出しは非対応**(詳細はlimitations.mdを参照)．  
同じ理由で，**同一関数の入れ子呼び出し**(例: `add(1, add(2, 3))`)も正しく動作しない．内側の呼び出しが外側の引数書き込み済みのパラメータを上書きするため．異なる関数同士の入れ子(例: `add(five(), 3)`)は問題ない．

変数配置の将来的な移行計画はlimitations.mdを参照．

## レジスタ使用規約

すべての変数はメモリ上に置くため，汎用レジスタr0〜r15(`6'h00`〜`6'h0f`)は変数の保持には使わず，**式評価のための一時レジスタ群**として使用する．

- r0〜r15は一時レジスタ．**`call`をまたいで値は保持されない**(揮発)．呼び出し後も必要な値はメモリ(変数)に置く
    - 外から見た動作: callをまたぐとr0〜r15の値は保証されない(残っていないものとして扱う)
    - 実際の動作: ハードウェアはレジスタを物理的にリセットしない．呼び出し先が自由に上書きしてよいという規約により，結果として値が壊れるだけ(各関数のコード生成がr0から番号を振り直すため低位レジスタが衝突する)
- 式の評価はr0から順に割り当てる**レジスタスタック方式**(部分式の結果を低位レジスタから積み，演算で畳んで解放する)．式の複雑さの上限はlimitations.mdの「その他の量的制限」を参照
- 関数の戻り値・演算結果の最終的な置き場はRAX(`6'h1e`)を用いる

`call`間でレジスタが揮発するのは，戻り先アドレスのみをハードウェアが専用レジスタに退避し(`6'h11`〜`6'h1a`)，汎用レジスタを退避するスタックが存在しないため(詳細は limitations.md を参照)．

式の評価中にサブ式として関数呼び出しを含む場合，呼び出しをまたいで生かしたい他のレジスタの値は，呼び出し前後で一時メモリ領域(退避領域)へ退避・復元する．退避領域は全変数のアドレス割り当て後に確保する固定番地．

## ファイル仕様

- 入力: ソースファイル(`.pn`)．`#include`で取り込んだファイルと合わせて1つのプログラムとしてコンパイルする
- 出力: アセンブリ言語ファイル(`.pt`)．コード生成より前の段階でコンパイルエラーになった場合は作らない
- ソース上の位置を特定できるコンパイルエラーは，`compiler error: <内容> at <ファイル名>:<行番号>`の形式で出力する

## 実行ファイル構成と起動引数

| 実行ファイル | 変換範囲 |
|:-|:-|
| `pn2asm.exe` | Pynesisソース(`.pn`) → アセンブリ(`.pt`) |
| `asm2sv.exe`(アセンブラ，`../assembler/`) | アセンブリ(`.pt`) → SystemVerilog ROM(`.sv`)．起動引数は`assembler.md`の「起動引数」を参照 |
| `pn2sv.exe` | Pynesisソース(`.pn`) → SystemVerilog ROM(`.sv`) まで一貫変換．今後のコンパイラの入口 |

`pn2sv.exe`は`pn2asm.exe`・アセンブラの本処理をそれぞれ`main`から分離した関数として直接リンクし，順に呼び出す(サブプロセス起動はしない)．  
`pn2asm.exe`・`asm2sv.exe`は単体の実行ファイルとしても引き続き動作する．

### pn2asm.exe

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pn` | 入力Pynesisソースファイル名(`.pn`)．必須 | - |
| `-pt` | 出力アセンブリファイル名(`.pt`) | `-pn`の拡張子を`.pt`に変えた名前を自動的に使う |

例: `pn2asm.exe -pn program.pn -pt program.pt`

同じフラグ(`-pn`／`-pt`)や指定子なし引数を複数回指定した場合，エラーにはならず最後に指定した値で上書きされる(後勝ち)．

### pn2sv.exe

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pn` | 入力Pynesisソースファイル名(`.pn`) | 必須(省略不可) |
| `-pt` | 中間生成物のアセンブリファイル名(`.pt`) | 必須(省略不可) |
| `-sv` | 出力SystemVerilog ROMファイル名(`.sv`) | `-pt`の拡張子を`.sv`に変えた名前を自動的に使う(`asm2sv.exe`単体の場合と同じ自動導出がそのまま働く) |

例: `pn2sv.exe -pn program.pn -pt program.pt -sv program.sv`

内部で呼び出す`pn2asm.exe`/`asm2sv.exe`相当の引数解析をそのまま使うため，`-pn`/`-pt`/`-sv`を複数回指定した場合の後勝ち上書きも同様に発生する．
