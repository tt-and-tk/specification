# Pynesisの言語・コンパイラ仕様

## 概要

Pynesisの言語仕様，およびそのコンパイラの仕様．

---

# 言語仕様

## 識別子

変数名・関数名等の識別子は，アルファベットまたは`_`で始まり，以降はアルファベット・数字・`_`のみで構成される．ただし，数値表記・レジスタ表記として読める綴り(`fh`・`r5`等)の関数名はアセンブラが受け付けない([assembler.md](assembler.md)を参照)．

## データ型

| 型 | ビット幅 | 値の範囲 | 備考 |
|:-|:-|:-|:-|
| `char` | 8ビット | -128〜127 | |
| `short` | 16ビット | -32768〜32767 | |
| `int` | 32ビット | -2147483648〜2147483647 | |
| `unsigned char` | 8ビット | 0〜255 | |
| `unsigned short` | 16ビット | 0〜65535 | |
| `unsigned int` | 32ビット | 0〜4294967295 | |
| ポインタ(`int *`等) | 32ビット | 番地 | 型名の後ろに`*`を付ける(「ポインタ」を参照) |
| `void` | - | - | 戻り値なし関数(関数ポインタを含む)の戻り値型と，引数なしの宣言`(void)`(「関数」を参照)にのみ使用 |

`char`/`short`/`int`の直前に符号修飾子`signed`(符号付き)または`unsigned`(符号なし)を書ける．省略時は`signed`である．  
符号修飾子は`char`/`short`/`int`の直前にのみ書け，単独(`unsigned x`)・重ね書き(`signed unsigned int`)・`void`や構造体型への付与はコンパイルエラーになる．`const`と組み合わせる場合は`const unsigned int`の順に書く(「const変数」を参照)．  
以降，`char`/`short`/`int`とその符号なし型をまとめて整数型と呼び，整数型とポインタをまとめてスカラー型と呼ぶ．  
`long` は将来対応予定．`float` / `double` はISAに浮動小数点命令がないため対応しない．

## リテラル

### 整数リテラル

| 記法 | 例 |
|:-|:-|
| 10進 | `10`，`255` |
| 16進(`0x`または`0X`に続けて1桁以上の16進数字) | `0x0f`，`0XFF` |

- 書ける値は，10進が`0`〜`2147483647`，16進が`0x0`〜`0xFFFFFFFF`で，範囲外はコンパイルエラーになる
- 型は`int`．ただし`int`で表せない`0x80000000`以上の値(16進でのみ書ける)は`unsigned int`になる(C言語と同じ)
- 負の値はリテラルに単項マイナスを付けて表す
- 型を指定する接尾辞(`10u`等)は書けない．小さい値を符号なしとして扱うには，`(unsigned int)10`のようにキャストする(「キャスト」を参照)
- 先頭が`0`でも8進ではなく10進として読む(`010`は`10`)．C言語とは解釈が異なる

### 文字リテラル

- ASCIIの1文字またはエスケープシーケンス1つを`'`で囲む(`'a'`，`'\n'`)．空(`''`)や2文字以上はコンパイルエラーになる
- 型は`char`，値はその文字の文字コード

### 文字列リテラル

- 文字とエスケープシーケンスの並びを`"`で囲む(`"hello"`)．途中で改行することはできない
- 隣接した文字列リテラルは1つに連結される(`"C" "D"`は`"CD"`)．間に空白・改行・コメントを挟んでも連結される

### エスケープシーケンス

文字リテラル・文字列リテラルで共通に使える．

| 記法 | 意味 |
|:-|:-|
| `\n` | 改行 |
| `\t` | タブ |
| `\r` | 復帰 |
| `\0` | ヌル文字 |
| `\\` | `\` |
| `\'` | `'` |
| `\"` | `"` |

上記以外は，`\`を取り除いた次の1文字そのものになる(`\a`は`a`)．`\x41`・`\101`のような数値によるエスケープには対応しない．8進表記を書いた場合，文字列リテラル中では`\0`とそれに続く数字として解釈され(`"\012"`は`\0`・`1`・`2`の3文字)，文字リテラル中(`'\012'`)ではコンパイルエラーになる．

## 変数

- グローバル変数・ローカル変数の両方に対応する
- const変数(後述の「const変数」)を除くすべての変数はメモリ上に配置する(レジスタへの割り当ては行わない)
  - スカラー変数は宣言場所・型によらず1ワード(4バイト)を占有する．`char`/`short`も値をワードの下位バイトに置き，他の変数と1ワードを共有しない
- ブロックスコープに対応する
- グローバル変数(const変数を含む)・関数・ハードウェア変数(後述の「定義済みハードウェア変数」)は同じ名前空間にあり，互いに同名のものは宣言できない(コンパイルエラー)．構造体名はこれらと別の名前空間にあり，同名にできる
- スコープが異なれば同名変数は別の変数として扱う(シャドーイング許容)
  - 例外として，次のものと同名のローカル変数・関数パラメータは宣言できない(コンパイルエラー)
    - ハードウェア変数: I/Oレジスタの誤上書きを防ぐため
    - 関数: 呼び出し`f(...)`の名前が，関数と関数ポインタの変数のどちらを指すかを一意にするため(「関数ポインタ」を参照)
- 宣言と同時に初期化できる(`int x = 0;`)
  - 初期化子のない変数の値は保証されない．C言語と異なり，グローバル変数も0で初期化されない([limitations.md](limitations.md)を参照)
- グローバル変数の初期化子は**定数式のみ**(後述の「定数式」．通常の変数や関数呼び出しは初期化の実行順序が不定になるため不可)．ポインタの初期化子は定数式の代わりに，番地が意味解析で確定する式を書く(「ポインタ」を参照)．初期化子の値を書き込む時点は「main関数」を参照
- 固定長配列に対応する(`int table[10]`，`char buf[64]`，`short arr[4]`，`char *names[4]`)
  - サイズは定数式で指定する
  - 初期化子(初期値リスト)には非対応(値は保証されない)
  - 要素単位で読み書きする．変数インデックスにも対応する(`buf[i]`，`table[i + 1] = x`)．配列そのもの(`buf`・`v.array`等)は，代入・`++`/`--`の対象にできない
  - 配列そのものを値として使うと，先頭要素へのポインタになる(値として使う場合と格納先の型の条件は「ポインタ」の「配列の扱い」を参照)
  - char(1ワードに4要素)・short(1ワードに2要素)はメモリ命令のmaskで該当バイト位置を直接指定してサブワードアクセスする(maskのビット位置とメモリ番地・レジスタのバイト位置が直接対応するため，シフト命令は不要)
- 文字列リテラル(記法は「リテラル」を参照)を次の用途に使える
  - `char msg[] = "hello";` のようにサイズ省略の配列宣言の初期化子として使う(サイズは文字列長+1(ヌル終端)から決定する)．初期化できるのは`char`または`unsigned char`の配列で，各要素に文字コードを格納する
  - 上記以外の式中(関数呼び出しの引数等)でも使え，その場合は匿名のグローバルchar配列として扱われる(1回だけメモリへ書き込まれ，以降は固定アドレスを参照する．書き込む時点は「main関数」を参照)．配列と同じく，値として使うと先頭要素へのポインタ(`char *`)になる

## const変数

`const`を付けて宣言した整数型のスカラー変数は，値がコンパイル時に確定する定数になる．  
メモリを確保せず，参照箇所へ値を直接埋め込む(`#define`による置換と同等)．このため定数式の中でも使える．

```c
const int CAP = 4;
int table[CAP];                  // 配列サイズに使える
void main(void) {
    for (int i = 0; i < CAP; i++) {
        table[i] = 0;
    }
}
```

- グローバル・ローカル(for文の初期化部を含む)のどちらでも宣言でき，初期化子は必須
- 値が宣言した型の範囲(「データ型」を参照)を外れる場合はコンパイルエラー(通常の変数への格納と異なり，下位ビットへの切り詰めは行わない．「型変換」を参照)．初期化子の値は定数式の型に従って判定するため，`const unsigned int A = -1;`・`const unsigned int B = ~0;`(いずれも`int`の`-1`)，`const int C = 0xFFFFFFFF;`(`unsigned int`の4294967295)はエラーになる．それぞれ`0xFFFFFFFF`または`(unsigned int)-1`，`-1`と書く
- 値を書き換える操作(代入・`++`/`--`・組み込み関数の書き込み先への指定等)の対象にするとコンパイルエラー
- 式の中では，宣言した型の値として扱う(`const unsigned int U = 1;`の`U`は`unsigned int`)
- `sizeof(const変数)`は宣言した型のバイト数を返す
- 名前の重複・シャドーイング・ハードウェア変数や関数との同名禁止は，通常の変数と同じ規則に従う
- 初期化子では，後方で宣言したconst変数の参照や，後方で宣言した変数の`sizeof`も使える(宣言どうしが循環して依存する場合は「グローバルスコープ」を参照)
- 配列・構造体(メンバを含む)・関数パラメータ・関数の戻り値型には付けられない(配列・構造体は初期値リストに非対応で，値を与える手段がないため)
- ポインタには付けられない(`const int *p`・`int *const p`はいずれも不可)．constは参照箇所へ値を埋め込む整数の定数を表すためで，指す先を書き換えられないポインタの意味は持たない
- `const`は符号修飾子・型名より前にのみ書ける(`int const x`・`unsigned const int x`は不可)

## 定数式

コンパイル時に値を計算する式．整数リテラル・文字リテラル(「リテラル」を参照)・const変数・`sizeof`と，代入・`++`/`--`以外の演算子，整数から整数へのキャスト(「キャスト」を参照)で構成する．通常の変数の参照や関数呼び出しは含められない．ポインタへのキャストとポインタから整数へのキャストは番地を値とするため含められない(グローバル変数の初期化子`int a = (int)&g;`はエラー)．  
定数式を要求する箇所は，グローバル変数の初期化子・配列サイズ(構造体メンバの配列・構造体配列を含む)・`case`の値・const変数の初期化子．

定数式は，同じ式を実行時に計算した場合と同じ値になるよう，「型変換」の規則(整数昇格・演算の符号・32ビットでの折り返し)に従って計算する(`2147483647 + 1`は`-2147483648`，`0xFFFFFFF0 / 2`は`2147483640`)．シフト量も実行時と同じく下位5ビットのみを使う(`1 << 33`は`1 << 1`と同じ`2`．CPUのシフト命令がシフト量の下位5ビットしか使わないため．[isa.md](isa.md)の「シフト系」を参照)．  
0での除算・剰余と，`int`の最小値を`-1`で割る除算・剰余は，実行時にCPUが停止する，または結果が定まらないためコンパイルエラーになる([halt.md](halt.md)を参照)．  
`case`の値は条件の値とビット列の一致で比較するため，ビット列が同じ値(`-1`と`0xFFFFFFFF`)を複数の`case`に書くと重複としてコンパイルエラーになる．

## 構造体

固定メンバ構成の`struct`に対応する．メンバはスカラー型(整数型・ポインタ)または固定長配列のみで構成する．  
ネスト構造体(メンバに別の構造体そのものを持つこと)・メンバの初期化子には非対応(将来対応予定はなし)．構造体へのポインタはメンバにでき，自身の構造体を指すメンバ(`struct Node { int v; struct Node *next; };`)も書ける．  
構造体そのものを値として使うこと(関数の引数・戻り値・代入の右辺・式文等に書くこと)にも非対応(全メンバの値をまとめて読み書きする仕組みがないため)．メンバを読み書きするか，構造体へのポインタを引数・戻り値にして受け渡す(「ポインタ」を参照)．

各メンバは(配列を含め)ワード単位で確保する．`char`/`short`型のスカラーメンバも1ワード(4バイト)を占有する(構造体に属さないスカラー変数と同じ割り当て方針．「変数」節を参照)．  
構造体配列(`struct Tag arr[N];`)は，この構造体1個分のワード数×要素数ぶんの領域を連続して確保する．

構造体定義は以下の3形式に対応する(いずれもグローバルスコープ直下でのみ記述可能)．

```c
struct Tag { int x; char c; };       // 構造体名の定義のみ(この時点では変数を作らない)
struct Tag2 { int x; } v1;           // 定義と同時に変数v1を宣言
struct { int x; } v2;                // 無名構造体(構造体名を持たない)．再利用できないため変数宣言は必須
```

変数宣言は`struct Tag v;`(単一変数)・`struct Tag arr[N];`(配列，要素数は定数式で明示する)の両方に対応する(グローバル・ローカルとも1変数につき1個の`struct`文)．  
カンマ区切りの複数変数宣言(`struct Tag v1, v2;`)には非対応(スカラー型の変数宣言と同様，カンマ区切り自体が未対応のため)．

メンバへのアクセスは`v.scalar`(スカラーメンバ)・`v.array[i]`(配列メンバの要素)・`arr[i].scalar`(構造体配列要素のスカラーメンバ)・`arr[i].array[j]`(構造体配列要素の配列メンバの要素)の形式で読み書きできる．構造体へのポインタ`p`からは`p->scalar`・`p->array[i]`・`p[i].scalar`の形式で指す先のメンバを読み書きできる(「ポインタ」を参照)．  
構造体変数・構造体配列の要素そのもの(`v`・`arr[i]`)は，メンバアクセスを介さずに直接読み書きすることはできない(`&v`・`&arr[i]`で番地を取ることはできる)．

メンバはどの形式でも関数の引数に渡せる．スカラーメンバはその値を，配列メンバは先頭要素へのポインタを渡す(組み込み関数に渡す場合は「組み込み関数」を参照)．

## ポインタ

変数・配列要素・構造体メンバ・関数の番地を値として扱う型．番地を関数へ渡すことで，構造体やバッファを値のコピーなしに受け渡せる．

### 宣言

型名の後ろに`*`を書くとその型を指すポインタになる．`*`を重ねるとポインタを指すポインタになる．

```c
int *p;                   // intを指すポインタ
unsigned char *buf;       // unsigned charを指すポインタ
struct Node *head;        // 構造体を指すポインタ
char **names;             // charを指すポインタを指すポインタ
int (*handler)(int, int); // int型の引数を2つ取りintを返す関数を指すポインタ(関数ポインタ)
void (*callback)(void);   // 引数も戻り値もない関数を指すポインタ
```

- ポインタは1ワード(4バイト)のスカラーとして扱い，変数(グローバル・ローカル)・関数の引数・戻り値・構造体のメンバ・配列の要素(`char *names[4]`)のいずれにもなれる．ただし関数ポインタは，関数の戻り値・配列の要素にはできない(「未対応機能」を参照)
- 関数ポインタは`戻り値型 (*名前)(引数の型の並び)`の形で宣言する．引数の並びには型だけを書いても，型に続けて名前を書いてもよい(名前は無視する)．引数なしは`(void)`・`()`のいずれでもよい．引数の型には関数ポインタの宣言子も書け，名前を省ける(`int (*runner)(int (*)(int), int)`)
- 関数の引数を配列の形(`char msg[]`)で宣言することはできない．ポインタ(`char *msg`)で宣言する．呼び出し側は配列をそのまま渡せる(下記「配列の扱い」を参照)
- ポインタの宣言で使えない型は「未対応機能」を参照

### nullptr

どのポインタも指していないことを表す組み込み定数．予約語であり，同名の変数・関数は宣言できない．

- 値は最上位ビットだけが立った`0x80000000`であり，メモリのアドレスバス幅を超えるため，どの変数の番地とも関数の番地とも一致しない．参照するとCPUが停止する([halt.md](halt.md)を参照)ため，誤った参照が別の変数を黙って壊すことはない
- 32ビットの範囲の中央にある値のため，メンバや添字で番地を前後にずらして参照しても，ずらす量が2^31に近くない限りアドレスバス幅を超えたままになり，同じく停止する
- C言語と異なり，ビット列は`0`ではない．`nullptr`を入れたポインタを整数へキャストした値は`0x80000000`になり，他のポインタとの差・大小比較の結果は意味を持たない．また，整数の`0`はヌルポインタとして扱わず，ポインタへ代入・比較できない．`(int *)0`は番地0を指すポインタであり，`nullptr`と等しくない
- どのポインタ型(関数ポインタを含む)へも代入・初期化・引数・戻り値として格納でき，`==`・`!=`で比較できる．加減算・大小比較・間接参照・キャストはできない
- 初期化子を持たないポインタ変数の値は，他の変数と同じく保証されない(`nullptr`になるとは限らない)

### 番地の取得と間接参照

| 式 | 意味 |
|:-|:-|
| `&x` | `x`の番地．`x`は変数・構造体変数・配列要素・構造体配列要素(`&arr[i]`)・構造体メンバ(すべての形式)・`*p`・`p[i]` |
| `*p` | `p`が指す先．読み書きでき，読み書きの幅と符号拡張は指す先の型に従う(`char`/`short`は「型変換」の読み出しと格納の規則に従う)．`p`は関数ポインタ以外のポインタ(関数ポインタは`*`を付けずに呼び出す) |
| `p[i]` | `*(p + i)`と同じ．配列の要素と同じ形で読み書きでき，要素が構造体なら`p[i].m`でメンバを読み書きできる．`p`はポインタ変数に限らず，ポインタの値を持つ式であればよい(`s.items[i].m`・`(*pp)[i].m`) |
| `p->m` | `(*p).m`と同じ．`p`は構造体へのポインタ．`p->array[i]`のように配列メンバの要素も読み書きできる |

番地を取れないものは次のとおり．

- ハードウェア変数・const変数: メモリ上の番地を持たないため
- 配列そのもの(`&arr`): 配列へのポインタに対応しないため．配列の名前をそのまま書けば先頭要素の番地になる
- 演算・関数呼び出しの結果: 値を置いた番地を持たないため
- 関数ポインタ変数: 関数ポインタへのポインタに対応しないため
- 関数名(`&add`): 関数の番地は関数名だけで書くため(「関数ポインタ」を参照)
- `main`: プログラムの開始点であり，呼び出して復帰させる手段を与えないため(「main関数」を参照)

### 演算

| 演算 | 結果 | 備考 |
|:-|:-|:-|
| `p + n`，`n + p`，`p - n` | ポインタ | `n`は整数．指す先の型のバイト数(`char`は1，`short`は2，`int`・ポインタは4，構造体は1個分のワード数×4)を単位にずらす |
| `p - q` | `int` | 同じ型のポインタどうし．間の要素数(番地の差を指す先のバイト数で割った値) |
| `p == q`，`p != q` | `int`(0/1) | 同じ型のポインタどうし，またはポインタと`nullptr` |
| `p < q`等の大小比較 | `int`(0/1) | 同じ型のポインタどうし．番地を符号なしの値として比べる |
| `p += n`，`p -= n`，`++`，`--` | ポインタ | 加減算と同じく，指す先の型のバイト数を単位に動かす |
| 真偽として使う位置(`if`・`while`・`do-while`・`for`の条件，三項演算子`?:`の条件，`!`・`&&`・`\|\|`のオペランド) | `!`・`&&`・`\|\|`は`int`(0/1) | `nullptr`なら偽，それ以外なら真として扱う(整数が`0`なら偽，それ以外なら真になるのと同じ)．`!p`は`p`が`nullptr`なら1，それ以外なら0 |
| `c ? p : q` | ポインタ | 両分岐が同じ型のポインタどうし，またはポインタと`nullptr`のとき，そのポインタの型 |

- 上記とキャスト(「キャスト」を参照)以外の演算(乗除・ビット演算・シフト・単項の`-`・`+`・`~`・`switch`の条件・配列の添字)にポインタは使えない
- 関数ポインタに使える演算は`==`・`!=`・真偽として使う位置(上表)・呼び出し・キャストのみ(関数の番地はメモリの番地とは別の空間のため，加減算・大小比較・`++`/`--`はできない)
- 番地が同じ配列の外を指すようになる加減算・範囲外の要素の読み書きは，配列の添字と同じく検査しない

### 型の一致

- ポインタと整数の間に暗黙の変換はない．整数をポインタへ，ポインタを整数へ代入・初期化・引数・戻り値として格納するとコンパイルエラーになる(ポインタへの`0`も同様．ヌルポインタは`nullptr`と書く)．変換するにはキャストを使う(「キャスト」を参照)
- ポインタどうしは，指す先の型が符号も含めて一致する場合のみ格納・比較・差の計算ができる(`char *`と`unsigned char *`は別の型)．関数ポインタどうしは，戻り値型と引数の型の並びがすべて一致する場合のみ同じ型とみなし，格納・比較ができる．型の異なるデータのポインタどうしはキャストで，型の異なる関数ポインタどうしは整数を経由したキャストで変換する(「キャスト」を参照)

### 配列の扱い

配列(配列変数・構造体のメンバ配列・構造体配列要素のメンバ配列・文字列リテラル)を値として使う(代入の右辺・初期化子・引数・戻り値・演算・式文・for文の更新に書く)と，先頭要素へのポインタになる．このため，関数の引数に配列を渡すと先頭要素の番地が渡る．格納先(代入先・初期化する変数・引数・戻り値)の型は，要素の型へのポインタでなければならない．  
組み込み関数の引数と，`char msg[] = "hello";`の初期化子の文字列リテラルは例外で，配列のまま扱う．

```c
struct Entry {
    char name[8];
};
struct Entry entries[4];
char buf[16];

void fill(char *dst, int n) {
    for (int i = 0; i < n; i++) {
        dst[i] = ' ';
    }
}

void main(void) {
    fill(buf, 16);                 // bufの先頭要素の番地を渡す
    fill(entries[1].name, 8);      // 構造体配列要素のメンバ配列も渡せる
}
```

### 関数ポインタ

関数名を呼び出しの括弧を付けずに書くと，その関数の番地(関数ポインタ)になる．関数ポインタは関数と同じ形で呼び出す．

```c
int (*op)(int, int) = add;     // addの番地
int x = op(1, 2);              // opが指す関数を呼び出す
```

- 関数の番地は関数名だけで書き，`&add`とは書けない．関数ポインタの呼び出しも`op(1, 2)`とだけ書き，`(*op)(1, 2)`とは書けない．同じ値・同じ呼び出しの書き方を1つにするためで，C言語ではどちらの書き方もできる

- 呼び出しの引数の数・型の検査，引数の受け渡し・戻り値の受け取りは，関数を直接呼び出す場合と同じ(「呼び出し規約」を参照)
- 変数・構造体メンバに入った関数ポインタを呼び出せる(`op(1, 2)`・`handlers.on_key(c)`・`p->callback()`・`ops[i].apply(x)`)
- 呼び出しの名前`f(...)`は，関数名なら関数を直接呼び，変数名ならその変数(関数ポインタでなければエラー)を通して呼ぶ．関数と同名のローカル変数・関数パラメータは宣言できない(「変数」を参照)ため，名前がどちらを指すかは一意に決まる

### グローバル変数の初期化子

ポインタ型のグローバル変数の初期化子には，次の式を書ける．いずれも番地が意味解析で確定する．

- `nullptr`
- グローバル変数・その配列要素(添字が定数式)・メンバの番地(`&g`・`&table[2]`・`&pairs[1].second`)
- グローバルの配列の名前・文字列リテラル(先頭要素の番地)
- 関数名(関数の番地)
- 整数の定数式をポインタへキャストした式(`(unsigned int *)0x8000`・`(void (*)(void))0x4000`)
- この一覧の式に，整数の定数式を足し引きすることとポインタへキャストすることを，任意の順に何度でも重ねた式(`&g + 1`・`(char *)&g`・`(char *)(&g + 1) + 1`)

変数の値(他のポインタ変数を含む)・関数呼び出し・`*`は，キャストしても書けない．番地の値は他の初期化子と同じく`main`の本体を実行する前に書き込む(「main関数」を参照)．後方で宣言した変数・関数の番地も書ける．

## 演算子

| 分類 | 演算子 |
|:-|:-|
| 算術 | `+`，`-`，`*`，`/`，`%` |
| ビット | `&`，`\|`，`^`，`~`，`<<`，`>>` |
| 論理 | `&&`，`\|\|`，`!` |
| 比較 | `==`，`!=`，`<`，`>`，`<=`，`>=` |
| 代入 | `=`，`+=`，`-=`，`*=`，`/=`，`%=`，`&=`，`\|=`，`^=`，`<<=`，`>>=` |
| 単項 | `-`(単項マイナス)，`+`(単項プラス)，`++`，`--`，`*`(間接参照)，`&`(番地の取得)，`(型名)`(キャスト) |
| メンバ | `.`，`->` |
| その他 | `?:`(三項演算子)，`sizeof` |

ポインタに対して使える演算子とその意味は「ポインタ」を参照．

1つの式の複雑さには上限があり，超えるとコンパイルエラーになる．入れ子が浅くても，組み込み関数のように評価に多くの作業領域を使う構文を含むと上限に達しうる(limitations.mdの「その他の量的制限」を参照)．

`sizeof`は`sizeof(型名)`(整数型とそのポインタ．`sizeof(unsigned int)`・`sizeof(char *)`のように符号修飾子・`*`も書ける)または`sizeof(変数名)`(スカラー変数・配列変数・構造体変数(配列を含む))のみに対応する(非対応の詳細は「未対応機能」を参照)．  
結果は常に`int`型のコンパイル時定数であり，実行時命令は生成しない．スカラー変数は型のバイト数，配列は要素数×要素型のバイト数を返し，いずれもワード境界を考慮しない論理サイズである(1ワードを占有する`char`のスカラー変数でも`1`)．構造体変数は，メンバの型のバイト数の合計ではなく実際の占有量(各メンバをワード単位で確保した構造体1個分のワード数×4．構造体配列はさらに×要素数．「構造体」を参照)を返す(`struct Tag { int x; char c; };`の変数は`5`ではなく`8`)．ポインタは指す先の型によらず`4`であり，引数で受け取ったポインタから指す先の配列のサイズは求められない．  
`sizeof(構造体変数.member)`(構造体メンバ単体のサイズ)・`sizeof(struct Tag)`・`sizeof(struct Tag *)`(型名としての構造体名)には非対応．

## 型変換

整数の式の値は32ビットの`int`または`unsigned int`として扱う．どちらになるかと，演算を符号付き・符号なしのどちらで行うかは，C言語と同じ整数昇格の規則で決まる．ポインタの式の型は「ポインタ」を，型を指定して値を変換する方法は「キャスト」を参照．

- 整数昇格: 演算のオペランドが`char`/`short`/`unsigned char`/`unsigned short`の場合は，`int`に変換してから演算する(どの値も`int`で表せるため)．`unsigned int`だけが符号なしのまま残る
- 二項演算(算術・ビット・比較): 昇格後のオペランドのどちらかが`unsigned int`なら符号なしで，そうでなければ符号付きで演算する．算術・ビット演算の結果の型は，符号なしで演算したなら`unsigned int`，符号付きなら`int`になる(比較の結果は`0`/`1`の`int`)
- 三項演算子`c ? a : b`: 値の型は，`a`・`b`の昇格後の型のどちらかが`unsigned int`なら`unsigned int`，そうでなければ`int`になる(条件`c`の型は影響しない)
- シフト`<<`・`>>`: 左オペランドの昇格後の型だけで決まる(右オペランドの型は影響しない)
- 単項演算子`-`・`+`・`~`: オペランドの昇格後の型になる(`unsigned int`の`-u`は`unsigned int`)
- 結果が`int`で固定のもの: 比較・`!`・`&&`・`||`の結果(`0`/`1`)，`sizeof`の結果(C言語の`size_t`と異なり符号付き．`int i = -1;`のとき`i < sizeof(buf)`は真)
- 複合代入`x op= e`は`x = x op e`と同じ規則で演算する
- 代入式の値の型は左辺の型，`++`/`--`の値の型はオペランドの型，関数呼び出しの値の型は戻り値型であり，式の中で使う際は上記の整数昇格を受ける

演算の符号によって，次の表の演算の結果が変わる．表にない演算は，符号によらず同じ結果になる．

| 演算 | 符号付き | 符号なし |
|:-|:-|:-|
| 右シフト`>>` | 空いた上位ビットを符号ビットで埋める算術シフト(`-8 >> 1`は`-4`) | 空いた上位ビットを0で埋める論理シフト |
| 大小比較`<`・`>`・`<=`・`>=` | 2の補数として比較する | 0〜4294967295の値として比較する(`unsigned int u = 0;`のとき`u < -1`は真．`-1`が`0xFFFFFFFF`として比較されるため) |
| 除算`/`・剰余`%` | 商は0方向へ切り捨て，剰余の符号は被除数に従う | 0〜4294967295の値として除算する |

値の読み出しと格納は次のとおり．

- 演算は32ビットで行い，結果は32ビットの範囲で折り返す．オペランドが`char`/`short`でも，演算結果をその型の幅に切り詰めることはない(`char a = 100;`のとき`a + a`は`200`)
- 8/16ビットの型の変数・配列要素・構造体メンバ(ポインタの指す先を含む)は，読み出した時点で`int`に変換される．`char`/`short`は上位ビットを符号ビットで埋め(符号拡張)，`unsigned char`/`unsigned short`は上位ビットを0で埋める(ゼロ拡張．`unsigned char c = 200;`の`c`は`200`)
- 8/16ビットの型の変数・配列要素・構造体メンバ(ポインタの指す先を含む)へ値を格納する際は，初期化・代入・`++`/`--`・整数型のパラメータへの引数の受け渡しなど操作の種類によらず，下位8/16ビットのみを格納し上位ビットは捨てる．32ビットの型どうし(`int`と`unsigned int`)の格納はビット列をそのまま格納する(`unsigned int u = -1;`の`u`は`4294967295`)．いずれもエラー・警告は出ない(`char c = 300;`の`c`は`44`)．関数の戻り値は格納に当たらず切り詰めない(「関数」を参照)
- 変数の値を型の異なる整数型のパラメータへ渡す場合も，上記の読み出しと格納の規則がそのまま適用される(`char c = -1;`を`int`のパラメータへ渡すと`-1`，`int n = 300;`を`char`のパラメータへ渡すと`44`)
- 格納した値は読み出し時に格納先の型で解釈されるため，格納前の値と一致しないことがある(`char c = 200;`のとき`c == 200`は偽になり，`case 200:`にも一致しない)
- 代入式・`++`/`--`の値は，格納時に切り詰める前の値である(`char c = 127;`のとき，`int`の変数`x`への`x = ++c;`で`x`は`128`，`c`は`-128`)．C言語では格納後の値になるため，8/16ビットの型では結果が食い違う(`unsigned char c = 0;`のとき`--c`の値は`-1`，`(c = -1) > 0`は偽．`return -1;`する`unsigned char`の関数`f`で`f() == 255`は偽)

### キャスト

`(型名)式`と書くと，式の値を型名の型へ変換する．前置単項演算子と同じ優先順位で結合する(`(unsigned int)a / b`は`((unsigned int)a) / b`，`(int *)p[i]`は`(int *)(p[i])`)．

| 変換 | 結果 |
|:-|:-|
| 整数→`char`/`short`/`unsigned char`/`unsigned short` | 下位8/16ビットへ切り詰めてから，その型の読み出しの規則(上記)で拡張した値(`(char)300`は`44`，`(char)200`は`-56`，`(unsigned char)-1`は`255`) |
| 整数→`int`/`unsigned int` | ビット列を変えず，型だけを変えた値(`(unsigned int)-1`は`4294967295`，`(int)0x80000000`は`-2147483648`) |
| 整数→ポインタ | 整数のビット列を番地とするポインタ(`(unsigned int *)0x8000`)．関数ポインタでは，関数の先頭の命令index(プログラムの先頭から数えた命令の位置．「呼び出し規約」を参照)とする |
| ポインタ→整数 | 番地のビット列を値とする整数(関数ポインタは関数の先頭の命令index)．8/16ビットの型へは上と同じく切り詰める |
| データのポインタ(関数ポインタ以外のポインタ)どうし | 番地を変えず，指す先の型だけを変えたポインタ(`(unsigned char *)words`) |
| 同じ型の関数ポインタどうし | 値を変えない関数ポインタ |

- 結果は書いた型名の型の値として扱う．8/16ビットの型へのキャストの結果は，演算のオペランドになると整数昇格により`int`へ変換される(`(unsigned char)x + 1`は`int`どうしの演算)
- 関数ポインタの型名は，宣言から名前を除いた`戻り値型 (*)(引数の型の並び)`の形で書く(`(void (*)(void))0x4000`)
- 結果は左辺値ではないため，代入・`++`/`--`・`&`の対象にできない(`(int)x = 1`はエラー)．ポインタへキャストした結果の指す先は読み書きできる(`*(unsigned int *)0x8000 = 1`)
- 結果に続けて添字・`->`・呼び出しを書ける(`((unsigned char *)words)[1]`・`((struct Pair *)p)->second`・`((void (*)(void))0x4000)()`)
- 整数から整数へのキャストは定数式に書ける(グローバル変数の初期化子・配列サイズ・`case`の値・const変数の初期化子．「定数式」を参照)
- ポインタへのキャストとポインタから整数へのキャストは定数式に書けない．例外として，ポインタのグローバル変数の初期化子には，整数の定数式やグローバル変数・関数の番地をポインタへキャストした式を書ける(「ポインタ」の「グローバル変数の初期化子」を参照)
- 次のキャストはコンパイルエラーになる
  - `void`・構造体そのもの・`const`を付けた型・宣言できない型(「未対応機能」を参照)へのキャスト
  - データのポインタと関数ポインタの間の直接のキャスト: データの番地はメモリのバイト単位，関数の番地は命令indexであり，指す空間が異なるため．整数を経由して書く(`(void (*)(void))(int)p`)
  - 型の異なる関数ポインタどうしのキャスト: 変換した関数ポインタを呼ぶと，呼び出す関数と引数・戻り値の型が食い違うため．整数を経由すれば書けるが，型が食い違う呼び出しの結果は保証しない
  - `nullptr`のキャスト: どのポインタへもキャストせずに格納でき，整数へ変換しても`nullptr`を表すために選んだビット列(`0x80000000`)を得るだけのため
  - 変換する式が1つの値として読めないもののキャスト: 構造体変数そのもの(`(int)s`)は値をまとめて読む仕組みがなく，戻り値のない関数の呼び出し(`void f(void)`に対する`(int)f()`)は値を持たないため
- キャストで作ったポインタは，指す先の型の境界に揃わない番地も指せる．`short`・`int`・ポインタを4バイト境界をまたいで読み書きすると，同じワードの中で折り返した番地を読み書きする([memory.md](memory.md)を参照)．コンパイラは検査しない
- 整数から作った関数ポインタの呼び出し先は，プログラムにない番地のコードを含め，スタック使用量の見積もり(limitations.mdの「その他の量的制限」を参照)に含まれない．`main`を呼び出すことも検査できない

## 制御構文

| 構文 | 備考 |
|:-|:-|
| `if` / `else if` / `else` | |
| `for` | |
| `while` | |
| `do`-`while` | |
| `switch` / `case` / `default` | |
| `break` | |
| `continue` | |
| `return` | `return;`(値なし)と `return expr;`(値あり)の両方に対応．どちらを書けるかは関数の戻り値型による(「関数」を参照) |

## ソースファイルの取り込み

`#include "パス"`を書くと，その位置に指定したファイルの内容が書かれていたものとして，1つのプログラムにまとめてコンパイルする．

```c
// main.pn
#include "lib/io.pn"      // main.pnがあるディレクトリのlib/io.pnを取り込む
void main(void) {
    put_line("hello");
}
```

- パスは`"`で囲む(`<...>`形式は非対応)．取り込み指令を書いたファイルがあるディレクトリからの相対パス，または絶対パスで指定する
- 取り込むファイルの拡張子は`.pn`に限る
- 指令の`#`は行の1文字目に書く(前に空白を置けない)．指令の後ろの同じ行にはコメントのみ書ける
- 1つのファイルに書いた宣言はそのファイルの中で完結させ，指令はグローバルスコープの宣言と宣言の間にのみ書ける(ファイルが宣言の途中で終わる場合や，関数本体・構造体定義の中，宣言の途中に指令を書いた場合はコンパイルエラー)．取り込んだファイルの宣言は，取り込み元の宣言と区別なくプログラム全体から参照できる(「グローバルスコープ」を参照)
- `#pragma once`(後述)が書かれていないファイルを1回のコンパイルで2回以上取り込むと，コンパイルエラーになる．同じファイルからの重複か，別のファイルを経由した間接的な重複かは問わない．`-pn`で指定したファイル自身を取り込む場合も同様
- 取り込みが循環する場合(`a.pn`が`b.pn`を，`b.pn`が`a.pn`を取り込む等)も，`#pragma once`で無視されない限りコンパイルエラーになる

### #pragma once

ファイルに`#pragma once`を書くと，そのファイルの2回目以降の取り込みはエラーにならず無視される．複数のファイルから共通に取り込むファイルに書く．

- `#include`と同じく宣言と宣言の間であればどこに書いてもよく，同じファイルに複数回書いてもよい
- 効果は`#pragma once`を読んだ時点から生じる．取り込みが循環する場合，そのファイルの字句解析の途中で再び取り込まれるため，循環のもとになる`#include`より前に書いておく必要がある．ファイルの先頭に書くことを推奨する
- `#pragma`に`once`以外を書くとコンパイルエラー

`#include`・`#pragma once`以外の指令(`#define`等)はコンパイルエラーになる．

## グローバルスコープ

ファイル直下(グローバルスコープ)に置かれた宣言はすべて，取り込んだファイル(「ソースファイルの取り込み」を参照)を含むプログラム全体のどこからでも順序問わず参照できる．  
関数定義とグローバル変数宣言(const変数を含む)の両方に適用され，定数式から参照する場合も同様である．  
ただし，宣言の値やサイズを求めるためにその宣言自身へ戻ってくる循環した依存はコンパイルエラーになる(例: `const int A = B; const int B = A;`，`int buf[sizeof(buf)];`)．

## 関数

- 戻り値型は `void` とスカラー型(整数型・ポインタ)に対応する(`main`は「main関数」を参照)
- 引数はスカラー型のカンマ区切りに対応する(`main`は「main関数」を参照)
  - 整数型のパラメータには型の異なる整数の式も渡せ，値はパラメータの型で格納される(「型変換」を参照)
  - ポインタのパラメータには，指す先の型が符号も含めて同じポインタのみ渡せる(「ポインタ」の「型の一致」を参照)．配列を渡すと先頭要素へのポインタが渡る(文字列リテラルは`char`の配列)
  - 配列の形のパラメータ(`int arr[]`)は宣言できず，ポインタ(`int *arr`)で宣言する
- 引数なしの宣言は `(void)` と `()` の両方を許容する
- void関数は `return;` で，非void関数は `return expr;` で呼び出し元へ復帰する．戻り値はRAX(`6'h1e`)を経由する
- `return`文を通らずに関数本体の終端へ達した場合は，戻り値型によらず呼び出し元へ復帰する．非void関数では戻り値が設定されないため，呼び出し元が受け取る値は不定になる(`f();`のように戻り値を使わない呼び出しであれば問題ない)．全経路で`return`文を通るかの解析は行わないため，終端へ達しうる非void関数もコンパイルエラー・警告にはならない．`main`での`return;`と終端への到達は復帰ではなくプログラムの終了を表す(「main関数」を参照)
- `return`文の形式が戻り値型と食い違う場合(`void`関数の`return expr;`，非void関数の`return;`)はコンパイルエラーになる．値を返す場合，戻り値型が整数なら整数の式のみ返せ，ポインタ・`nullptr`を返すとコンパイルエラーになる．戻り値型がポインタなら，指す先の型が符号も含めて同じポインタ(配列は先頭要素へのポインタ)と`nullptr`のみ返せ，整数(`0`を含む)や指す先の型が異なるポインタを返すとコンパイルエラーになる(「ポインタ」の「型の一致」を参照)．整数どうしは型が異なっても返せ，戻り値型の幅への切り詰めも行わない(`char`の関数が`return 300;`と書けば300がそのまま渡る)．すべての関数定義が対象であり，`main`から一切呼ばれない関数も検査される
- 引数の受け渡しは，呼び出し元が引数を評価し，呼び出し先のスタックフレームの引数領域になる位置へ直接書き込む方式(詳細は「呼び出し規約」を参照)
- 再帰呼び出し(直接・間接を問わない．`func1()`が`func2()`を呼び，その`func2()`が`func1()`を呼ぶような経路も含む)に対応する．呼び出しごとに引数・ローカル変数を別のスタックフレームへ確保するため，呼び出しの深さによらず値が保たれる

### 関数に関する制限

- 関数のオーバーロードには対応しない(関数名で一意に特定する)
- デフォルト引数には対応しない
- 呼び出しの深さに固定の上限はなく，スタックの残り容量で決まる(limitations.mdの「その他の量的制限」を参照)

### main関数

`main`はプログラムの開始点となる関数であり，定義は必須(定義がない場合はコンパイルエラー)．  
開始点として実行が始まる`main`には値を受け渡す呼び出し元が存在しないため，戻り値型は`void`とし，引数は取らない．これに違反する定義(`int main(void)`・`void main(int a)`等)はコンパイルエラーになる．引数なしの書き方は関数一般と同じく`(void)`・`()`のいずれでもよい(「関数」を参照)．`main`での`return;`および関数本体の終端への到達は，呼び出し元への復帰ではなくプログラムの終了を表す(生成される`main`の`ret`の動作は[assembler.md](assembler.md)を参照)．呼び出しても復帰できないため，`main`は直接呼び出すこと(`main();`)も，番地を取って関数ポインタから呼び出すこと(「ポインタ」の「番地の取得と間接参照」を参照)もできず，いずれもコンパイルエラーになる．

初期化子を持つグローバル変数への書き込みと，式中に現れる文字列リテラル(匿名のグローバルchar配列)へのメモリ書き込みは，`main`の本体を実行する前に1回だけ行う．`main`より前に実行されるコードは他に存在しないため，これらの書き込みがプログラム中で最初に実行される．

## 組み込み関数

標準ライブラリは提供しない．代わりに以下の組み込み関数を提供する．  
`print`/`scan`/`streq`/`strcopy`は予約語であり，同名の変数・関数を宣言することはできない(宣言した時点でコンパイルエラーになる)．  
いずれも呼び出し箇所へ命令列を直接展開する(`call`を伴わない)ため，スタックを消費しない．

| 関数 | 説明 | 対応ISA命令 |
|:-|:-|:-|
| `print(buf)` | `char`配列`buf`の先頭からヌル終端(`'\0'`)まで1文字ずつ標準出力に出力する | PRINT |
| `scan(buf)` | 標準入力から改行(`'\n'`)までの1行を読み込み，`char`配列`buf`へヌル終端付きで格納する | SCAN |
| `streq(a, b)` | `char`配列`a`・`b`の内容が一致するか比較し，一致すれば`1`，不一致なら`0`を返す(`int`) | なし(既存の比較・メモリアクセス命令のみで実装) |
| `strcopy(dst, src)` | `char`配列`src`の内容をヌル終端付きで`char`配列`dst`へコピーする | なし(既存のメモリアクセス命令のみで実装) |

引数には`char`型または`unsigned char`型の配列を渡す．渡せる配列の種類は，要素型の符号によらず関数ごとに次のとおり異なる．

| 引数 | `print`/`streq`/`strcopy` | `scan` |
|:-|:-|:-|
| 固定サイズで宣言した配列変数(`buf`) | ○ | ○ |
| 単一の構造体変数のメンバ配列(`entry.name`) | ○ | ×(「未対応機能」を参照) |
| 番地が実行時に決まるメンバ配列(`arr[i].name`・`p->name`) | × | × |
| ポインタ(`char *p`) | × | × |
| スカラー変数・上記以外の式 | × | × |

引数は配列のまま受け取り，先頭要素へのポインタとしては扱わない(「ポインタ」の「配列の扱い」を参照)．すべての関数で渡せないものの理由は次のとおり．

- 番地が実行時に決まるメンバ配列: 先頭番地が添字やポインタの値に応じて決まり，各関数のコード生成が前提とする「先頭番地が変数の宣言だけで決まること」と相容れない
- ポインタ: 各関数が打ち切りの判定に使う，指す先の配列の宣言サイズが分からない
- スカラー変数・上記以外の式: オーバーロードに対応しないため，用途ごとに別の組み込み関数を用意する設計は取らない

各関数の動作の詳細は次のとおり．

- `print`: 配列の宣言サイズに達してもヌル終端が見つからない場合は，そこで安全に打ち切る
- `scan`: 標準入力の先頭に続く改行はすべて読み飛ばし，改行以外の最初の文字から1行として読み込む．バックスペース(DEL，`0x7F`)は配列に格納せず，直前に格納した1文字を取り消す(格納済みの文字が無ければ何もしない)．BS(`0x08`)は取り消しに使わず通常の文字として格納する．1文字も格納しないまま改行に達した場合は空文字列を格納する．格納した文字数が配列サイズ-1に達した時点で読み込みを打ち切り，残りの入力(改行・バックスペースを含む)は次に`scan`を実行したときに読み込まれる(破棄しない)．対象配列は**2要素以上**(最低1文字+ヌル終端)が必要
- `streq`: 先頭から1文字ずつ比較し，不一致文字が出た時点，または片方だけが先にヌル終端に達した時点で不一致(`0`)．両方同時にヌル終端に達すれば一致(`1`)．どちらかの宣言サイズに達してもヌル終端が見つからない場合は，安全のため不一致(`0`)として扱う
- `strcopy`: `src`の宣言サイズに達してもヌル終端が見つからない場合は，そこで安全に打ち切る(`print`と同じ考え方)．`dst`の宣言サイズ-1文字を超える場合は，そこで打ち切ってヌル終端を書き込む(`scan`と同じ安全な打ち切り)

## 定義済みハードウェア変数

LED・DIPスイッチ等のI/Oレジスタを，あらかじめ定義された変数として提供する．  
通常の変数と同様に代入・参照でき，コンパイラが対応するレジスタ番地への読み書き命令に変換する．

```c
LED = 0x0f;      // LEDレジスタへの書き込み
int x = DIPSW;   // DIPスイッチレジスタからの読み込み
```

変数名と対応するレジスタ番地は以下の通り．読み書き可否は[register.md](register.md)に従い，違反した場合はコンパイルエラーとなる．

| 変数名 | 番地 | 内容 | 読み | 書き |
|:-|:-|:-|:-|:-|
| `BTN` | `6'h20` | タクトスイッチ×4 | 可 | 不可 |
| `DIPSW` | `6'h21` | DIPスイッチ×2 | 可 | 不可 |
| `LED` | `6'h22` | LED×4 | 不可 | 可 |
| `RGBLED` | `6'h23` | RGB LED×2 | 不可 | 可 |
| `PMOD_A` | `6'h24` | Pmod A | 可 | 可 |
| `PMOD_B` | `6'h25` | Pmod B | 可 | 可 |
| `AR8_13` | `6'h26` | Arduinoピン AR8〜AR13 | 可 | 可 |
| `AR_I2C` | `6'h27` | A(AREF)，AR_SDA，AR_SCL | 可 | 可 |
| `AR0_7` | `6'h28` | Arduinoピン AR0〜AR7 | 可 | 可 |
| `AR_RST` | `6'h29` | Arduinoリセット | 可 | 不可 |
| `AR_SPI` | `6'h2A` | AR_MISO，AR_SCK，AR_MOSI，AR_SS | 可 | 可 |
| `GPIO0_7` | `6'h2D` | GPIO0〜7 | 可 | 可 |
| `GPIO8_15` | `6'h2E` | GPIO8〜15 | 可 | 可 |
| `GPIO16_23` | `6'h2F` | GPIO16〜23 | 可 | 可 |
| `GPIO24_27` | `6'h30` | GPIO24〜27 | 可 | 可 |

型は全て`unsigned int`として扱う(ピンやスイッチの状態を表すビット列として使うため．演算の結果への影響は「型変換」を参照)．  
レジスタに置かれメモリ上の番地を持たないため，`&`で番地を取ることはできない．  
同名の変数・関数は宣言できない(「変数」を参照)．

次のレジスタは変数として公開しない(各レジスタの制約は[register.md](register.md)を参照)．

- CPU内部レジスタ(`6'h00`〜`6'h1f`)．番地ごとの理由は次のとおり
  - r0〜r15・RAX・SP: コンパイラがコード生成に内部で使用する(SPはスタックフレームの確保・参照に使う)
  - 空き番地(`6'h11`〜`6'h1b`): ハードウェア上で読み書きが禁止されている
  - FLG・RSI: 未実装であり，意味のある値を持たない
  - PC: 書き込みが禁止されている．読み出すとその命令自身の番地が返るが，関数の番地は関数名から得られるため使い道がない
- 標準入出力レジスタ(`6'h31`〜`6'h34`): 組み込み関数`print`/`scan`経由でのみ使用する
- アナログピン(`6'h2B`)・XADC(`6'h2C`): 現時点でCPUで使用する予定がなくオミットされている

## コメント

```c
// 行コメント

/* ブロックコメント */
```

## 未対応機能

スモールスタートの方針により，以下は当面対応しない．

| 機能 | 備考 |
|:-|:-|
| `#define` / `#ifdef` 等のプリプロセッサ指令 | 「ソースファイルの取り込み」に記載した指令以外は非対応．定数の定義にはconst変数を使う(「const変数」を参照) |
| `sizeof(計算式)` | `sizeof(1 + 2)`のようなC言語の任意の式には非対応で，型名・変数名のみ対応する．将来対応予定 |
| `void *`・配列へのポインタ(`int (*p)[4]`)・関数ポインタの配列・関数ポインタへのポインタ・関数ポインタを返す関数・`const`とポインタの組み合わせ | ポインタ(「ポインタ」節を参照)のうち，これらの型は宣言できない |
| `long` | 将来対応予定 |
| `float` / `double` | ISAに浮動小数点命令なし |
| 整数リテラルの接尾辞(`10u`等) | 型はキャストで指定する(`(unsigned int)10`．「キャスト」を参照) |
| `union` | |
| カンマ区切りの複数変数宣言・`typedef`・構造体を関数の引数/戻り値にすること | `struct`自体(固定メンバ構成，単一変数・配列の宣言，`v.member`／`arr[i].member`アクセス)は対応済み(「構造体」節を参照)．構造体は，そのポインタを引数・戻り値にして受け渡せる．上記3点は個別に対応検討予定(pynesisリポジトリ issue #15(カンマ区切り)・#16(typedef)・#17(引数/戻り値)) |
| `static` 変数 | |
| `goto` / ラベル文 | |
| カンマ演算子 | 対応予定なし．カンマは関数のパラメータ宣言と関数呼び出しの引数(組み込み関数を含む)の区切りとしてのみ使用する |
| 動的メモリ管理(`malloc`等) | |
| 標準ライブラリ全般(`printf`，`scanf`等) | |
| 配列添字の範囲外アクセス検査 | コンパイル時・実行時ともに検査しない．C言語と同様，未定義動作として扱う |
| `scan`で配列が満杯になった後のバックスペース | 格納した文字数が配列サイズ-1に達した時点で読み込みを打ち切るため(「組み込み関数」を参照)，その後のバックスペースはその行の最後の1文字を取り消さず，改行とともに次の`scan`へ持ち越される |
| `scan`の対象を構造体のメンバ配列にすること | `scan`は現状，識別子1個(配列変数そのもの)のみを対象とする．メンバ配列(単一の構造体変数のもの)は先頭番地が変数の宣言だけで決まるため技術的には対象にできるが，`print`等のように任意の式を受け付ける構文へは未拡張．将来対応予定 |
| `int`の最小値(`-2147483648`)のリテラル表現 | 10進の整数リテラルに書ける値の範囲(「リテラル」を参照)に含まれないため，単項マイナスと組み合わせても`-2147483648`は表現不可．16進の`-0x80000000`は`unsigned int`の`0x80000000`になる(「型変換」を参照)．`(int)0x80000000`・`-2147483647 - 1`等で代替可能 |

---

# コンパイラ仕様

## メモリ配置

### アドレッシング方式

グローバル変数と文字列リテラルは**絶対アドレス**に配置する．番地は0番地から順にコンパイル時(意味解析)に確定し，アセンブリ言語の生成では，この番地をそのまま命令の即値として埋め込む．

ローカル変数と引数は，呼び出しごとに確保する**スタックフレーム**に置く．スタックはメモリの前半の上端から番地の小さい方へ伸びるため([memory.md](memory.md)を参照)，0番地から上へ伸びるグローバル変数とは反対側から領域を使う．フレーム内の位置はコンパイル時に確定するが，実際の番地は実行時のSP(スタックポインタ，`6'h10`)に応じて決まるため，SPからの相対位置を指定するRMR/WMR命令で読み書きする([isa.md](isa.md)を参照)．この方式により，同じ関数が呼び出しの途中で再び呼ばれても値が上書きされない．  
ローカル変数・引数の番地を`&`で取ると，SPにフレーム内の位置を足した実行時の番地になる．この番地は関数から復帰してフレームを解放した後は別の呼び出しのフレームに再利用されるため，復帰後に参照した場合の値は保証されない(C言語と同じ)．

スカラー変数・メンバは4の倍数の番地に，配列の要素は要素のバイト数の倍数の番地に置く．このため，それらの番地を指すポインタを通した読み書きはmaskが同一ワードに収まる([memory.md](memory.md)の制約に抵触しない)．`char`へのポインタは奇数番地も指すが，1バイトの読み書きは常に同一ワードに収まる．整数からキャストで作ったポインタなど，型の境界に揃わない番地を指すポインタを通した読み書きはこの制約に抵触しうる(「キャスト」を参照)．  
ヌルポインタ`nullptr`の値`0x80000000`はアドレスバス幅を超える番地であり，グローバル変数を0番地から配置しても，どの変数の番地とも一致しない．

変数配置の将来的な移行計画はlimitations.mdを参照．

### スタックフレーム

関数は自分のフレームを先頭で確保し(SPを下げる)，復帰の直前に解放する(SPを戻す)．フレームの大きさはコンパイル時に確定する．確保した直後のSPを基準とした配置は次のとおりで，基準はフレームの中で最も小さい番地にあたる．

| 位置 | 内容 |
|:-|:-|
| `SP+0` | ローカル変数領域(Lバイト．宣言ごとに固有の位置を割り当て，ブロックを抜けても再利用しない) |
| `SP+L` | レジスタ退避領域(Sバイト．`r{i}`の退避先は`SP+L+i*4`) |
| `SP+L+S` | 引数領域(4×nバイト．宣言順に1つ1ワード) |
| `SP+L+S+4n` | 戻り先アドレス(CALLが積む) |

### 呼び出し規約

呼び出し元はSPを動かさず，i番目(先頭を0とする)の引数を`SP-4-4n+4i`番地へ書き込んでから`call`する．CALLが戻り先アドレスを`SP-4`番地へ積み，呼び出し先がフレームを確保してSPを下げた結果，書き込んだ引数がそのまま呼び出し先の引数領域になる．  
値は引数の型の幅で書き込む(「型変換」の格納の規則に従う)．ポインタの引数へは番地を1ワードで書き込む(配列を渡す場合は先頭要素の番地)．  
引数の書き込み先は，同じSPから行うどの呼び出しでも同じ位置になる．このため`add(1, add(2, 3))`のように引数の式が関数呼び出しを含む場合，先に`1`を書き込むと，内側の`add(2, 3)`が同じ位置へ自分の引数を書いて壊してしまう．内側の呼び出しを含む引数までは値をレジスタに残したまま評価し，内側の呼び出しがすべて終わってからまとめて書き込む．  
戻り値はRAX(`6'h1e`)を経由する．

関数ポインタを通した呼び出しも，引数の受け渡し・戻り値の受け取りは同じである．呼び出す関数の番地(関数の先頭の命令index．関数ポインタの値)を引数より先にレジスタへ読み込み，引数を書き込んだ後に，そのレジスタを飛び先に指定した`call`(番地を`r1`に読み込んだ場合は`call r1`)で呼び出す([assembler.md](assembler.md)の「ジャンプ系(J系)」を参照)．関数名による直接呼び出しは，`call add`のように飛び先に関数名を書く．関数の番地は，アセンブリの即値の位置に関数名を書いて求める．

## レジスタ使用規約

すべての変数はメモリ上に置くため，汎用レジスタr0〜r15(`6'h00`〜`6'h0f`)は変数の保持には使わず，**式評価のための一時レジスタ群**として使用する．

- r0〜r15は一時レジスタ．**`call`をまたいで値は保持されない**(揮発)．呼び出し後も必要な値はメモリ(変数)に置く
    - 外から見た動作: callをまたぐとr0〜r15の値は保証されない(残っていないものとして扱う)
    - 実際の動作: ハードウェアはレジスタを物理的にリセットしない．呼び出し先が自由に上書きしてよいという規約により，結果として値が壊れるだけ(各関数のコード生成がr0から番号を振り直すため低位レジスタが衝突する)
- 式の評価はr0から順に割り当てる**レジスタスタック方式**(部分式の結果を低位レジスタから積み，演算で畳んで解放する)．式の複雑さの上限はlimitations.mdの「その他の量的制限」を参照
- 関数の戻り値・演算結果の最終的な置き場はRAX(`6'h1e`)を用いる

`call`間でレジスタが揮発するのは，ハードウェアが退避するのは戻り先アドレスだけであり，汎用レジスタの退避を呼び出し元・呼び出し先のどちらにも義務づけていないためである(詳細は limitations.md を参照)．

式の評価中にサブ式として関数呼び出しを含む場合，呼び出しをまたいで生かしたい他のレジスタの値は，呼び出し前後で自分のフレームの退避領域へ退避・復元する．退避先が呼び出しごとに別の番地になるため，呼び出し先が同じレジスタを退避しても呼び出し元の退避値は壊れない．

## ファイル仕様

- 入力: ソースファイル(`.pn`)．`#include`で取り込んだファイルと合わせて1つのプログラムとしてコンパイルする
- 出力: アセンブリ言語ファイル(`.pt`)．コード生成より前の段階でコンパイルエラーになった場合は作らない
- ソース上の位置を特定できるコンパイルエラーは，`compiler error: <内容> at <ファイル名>:<行番号>`の形式で出力する

## 実行ファイル構成と起動引数

| 実行ファイル | 変換範囲 |
|:-|:-|
| `pn2asm.exe` | Pynesisソース(`.pn`) → アセンブリ(`.pt`) |
| `asm2sv.exe`(アセンブラ，`../assembler/`) | アセンブリ(`.pt`) → SystemVerilog ROM(`.sv`)．起動引数は`assembler.md`の「起動引数」を参照 |
| `pn2sv.exe` | Pynesisソース(`.pn`) → SystemVerilog ROM(`.sv`) まで一貫変換．今後のコンパイラの入口 |

`pn2sv.exe`は`pn2asm.exe`・アセンブラの本処理をそれぞれ`main`から分離した関数として直接リンクし，順に呼び出す(サブプロセス起動はしない)．  
`pn2asm.exe`・`asm2sv.exe`は単体の実行ファイルとしても引き続き動作する．

### pn2asm.exe

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pn` | 入力Pynesisソースファイル名(`.pn`)．必須 | - |
| `-pt` | 出力アセンブリファイル名(`.pt`) | `-pn`の拡張子を`.pt`に変えた名前を自動的に使う |

例: `pn2asm.exe -pn program.pn -pt program.pt`

同じフラグ(`-pn`／`-pt`)や指定子なし引数を複数回指定した場合，エラーにはならず最後に指定した値で上書きされる(後勝ち)．

### pn2sv.exe

| フラグ | 内容 | 省略時 |
|:-|:-|:-|
| `-pn` | 入力Pynesisソースファイル名(`.pn`) | 必須(省略不可) |
| `-pt` | 中間生成物のアセンブリファイル名(`.pt`) | 必須(省略不可) |
| `-sv` | 出力SystemVerilog ROMファイル名(`.sv`) | `-pt`の拡張子を`.sv`に変えた名前を自動的に使う(`asm2sv.exe`単体の場合と同じ自動導出がそのまま働く) |

例: `pn2sv.exe -pn program.pn -pt program.pt -sv program.sv`

引数は変換を始める前にまとめて検査し，誤りがあれば中間アセンブリファイルを書き出さずにエラーにする．指定子なし引数(`pn2asm.exe`は入力Pynesisソース名，`asm2sv.exe`はアセンブリ名と解釈が異なる)と，上表にない指定子・値のない指定子(単体の各ツールでは黙って無視され，打ち間違いに気づけない)は受け付けない．`-pn`/`-pt`/`-sv`を複数回指定した場合は，`pn2asm.exe`/`asm2sv.exe`と同じく後勝ちで上書きされる．
