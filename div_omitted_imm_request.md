# DIV命令のimm省略時デフォルト値についての検討依頼(アセンブラプロジェクトへ)

## 経緯

コンパイラプロジェクトのコードレビューにて，`specification/assembler.md`のDIV命令の説明(3引数形式`div r1 r2 r3`)にドキュメント不足が指摘された．
調査したところ，3引数形式(余りの格納先レジスタ番地を省略した場合)の挙動に設計上の懸念が見つかったため，ドキュメント修正の前に検討を依頼したい．

## 疑問点

`isa.md`の記述によれば，DIVの`imm`は「剰余を格納するレジスタ番地」を表す即値である(ハードウェア自体の挙動はこの記述通りで確認不要)．

`asm2bin.cpp`(505-507行目)を確認したところ，アセンブリ上で4つ目の引数(imm)を省略した場合，全命令共通の処理として以下のようにimmへ**0を自動補完**している．

```cpp
// immあり・未出力なら，0にしておく
if (command_arg.has_imm && !command_arg.imm_required && (arg_num == command_arg.arg_num_min)) {
    instr += ", 0";
}
```

この処理は`sll`/`srl`/`sla`/`sra`/`mov`/`rm`/`wm`等，immを省略可能な命令全般で共通だが，これらの命令ではimm=0が「シフト量0」「即値0」等，そのまま無害な意味を持つ．

一方DIVの場合，imm=0は「余りをレジスタ番地0(`r0`)に格納する」ことを意味する．つまり`div r1 r2 r3`(3引数形式)を書くと，**余りが破棄されるのではなく，暗黙にr0へ書き込まれてr0の値が上書きされる**という挙動になる．これは他の省略可能命令と一貫しない特殊な副作用であり，利用者が気づかずr0を破壊するおそれがある．

## 検討してほしいこと

1. 3引数形式`div r1 r2 r3`を今後も許可するか
   - 許可する場合: `assembler.md`に「imm省略時はr0に余りが書き込まれ，r0の値が上書きされる」ことを明記する
2. DIVに限りimmを省略不可(4引数必須)にする方が安全ではないか
   - `asm2bin.hpp`のDIVエントリを`imm_required = true`に変更する対応が考えられる

なお，コンパイラの`generator.cpp`(`gen_binop_instr`)は`%`演算子のコード生成時に常に4引数形式(`div rs1 rs2 rs2 dst`)を明示的に使用しており，3引数形式は使用していない．そのためどちらの対応を取ってもコンパイラ側への影響はない．
