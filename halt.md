# 停止と復帰
CPUは，実行できない命令や不正な値を検出すると停止する．停止は異常の検出によってのみ起こり，プログラムの正常終了では停止しない(正常終了の扱いは[assembler.md](assembler.md)を参照)．例外・トラップの仕組みは持たず，停止した状態から自力で復帰する手段もない．\
停止したCPUは，外部からのリセットが入るまで実行を再開しない．停止と同時に，すべてのレジスタは初期値(SPは`6'h10`，それ以外は0)に戻され，停止した時点の値は残らない．\
リセットを受けるとPCも0に戻っているため，プログラムの先頭から実行し直す．メインメモリの内容は初期化されず，停止前の値がそのまま残る．

# 命令の番地に関する条件
プログラムカウンタが次のいずれかに当たる番地から命令を取得しようとした場合に停止する(プログラムカウンタの扱いは[rom.md](rom.md)を参照)．

- ROMへ渡せるビット幅を超える上位ビットが1ビットでも立っている
- ROMに格納された命令数の範囲外である．最後の命令の次へ進んだ場合もこれに当たる

# 命令の内容だけで判定できる条件
命令タイプ・func・レジスタ番地・immから，レジスタの値を参照せずに判定する条件．

- rs1・rs2・rdのいずれかが，レジスタ番地の上限(`6'h34`)を超えている
- funcが，その命令タイプに定義されていない値である(命令表に載っているBRM/BWMも，未実装のため定義されていない値として扱う)
- F系でimm[32]が0である(F系は分岐先をイミディエイトデータで指定することが必須のため)
- 命令が読み出しに使うレジスタが読み込み不可である，または書き込みに使うレジスタが書き込み不可である(読み書きの可否は[register.md](register.md)を参照)

レジスタ番地に関する上記2つの条件は，判定の対象とする範囲が異なる．\
番地の上限は，rs1・rs2・rdが命令タイプによらず機械語中に必ず存在するフィールドであるため，その命令が実際には使わないフィールドも含めて判定する．\
読み書きの可否は，その実行で実際に使うオペランドのみを判定する．イミディエイトデータの使用によって無視されるrs1・rs2，命令が使用しないrd，imm[32]が0のときのDIV・DIVUの剰余格納先は判定しない．\
CALLによるSP・戻り先レジスタへの書き込み，RETによるSPの更新，および各命令に伴うPCの更新は，オペランドとして指定した番地への書き込みではないため，これらのレジスタが書き込み禁止であっても判定の対象外である．

# レジスタの値が確定してから判定する条件
レジスタの値そのものに依存するため，命令の実行中に判定する条件．

- DIV・DIVUの除数(rs2)が0である
- RM/WMのアドレス(rs1またはイミディエイトデータ)に，メモリのアドレスバス幅を超える上位ビットが1ビットでも立っている(メモリのアドレス空間は[memory.md](memory.md)を参照)
- CALLの時点で，SPが最後の戻り先レジスタ(`6'h1a`)を指している．関数呼び出しのネストが最大10段(`CALL`の入れ子回数で数える)を超える呼び出しがこれに当たる([limitations.md](limitations.md)を参照)
- RETの時点で，SPが`6'h10`を指している．CALLしていない状態でのRETがこれに当たる．どこからもCALLされない`main`の`ret`は，アセンブラが正常終了へ変換するためこれに当たらない([assembler.md](assembler.md)を参照)

上記の条件に当たるCALL/RETは，戻り先の保存・SPの更新・PCの移動をいずれも行わずに停止する．

# 停止しない不正
次のものは不正な使い方だが停止せず，誤った動作のまま実行を続ける．CPUは検出しないため，プログラムを生成・記述する側で避ける必要がある．

- DIVで32ビット符号あり整数の最小値(`-2147483648`)を`-1`で割る場合．商が32ビットで表現できないが，除算は実行され誤った値が格納される
- RM/WMで，maskが対象とするバイト範囲が4バイト境界に揃った同一ワード内に収まっていない([memory.md](memory.md)を参照)
