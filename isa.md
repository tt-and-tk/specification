# 基本構成
- m_type 何系のコマンドか
- func 機械語の種類
- mask マスク
- rs1 演算に使用するレジスタのアドレスひとつめ
- rs2 演算に使用するレジスタのアドレスふたつめ
- rd 演算結果を出力するレジスタのアドレス
- imm 即値関連の33ビットの値．最上位ビット(imm[32])が即値使用フラグ，残り32ビット(imm[31:0])が即値本体(イミディエイトデータ)．\
      フラグが立っている場合はrs1またはrs2を無視し，即値のみまたはrs1と即値で演算を行う．\
      immを扱うかどうかは命令ごとに決まり，機械語表現でimmがフィールド名で示される命令のみが対象となる．\
      そのうち使用が必須の命令は，各命令の説明でその旨を示す
```
 31  29 28  23 22  19 18 13 12  7  6    1    0
| m_type | func | mask | rs1 | rs2 | rd |imm[32]|
```

機械語が32ビット，その後ろにイミディエイトデータ(imm[31:0])が32ビットある．機械語末尾のimm[32]と合わせて，immは合計33ビットの値になる．

実行できない命令や不正な値を検出した場合，CPUは停止する．停止する条件と停止後の挙動は[halt.md](halt.md)を参照．

各命令の機械語表現(下表)において，`4'h0`・`6'h00`・`33'h000000000`のような固定値で示されているフィールドは，原則としてその命令では使用しない(アセンブリ構文上も指定しない)ことを表す．固定値ではなくフィールド名(`rs1`等)で示されている場合は，原則としてその命令で実際に使用される値を表す．いずれも例外があり，詳細は各セクションを参照する．

レジスタの値は32bitのビット列であり，符号の有無は命令ごとに決まる．値を演算・比較する命令のうち，符号の解釈によって結果が変わるものには，値を2の補数表現の符号あり整数として扱う符号あり系の命令と，符号なし32bit整数として扱う符号なし系の命令を別々に用意し，符号なし系の命令は名前の末尾にUを付けて区別する．\
論理演算・加算・減算・乗算(下位32bitのみを格納するため)・一致判定のように，符号の解釈によらず同じ結果になる演算には符号なし系の命令を設けない．\
シフトは例外で，右シフトの空いたビットの埋め方を論理シフト・算術シフトの命令で区別する(詳細はシフト系を参照)．

# 処理を実行しない(N系)
指定された処理と同時に，PCをインクリメントする．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|NOP|何もしない|`{3'h0, 6'h00, 4'h0, 6'h00, 6'h00, 6'h00, 33'h000000000}`|

# 演算系(P系)
指定された処理と同時に，PCをインクリメントする．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|AND|論理積|`{3'h1, 6'h00, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|OR|論理和|`{3'h1, 6'h01, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|XOR|排他的論理和|`{3'h1, 6'h02, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|NOT|反転(rs2は無視する)|`{3'h1, 6'h03, 4'h0, rs1, 6'h00, rd, 33'h000000000}`|
|NAND|否定論理積|`{3'h1, 6'h04, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|ADD|算術和|`{3'h1, 6'h05, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|SUB|算術差|`{3'h1, 6'h06, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|MUL|積|`{3'h1, 6'h07, 4'h0, rs1, rs2, rd, 33'h000000000}`|
|DIV|符号ありの商と余り|`{3'h1, 6'h08, 4'h0, rs1, rs2, rd, imm}`|
|DIVU|符号なしの商と余り|`{3'h1, 6'h09, 4'h0, rs1, rs2, rd, imm}`|

MULの乗算結果は最大64bitになりうるが，rdは32bit幅のため下位32bitのみを格納する．上位32bitは破棄され，読み出す手段はない．

DIV・DIVUはrs1(被除数)をrs2(除数)で割る．\
符号あり系のDIVでは，商は0方向への切り捨て(truncation)，剰余の符号は被除数の符号に従う(C++11以降の`/`・`%`と同じ丸め方向)．\
以下の規則はDIV・DIVUに共通する．\
除数(rs2)が0の場合は除算を実行しない(そのときの挙動は[halt.md](halt.md)を参照)．\
商はrdへ，剰余はimm[5:0]が指すレジスタ番地へ格納する．\
imm未使用(即値使用フラグが立っていない)の場合は剰余の格納自体を行わない(rdへの商の格納のみ実行されそれ以外のレジスタは更新されない)．\
基本構成の一般則(imm使用時はrs1またはrs2を無視する)の例外であり，rs1(被除数)・rs2(除数)を常に両方使用する．レジスタ番地を指定する引数をrs1・rs2・rd(商)で使い切るため，immを演算対象ではなく剰余の格納先レジスタ番地という別の意味で使用する．\
rdとimmが指す先が同一レジスタの場合，商への書き込みより後に剰余への書き込みが反映されるため，最終的に剰余が残る．

P系命令(ADD/SUB/MULを含む)はいずれもFLGレジスタ(`6'h1c`．ビット定義は[register.md](register.md)を参照)を更新しない．現在のqurge実装ではFLGはいずれの命令からも書き込まれず，リセット時の初期値`0`のまま変化しない未実装のレジスタである．

# シフト系(S系)
指定された処理と同時に，PCをインクリメントする．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|SLL|左シフト命令|`{3'h2, 6'h00, 4'h0, rs1, rs2, rd, imm}`|
|SRL|右シフト命令|`{3'h2, 6'h01, 4'h0, rs1, rs2, rd, imm}`|
|SLA|算術左シフト命令|`{3'h2, 6'h02, 4'h0, rs1, rs2, rd, imm}`|
|SRA|算術右シフト命令|`{3'h2, 6'h03, 4'h0, rs1, rs2, rd, imm}`|

rs1がシフトされる値そのものであり，シフト量として使用するのはrs2またはイミディエイトデータの下位5bit(0〜31)のみであり，それより上位のビットは無視される．\
シフト量を32以上の値を表すビットパターンで指定した場合も，上位ビットが無視された結果として0〜31の範囲に収まる値としてシフトする(シフト量32以上そのものを表す手段がない)．\
imm使用時はシフト量としてrs2を無視し，rs1(シフトされる値)は常に使用する．

左シフトは符号の解釈によらず結果が変わらない(空いたビットは常に0で埋まる)ため，SLLとSLAは常に同一の結果を返す実質的に同じ命令である．符号拡張の違いが意味を持つのは右シフトのみであり，SRLは空いた上位ビットを0で埋め，SRAはrs1の最上位ビット(符号ビット)を上位に複製する(符号拡張)．

# 代入系(A系)
指定された処理と同時に，PCをインクリメントする．\
rs1またはイミディエイトデータのデータをrdに格納する．\
maskは使用されず，常にrdの全バイトに書き込まれる(構文上は必須引数のため値を指定する必要があるが，何を指定しても動作は変わらない未実装のフィールド)．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|MOV|rs1またはイミディエイトデータをrdに格納する|`{3'h3, 6'h00, mask, rs1, 6'h00, rd, imm}`|

# 分岐系(F系)
rs1とrs2の数字を比較し，その結果が`true`ならPCにイミディエイトデータを足し，`false`ならインクリメントする．\
maskとrdは使用しないため0固定．\
イミディエイトデータを必ず使用するためimm[32]は`1'b1`固定\
F系は基本構成の一般則(imm使用時はrs1またはrs2を無視する)の例外であり，rs1・rs2(比較対象)とimm(分岐先)を同時に使用する．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|EQ|等号(rs1 == rs2)|`{3'h4, 6'h00, 4'h0, rs1, rs2, 6'h00, imm}`|
|NE|不等号(rs1 != rs2)|`{3'h4, 6'h01, 4'h0, rs1, rs2, 6'h00, imm}`|
|LT|符号あり小なり(rs1 < rs2)|`{3'h4, 6'h02, 4'h0, rs1, rs2, 6'h00, imm}`|
|GT|符号あり大なり(rs1 > rs2)|`{3'h4, 6'h03, 4'h0, rs1, rs2, 6'h00, imm}`|
|ELT|符号あり以下(rs1 <= rs2)|`{3'h4, 6'h04, 4'h0, rs1, rs2, 6'h00, imm}`|
|EGT|符号あり以上(rs1 >= rs2)|`{3'h4, 6'h05, 4'h0, rs1, rs2, 6'h00, imm}`|
|LTU|符号なし小なり(rs1 < rs2)|`{3'h4, 6'h06, 4'h0, rs1, rs2, 6'h00, imm}`|
|GTU|符号なし大なり(rs1 > rs2)|`{3'h4, 6'h07, 4'h0, rs1, rs2, 6'h00, imm}`|
|ELTU|符号なし以下(rs1 <= rs2)|`{3'h4, 6'h08, 4'h0, rs1, rs2, 6'h00, imm}`|
|EGTU|符号なし以上(rs1 >= rs2)|`{3'h4, 6'h09, 4'h0, rs1, rs2, 6'h00, imm}`|

比較判定にFLGレジスタは関与せず，rs1・rs2の値を直接比較する(FLGは前述の通り現在未実装)．

# ジャンプ系(J系)
`JMP`・`CALL`は，rs1またはイミディエイトデータが示す値をそのままPCとする．\
`CALL`の場合はさらに，戻り先アドレス(PC+1)をメモリのSP-4番地へ書き込み，SPを4減らしてからジャンプする．\
`RET`の場合，メモリのSP番地から読み出した値をPCとし，SPを4増やす．引数なし．\
プログラムの開始点(`main`)からの復帰にはRETを使わず，同じ命令を実行し続ける`JMP`で表現する(正常終了の規定は[assembler.md](assembler.md)を参照)．\
戻り先はメモリ上のスタックへ4バイト単位(mask=`4'hf`)で積み下ろしする(スタックとSPの扱いは[memory.md](memory.md)を参照)．CALL/RETが読み書きする番地にはRM/WMと同じ番地の条件が適用され，空のスタックでの`RET`などはCPUを停止させる([halt.md](halt.md)を参照)．
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|JMP|ジャンプ|`{3'h5, 6'h00, 4'h0, rs1, 6'h00, 6'h00, imm}`|
|CALL|関数呼び出し|`{3'h5, 6'h01, 4'h0, rs1, 6'h00, 6'h00, imm}`|
|RET|関数リターン|`{3'h5, 6'h02, 4'h0, 6'h00, 6'h00, 6'h00, 33'h000000000}`|

# メモリ系(M系)
指定された処理と同時に，PCをインクリメントする．\
読み込み: 指定された番地のデータをrdのレジスタへ読み込み\
書き込み: 指定された番地へrs2のデータを書き込み
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|RM|メモリ読み込み|`{3'h6, 6'h00, mask, rs1, 6'h00, rd, imm}`|
|WM|メモリ書き込み|`{3'h6, 6'h01, mask, rs1, rs2, 6'h00, imm}`|
|BRM|メモリ読み込み(バースト転送)|`{3'h6, 6'h02, mask, rs1, rs2, rd, imm}`|
|BWM|メモリ書き込み(バースト転送)|`{3'h6, 6'h03, mask, rs1, rs2, rd, imm}`|
|RMR|メモリ読み込み(レジスタ相対)|`{3'h6, 6'h04, mask, rs1, 6'h00, rd, imm}`|
|WMR|メモリ書き込み(レジスタ相対)|`{3'h6, 6'h05, mask, rs1, rs2, 6'h00, imm}`|

RM/WMは，rs1またはイミディエイトデータで番地を指定する(基本構成の一般則どおり，imm使用時はrs1を無視する)．WMのrs2(書き込みデータ)は番地の指定に関わらないため，immの使用によらず常に使用する．

RMR/WMRは，rs1にイミディエイトデータを足した番地を読み書きする．足し算は32ビットで行い，桁あふれした上位ビットは捨てる(負の値を表すイミディエイトデータを足せば，rs1より小さい番地を指定できる)．足した結果の番地にRM/WMと同じ番地の条件を適用する([halt.md](halt.md)を参照)．immの使用は必須であり，rs1とimmを常に両方使用する点が基本構成の一般則(imm使用時はrs1またはrs2を無視する)の例外にあたる．\
SPをrs1に指定すれば，スタック上の値をSPからの相対位置で読み書きできる(スタックの扱いは[memory.md](memory.md)を参照)．

BRM/BWMはバースト転送自体が未実装である(実行しようとした場合の挙動は[halt.md](halt.md)を参照)．また，詳細仕様(タイミング・last信号の扱い等)自体も未検討である．RAM側のバースト転送機構自体は[memory.md](memory.md)を参照．

## 番地とmaskの詳細動作

`address`(上記の方法で指定した番地)は4の倍数である必要はなく，任意のバイト番地を指定できる．ただし`mask`が対象とする範囲は，同一の4バイトワード境界内に収まっている必要がある(ワードをまたぐ組み合わせは非対応．詳細は[memory.md](memory.md)を参照)．\
`mask`はビットiが立っているとき，メモリの`address + i`番地と，レジスタ/データバスの`bit[i*8+7:i*8]`とを直接結び付ける(番地とビット位置の対応はmaskのビット位置iのみで決まり，シフト処理は行われない)．

- **RM・RMR**: `mask`のビットiが立っている番地(`address+i`)のデータを，そのまま`rd`の`bit[i*8+7:i*8]`に格納する．ビットiが立っていない桁は`0`で埋める(ゼロ拡張)．\
  そのため，読みたいバイトの実アドレスを`address`に指定し，`mask`の最下位ビット(bit0)から詰めて指定すれば，自動的にレジスタのLSB側に値が格納される．
- **WM・WMR**: `mask`のビットiが立っている桁(`rs2`の`bit[i*8+7:i*8]`)のみ，`address+i`番地に書き込む．ビットiが立っていない番地は書き込みを行わず，元の値をそのまま保持する(read-modify-write)．

例: char型変数が番地3に配置されている場合，`address=3`，`mask=0001`(bit0のみ)を指定すれば，`memory_data[3]`が`rd`の`bit[7:0]`に読み込まれる(シフト不要)．\
short型(2バイト)の場合は`mask=0011`のように連続した2ビットを指定すれば，2バイトがLSB側から詰めて読み書きされる．

# 標準入出力系(IO系)
指定された入出力が完了する(入力が届く，または出力が受け取られる)まで次の命令へ進まず，完了したらPCをインクリメントする．\
maskと，使用しないアドレスは`0`で固定．\
入力: 読み込んだデータをrdに書き込み\
出力: rs1またはイミディエイトデータを出力
|機械語名称|日本語名称|機械語|
|:-|:-|:-|
|SCAN|標準入力|`{3'h7, 6'h00, 4'h0, 6'h00, 6'h00, rd, 33'h000000000}`|
|PRINT|標準出力|`{3'h7, 6'h01, 4'h0, rs1, 6'h00, 6'h00, imm}`|
