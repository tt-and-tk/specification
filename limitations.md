# 一般的なCPUとの差分・非対応事項

一般的なCPUでは広く使われているが，QurgeのCPUでは(主にハードウェア制約により)対応しない事項をまとめる．
C言語機能としての非対応一覧は compiler.md を参照．

| 項目 | 一般的なCPU | QurgeのCPU | 理由・帰結 | 将来対応 |
|:-|:-|:-|:-|:-|
| データスタック | SPで伸縮するメモリ上のスタック | 無し．SP(`6'h10`)は読み書き禁止で戻り先インデックス専用 | スタックフレームを確保できない | SPの役割を戻り先インデックスから汎用メモリスタックポインタへ再定義するハードウェア変更で対応予定 |
| 戻り先アドレス | メモリスタックに積む(深さは事実上無制限) | 専用レジスタ`6'h11`〜`6'h1a`に保存(10本) | 関数呼び出しネストは**最大10段**(`main`自身は数えない)．call/retでハードウェアが自動管理し，超過したcallは実行時にCPUを停止させる([halt.md](halt.md)を参照) | 予定なし |
| ローカル変数の配置 | スタックフレーム上(呼び出しごとに確保) | 絶対番地への静的割り当て(固定番地1つ) | スタックが無いため | OS対応時に相対アドレスへ移行予定 |
| グローバル変数の配置 | 相対アドレス等(ロード時に再配置) | 絶対番地への固定配置 | 簡易実装のため | OS対応時に相対アドレスへ移行予定 |
| 初期化子のないグローバル変数の初期値 | スタートアップコードやローダが静的記憶域を0にクリアする | 初期化しない．値は保証されない | メモリはリセットで初期化されないため，前回の実行で書き込まれた値が残りうる([memory.md](memory.md)を参照)．0から始める場合は初期化子を明示し，初期化子を持てない変数([compiler.md](compiler.md)を参照)には実行時に0を書き込む | OS(プログラムのロード実行)対応時に検討 |
| 再帰呼び出し・同一関数の入れ子呼び出し | 対応 | **非対応** | パラメータ・ローカル変数が関数ごとの固定番地にあるため，ある関数の呼び出しの途中(引数の書き込みから戻るまで)に同じ関数が再び呼ばれると，書き込み済みの値が上書きされる．再帰はコンパイルエラーになる．同一関数の入れ子呼び出し(例: `add(1, add(2, 3))`)は，再帰ではないため検出されず，条件によって正しく動作しない(条件は[compiler.md](compiler.md)を参照) | SPの役割を戻り先インデックスから汎用メモリスタックポインタへ再定義するハードウェア変更，およびパラメータ・ローカル変数を呼び出しごとのスタックフレームとして確保するコンパイラ対応の両方を伴う対応を予定 |
| 汎用レジスタの呼び出し間保存 | callee-saved等で保持可能 | 揮発(呼び出しをまたいで保持されない) | 退避先スタックが無い．永続データはメモリに置く | 予定なし |
| 動的メモリ確保 | ヒープ/可変長スタック | 静的割り当てのみ | 変数は絶対番地に固定配置 | 予定なし |
| PCへの直接書き込み | 可能な場合がある | 不可(`6'h1f`は書き込み禁止) | 分岐はJ系/F系命令で行う | 予定なし |
| プログラムの終了 | 呼び出し元やOSへ制御と終了ステータスを返す | 終了した位置で同じ命令を実行し続ける(自己ループ)．リセットまでその状態を保つ([assembler.md](assembler.md)を参照) | 制御を返す先も終了を外部へ通知する手段も存在しない | OS(プログラムのロード実行)対応時に検討 |
| メモリのバースト転送 | 対応可能な場合がある | 非対応．BRM/BWM命令のアセンブル自体は可能だが，CPU側の実行ロジックが未実装で実行しようとすると停止する([halt.md](halt.md)を参照) | ハードウェア側の転送ロジックが未実装([memory.md](memory.md)も参照) | 予定なし |

## その他の量的制限

- プログラム最大命令数: **4096命令**(プログラムカウンタのビット幅を4096命令をちょうど表現できる幅(12ビット)に固定しており，ハードウェア側にも同じ4096命令の上限がある．コンパイラ・アセンブラ側でも同じ値を安全上限として検査しており，超過するとコンパイルエラー．詳細は下記「ROM容量について」を参照)
- 関数呼び出しネスト: **最大10段**(上記の戻り先レジスタ本数による．`main`自身は数えない．コンパイラが呼び出しグラフから静的検査．超過・再帰呼び出しはコンパイルエラー)
- 式の複雑さ: **汎用レジスタ16本(r0〜r15)で評価できる範囲まで**(式の評価途中の値や組み込み関数の作業用の値を汎用レジスタに置き，足りなくなった分をメモリへ逃がして評価を続けることはしないため．入れ子が深い式ほど多くのレジスタを同時に使う．コンパイラが式ごとに検査し，超過するとコンパイルエラー)

## ROM容量について

ROMの命令数上限は，コンパイル対象プログラムのサイズに応じてアセンブラが実行時に自動算出するが，プログラムカウンタのビット幅がアセンブラの安全上限(4096命令)をちょうど表現できる12ビットに固定されているため，ハードウェア側にも同じ4096命令の上限が存在する．\
アセンブラ側の安全上限を4096命令より増やす場合は，ハードウェア側のプログラムカウンタのビット幅も同時に拡張する必要がある．\
ROM読み出し回路はBlock RAM(BRAM)に推論される設計であり，PYNQ-Z2搭載のZynq-7020(XC7Z020)全体のBRAM容量(4.9Mbit)を使い切れば理論上は数万命令規模まで格納できるが，これを活用するにはプログラムカウンタのビット幅の拡張が別途必要となる．

## 将来対応予定について

上記「将来対応」列のうち対応予定のものは，現状がスモールスタートの簡易実装であることに由来する暫定制約である．
変数配置の相対アドレス化はOS(プログラムのロード実行)対応が前提．データスタックの追加はSP(`6'h10`)の役割を戻り先インデックスから汎用メモリスタックポインタへ再定義するハードウェア変更が前提．再帰呼び出し・同一関数の入れ子呼び出しへの対応は，このSP再定義とパラメータ・ローカル変数を呼び出しごとのスタックフレームとして確保するコンパイラ対応の両方を前提とする．
