# 通常のCPUとの差分・非対応事項

通常のCPUでは一般的だが，QurgeのCPUでは(主にハードウェア制約により)対応しない事項をまとめる．
C言語機能としての非対応一覧は compiler.md を参照．

| 項目 | 通常のCPU | QurgeのCPU | 理由・帰結 | 将来対応 |
|:-|:-|:-|:-|:-|
| データスタック | SPで伸縮するメモリ上のスタック | 無し．SP(`6'h10`)は読み書き禁止で戻り先インデックス専用 | スタックフレームを確保できない | SPを使用可能にするハードウェア変更で対応予定 |
| 戻り先アドレス | メモリスタックに積む(深さは事実上無制限) | 専用レジスタ`6'h11`〜`6'h1a`に保存(10本) | 関数呼び出しネストは**最大10段**．call/retでハードウェアが自動管理 | 予定なし |
| ローカル変数の配置 | スタックフレーム上(呼び出しごとに確保) | 絶対番地への静的割り当て(固定番地1つ) | スタックが無いため | OS対応時に相対アドレスへ移行予定 |
| グローバル変数の配置 | 相対アドレス等(ロード時に再配置) | 絶対番地への固定配置 | 簡易実装のため | OS対応時に相対アドレスへ移行予定 |
| 再帰呼び出し | 対応 | **非対応** | 固定番地のため再帰すると上書きされる | 対応予定(上記SPのハードウェア変更が前提) |
| 汎用レジスタの呼び出し間保存 | callee-saved等で保持可能 | 揮発(呼び出しをまたいで保持されない) | 退避先スタックが無い．永続データはメモリに置く | 予定なし |
| 動的メモリ確保 | ヒープ/可変長スタック | 静的割り当てのみ | 変数は絶対番地に固定配置 | 予定なし |
| PCへの直接書き込み | 可能な場合がある | 不可(`6'h1f`は書き込み禁止) | 分岐はJ系/F系命令で行う | 予定なし |

## その他の量的制限

- プログラム最大命令数: **4096命令**(プログラムカウンタのビット幅をこの値に合わせて固定しており，ハードウェア側にも同じ4096命令の上限がある．コンパイラ・アセンブラ側でも同じ値を安全上限として検査しており，超過するとコンパイルエラー．詳細は下記「ROM容量について」を参照)
- 関数呼び出しネスト: **最大10段**(上記の戻り先レジスタ本数による．コンパイラが呼び出しグラフから静的検査．超過・再帰呼び出しはコンパイルエラー)

## ROM容量について

ROM(`rom_sv.sv`)の命令数上限は，コンパイル対象プログラムのサイズに応じてアセンブラが実行時に自動算出する(`machines`配列は実際の命令数分だけ確保される)が，プログラムカウンタのビット幅(`pc_bus_t`)がアセンブラの安全上限(`MAX_LINE_NUM`)と同じ4096に固定されているため，ハードウェア側にも同じ4096命令の上限が存在する．\
アセンブラ側の上限(`MAX_LINE_NUM`)を4096より増やす場合は，`pc_bus_t`のビット幅も同時に拡張する必要がある．\
また，現在のROM読み出し回路は組合せ論理(非同期読み出し)であり，この方式のままではBlock RAM(BRAM)には推論されずLUT資源を消費するため，`pc_bus_t`を拡張しても実質的な上限はLUT資源に依存する．\
PYNQ-Z2搭載のZynq-7020(XC7Z020)全体のBRAM容量(4.9Mbit)を使い切れば理論上は数万命令規模まで格納できるが，これを活用するには`pc_bus_t`の拡張に加え，ROM読み出し回路を同期(クロック同期)方式に変更しBRAM推論可能な設計にするハードウェア変更が別途必要となる．

## 将来対応予定について

上記「将来対応」列のうち対応予定のものは，現状がスモールスタートの簡易実装であることに由来する暫定制約である．
変数配置の相対アドレス化はOS(プログラムのロード実行)対応が前提，データスタック・再帰対応はSP(`6'h10`)を使用可能にするハードウェア変更が前提となる．
詳細な移行計画は CLAUDE.md「将来対応予定」表，および compiler.md を参照．
