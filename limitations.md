# 一般的なCPUとの差分・非対応事項

一般的なCPUでは広く使われているが，QurgeのCPUでは(主にハードウェア制約により)対応しない事項をまとめる．
C言語機能としての非対応一覧は compiler.md を参照．

| 項目 | 一般的なCPU | QurgeのCPU | 理由・帰結 | 将来対応 |
|:-|:-|:-|:-|:-|
| グローバル変数の配置 | 相対アドレス等(ロード時に再配置) | 絶対番地への固定配置 | プログラムを置く番地が常に同じ(ROMは0番地から，Qosmosの実行ファイルはメモリの後半の先頭から)ため，再配置しない簡易な実装で動く | 実行ファイルを固定でない番地へ読み込む(再配置する)ようになれば，相対アドレスへ移行する |
| 初期化子のないグローバル変数の初期値 | スタートアップコードやローダが静的記憶域を0にクリアする | 初期化しない．値は保証されない | メモリはリセットで初期化されないため，前回の実行で書き込まれた値が残りうる([memory.md](memory.md)を参照)．0から始める場合は初期化子を明示し，初期化子を持てない変数([compiler.md](compiler.md)を参照)には実行時に0を書き込む | 機会があれば検討 |
| 汎用レジスタの呼び出し間保存 | callee-saved等で保持可能 | 揮発(呼び出しをまたいで保持されない) | 呼び出し先が汎用レジスタを自由に使ってよい規約のため．呼び出しをまたいで必要な値は，コンパイラがメモリ(変数・スタックフレーム上の退避領域)へ置く | 予定なし |
| 動的メモリ確保 | ヒープ/スタック上の可変長領域(allocaなど) | 静的割り当てのみ | 確保する大きさが実行時に決まる領域を扱う仕組みを持たない(スタックフレームの大きさはコンパイル時に確定する) | 予定なし |
| PCへの直接書き込み | 可能な場合がある | 不可(`6'h1f`は書き込み禁止) | 分岐はJ系/F系命令で行う | 予定なし |
| メモリのバースト転送 | 対応可能な場合がある | 非対応．BRM/BWM命令のアセンブル自体は可能だが，CPU側の実行ロジックが未実装で実行しようとすると停止する([halt.md](halt.md)を参照) | ハードウェア側の転送ロジックが未実装([memory.md](memory.md)も参照) | 予定なし |

## その他の量的制限

- プログラム最大命令数: ROMに置くプログラムは**16384命令**，実行ファイルはメモリの後半に収まる**4096命令**
  - ROM: ハードウェアはこの上限をちょうど表現できる幅でプログラムカウンタをROMへ渡しており，同じ16384命令の上限がある．コンパイラ・アセンブラ側でも同じ値を安全上限として検査しており，超過するとコンパイルエラー．詳細は下記「ROM容量について」を参照
  - 実行ファイル: メモリの後半の大きさ(32KB)で決まる．アセンブラが検査し，超過するとエラー([assembler.md](assembler.md)の「出力先」を参照)
- 関数呼び出しネスト: 固定の上限はない．深さの上限は，戻り先とスタックフレームを積むスタックの残り容量で決まる([memory.md](memory.md)を参照)
- 変数に使えるメモリ: 出力先によって次のように異なる
  - ROM: グローバル変数は0番地から，スタックはメモリの前半の上端(`0x8000`)から伸びるため，両者の合計がメモリの前半(32768バイト)に収まる必要がある([memory.md](memory.md)を参照)
  - 実行ファイル: グローバル変数はメモリの後半の上端から下へ，命令列(コード領域)はメモリの後半の先頭から上へ伸びるため，両者の合計がメモリの後半(32768バイト)に収まる必要がある．スタックは，呼び出された時点のSPからシェルのグローバル変数の手前までを使える([qosmos.md](qosmos.md)の「実行ファイル」を参照)
  - コンパイラは，上記の領域に収まらない次の場合をコンパイルエラーにする
    - 出力先によらず，グローバル変数だけでメモリの半分(32768バイト)を超える場合
    - ROM用で，再帰呼び出しのないプログラムのスタックを含めた合計がメモリの前半を超える場合．関数ポインタを通した呼び出しは呼び出し先が実行時に決まるため，番地を取得された全関数を呼びうるものとして見積もる(実際より多く見積もりうる)．整数からキャストで作った関数ポインタの呼び出し先は見積もりに含まない．再帰呼び出しがある場合と，この見積もりの中で呼び出しが循環する場合は，スタックの深さが実行時にしか決まらないため検査できず，重なってもCPUは検出しない
    - 実行ファイル用で，命令列(1命令8バイト)を含めた合計がメモリの後半を超える場合．スタックは呼び出された時点のSPによって空きが決まるため検査しない
- 式の複雑さ: **汎用レジスタ16本(r0〜r15)で評価できる範囲まで**(式の評価途中の値や組み込み関数の作業用の値を汎用レジスタに置き，足りなくなった分をメモリへ逃がして評価を続けることはしないため．入れ子が深い式ほど多くのレジスタを同時に使う．コンパイラが式ごとに検査し，超過するとコンパイルエラー)

## ROM容量について

ROMに実際に確保される容量は，コンパイル対象プログラムの命令数に応じてアセンブラが自動算出する．\
一方，命令数の上限は16384命令に固定されている．ハードウェアは32ビットのプログラムカウンタのうち，これをちょうど表現できる下位14ビットをROMへ渡し([rom.md](rom.md)を参照)，アセンブラ・コンパイラは同じ値を安全上限として検査する．

上限を増やす場合は，次のものを合わせて変更する．

- ハードウェア: 命令数の上限と，そこから決まるROMへ渡すプログラムカウンタの幅・その直後に置く，メモリの後半の先頭に対応するPC([memory.md](memory.md#メモリ上の命令の実行)を参照)
- アセンブラ・コンパイラ: 命令数の安全上限
- アセンブラ: 実行ファイルの先頭の命令に割り当てるPC([assembler.md](assembler.md)の「出力先」を参照)
- 作り直すもの: メモリの後半に置くプログラムと，それを読み込んで呼び出すシェル([qosmos.md](qosmos.md)の「実行ファイル」を参照)

上限は，メモリの後半に置ける命令数(4096)以上の2のべき乗とする．CPUは次の条件を前提に，PCがメモリの後半を指すかを判定するためである(満たさない値にすると論理合成がエラーで止まる)．

- メモリの後半の先頭に対応するPCが，メモリの後半に置ける命令数の倍数である
- メモリの後半の先頭に対応するPCが，ROMへ渡す幅で表せる範囲のちょうど直後である

ROM読み出し回路はBlock RAM(BRAM)に推論され，36KbitのBRAM1個におよそ512個の64ビット命令が収まる．上限は，ファイルシステム(FAT32)を含むQosmos全体を収めた上で今後の機能追加にも余裕を残す値として定めている．上限まで使ってもPYNQ-Z2搭載のZynq-7020(XC7Z020)が持つ140個のうち32個(約23%)にとどまり，残りはメインメモリに充てられる．

## 将来対応予定について

上記「将来対応」列のうち対応予定のものは，現状がスモールスタートの簡易実装であることに由来する暫定制約である．
グローバル変数配置の相対アドレス化は，実行ファイルを再配置して読み込むようになることが前提．
