# 仕様
|用語|仕様|
|:-|:-|
|データバス|32ビット|
|アドレスバス|12ビット|
|容量|8ビットのレジスタ4096本，4KB|

書き込みと読み込みを別のインターフェースで同時に行える．\
同じアドレスに対して同時に書き込み・読み込みすることも可能だが，動作は保証しない．

バースト転送用の仕組み(last信号)は用意されているが，現時点では非対応であり，転送のたびにlast信号を`true`にして単発の転送として扱う．また，詳細仕様(タイミング・last信号の扱い等)自体も未検討である．

イミディエイトデータで指定するメモリアドレスは，データバス幅(32ビット)で扱われる．\
このうちアドレスバス幅を超える上位ビットが1ビットでも立っている場合，折り返して有効な番地とみなすことはせず，不正な番地として実行を停止する(プログラムカウンタについては[rom.md](rom.md)を参照)．\

読み込み・書き込みそれぞれにおいてマスクを使用して対象のバイトを指定できる．マスクのビット幅は4ビットで，1ビットが読み書き対象の1バイトに対応する．\
リトルエンディアン(番地の小さいほうがLSB側)を採用する．\
なお，メモリ上の番地自体は0,1,2,3,…と普通の昇順に並んでおり，特殊な並び替えはない．\
下記の図は物理的な番地の配置ではなく，`address`を起点に読み書きした際，各番地のバイトがレジスタ/データバス上のどのビット位置に対応するかを示したものである．
```
31          23          15          7        0
|address + 3|address + 2|address + 1|address|
```

1回の読み込み・書き込みでmaskが対象とするバイト範囲(`address`からmaskの最上位ビットが指す番地まで)は，4バイト境界に揃った同一ワード内に収まっている必要がある．ワードをまたぐ`address`とmaskの組み合わせを指定した場合，範囲外番地アクセスとしてのエラーにはならず，同一ワード内で折り返した誤った番地を読み書きする(実装がバイトレーンごとに分割されたBlock RAMで構成されており，1回のアクセスで参照できるワード番地が1つに限られるため)．\
コンパイラ(Pynesis)が生成するコードでは，全ての変数が4バイト境界に配置され，short(2バイト)・int(4バイト)型のアクセスもその境界内に収まるよう生成されるため，この制約に抵触しない．手書きのアセンブリ(Pyntaxis)でRM/WM/BRM/BWMを直接記述する場合は，この制約を意識する必要がある．
