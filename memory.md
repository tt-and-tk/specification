# 仕様
|用語|仕様|
|:-|:-|
|データバス|32ビット|
|アドレスバス|28ビット|
|容量|8ビットのレジスタ28本，約270MB|

書き込みと読み込みを別のインターフェースで同時に行える．\
同じアドレスに対して同時に描き込み・読み込みすることも可能だが，動作は保証しない．

バースト転送も可能．その場合，転送中はlast信号を`false`にし，一回分のメモリ書き込み・読み込みが終わるたびに次のアドレスを指定する．\
※バースト転送にはひとまず対応しない．

マスクを使用し，どのバイトを読み込む・書き込むのか指定する．\
リトルエンディアン(番地の小さいほうがLSB側)を採用する．\
なお，メモリ上の番地自体は0,1,2,3,…と普通の昇順に並んでおり，特殊な並び替えはない．\
下記の図は物理的な番地の配置ではなく，`address`を起点に読み書きした際，各番地のバイトがレジスタ/データバス上のどのビット位置に対応するかを示したものである．
```
31          23          15          7        0
|address + 3|address + 2|address + 1|address|
```
