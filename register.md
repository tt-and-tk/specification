# 仕様
QurgeのCPUは，そのレジスタおよびpc，ボード上の各種スイッチに直接アクセスできる．

16進数リテラルの英字部分は大文字・小文字のどちらでも機能上の違いはないが，本ドキュメントでは小文字表記に統一する．

|アドレス|割り当て|
|:-|:-|
|`6'h00`～`6'h0f`|CPU内のレジスタ．|
|`6'h10`|SP．スタックポインタ．下記戻り先のうち，現在使用しているもの．読み書き禁止．|
|`6'h11`～`6'h1a`|呼び出し関数の戻り先を保存．読み書き禁止．|
|`6'h1b`|空き．読み書きとも不可．|
|`6'h1c`|FLG．フラグ．書き込み禁止．|
|`6'h1d`|RSI．引数が格納されているレジスタの一つ目の番地|
|`6'h1e`|RAX．演算結果．|
|`6'h1f`|PC．プログラムカウンタ．書き込み禁止．読み出すと，その命令自身の番地が返る．|
|`6'h20`|タクトスイッチ．上位4ビットは0固定．書き込み禁止．|
|`6'h21`|DIPスイッチ．上位6ビットは0固定．書き込み禁止．|
|`6'h22`|LED．上位4ビットは0固定．読み込み禁止．|
|`6'h23`|RGB LED．上位2ビットは0固定．読み込み禁止．|
|`6'h24`|Pmod A．現在は物理ピンと未結線．|
|`6'h25`|Pmod B．現在は物理ピンと未結線．|
|`6'h26`|AR8～AR13．上位2ビットは0固定．現在は物理ピンと未結線．|
|`6'h27`|A，AR_SDA，AR_SCL．上位5ビットは0固定．現在は物理ピンと未結線．|
|`6'h28`|AR0～AR7．現在は物理ピンと未結線．|
|`6'h29`|AR_RST．上位7ビットは0固定．書き込み禁止．現在は物理ピンと未結線．|
|`6'h2a`|AR_MISO，AR_SCK，AR_MOSI，AR_SS．上位4ビットは0固定．現在は物理ピンと未結線．|
|`6'h2b`|アナログピン．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2c`|XADC．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2d`|GPIO0～GPIO7．現在は物理ピンと未結線．|
|`6'h2e`|GPIO8～GPIO15．現在は物理ピンと未結線．|
|`6'h2f`|GPIO16～GPIO23．現在は物理ピンと未結線．|
|`6'h30`|GPIO24～GPIO27．上位4ビットは0固定．現在は物理ピンと未結線．|
|`6'h31`|標準入力データ．書き込み禁止．|
|`6'h32`|標準入力シグナル．`{29'h0, tlast, tvalid, tready}`|
|`6'h33`|標準出力データ．読み込み禁止．|
|`6'h34`|標準出力シグナル．`{29'h0, tlast, tvalid, tready}`|

アドレスは6ビットで`6'h3f`まで表現できるが，`6'h35`～`6'h3f`には対応するレジスタが存在しない．

# フラグについて
フラグ内の使用ビット
|ビット|名前|機能|
|:-|:-|:-|
|0ビット目|CF|キャリーフラグ．桁あふれが生じたときtrue|
|1ビット目|ZF|ゼロフラグ．計算結果が0のときtrue，0以外のときfalse|
|2ビット目|OF|オーバーフローフラグ．符号付の数の足し算や引き算において，最上位ビットが不正に書き変えられてしまったときにtrue|
|3ビット目|SF|サインフラグ．計算結果の符号を保存する．正ならfalse，負ならtrue|
