# 仕様
QurgeのCPUは，下表のアドレスを指定して，CPU内のレジスタ・ボード上の周辺機器(スイッチ・LED・外部接続用のピン等)・標準入出力に直接アクセスできる．各レジスタは32ビット幅である．

|アドレス|割り当て|
|:-|:-|
|`6'h00`～`6'h0f`|汎用レジスタ．|
|`6'h10`|SP．スタックポインタ．メモリ上のスタックに最後に積んだ値の番地を保持する(スタックの扱いは[memory.md](memory.md)を参照)．リセット時の初期値はメモリ容量と同じ`0x10000`．|
|`6'h11`～`6'h1b`|空き．読み書きとも不可．|
|`6'h1c`|FLG．フラグ．書き込み禁止．|
|`6'h1d`|RSI．現在いずれの命令からも使用されない未実装のレジスタである(実際の引数渡しは[compiler.md](compiler.md)の呼び出し規約を参照)．|
|`6'h1e`|RAX．演算結果．|
|`6'h1f`|PC．プログラムカウンタ．書き込み禁止．読み出すと，その命令自身の番地が返る．|
|`6'h20`|タクトスイッチ．下位4ビットを使用し，上位28ビットは0固定．書き込み禁止．基板上のBTN0～BTN3に結線(0ビット目=BTN0～3ビット目=BTN3)．押下中が1，非押下が0．|
|`6'h21`|DIPスイッチ．下位2ビットを使用し，上位30ビットは0固定．書き込み禁止．基板上のSW0・SW1に結線(0ビット目=SW0，1ビット目=SW1)．ON側が1，OFF側が0．|
|`6'h22`|LED．下位4ビットを使用し，上位28ビットは0固定．読み込み禁止．基板上のLD0～LD3に結線(0ビット目=LD0～3ビット目=LD3)．1で点灯する．|
|`6'h23`|RGB LED．下位6ビットを使用し，上位26ビットは0固定．読み込み禁止．基板上のLD4・LD5の各色に結線(0ビット目=LD4青，1ビット目=LD4緑，2ビット目=LD4赤，3ビット目=LD5青，4ビット目=LD5緑，5ビット目=LD5赤)．1で点灯し，同じLEDの複数の色を同時に立てると混色になる．|
|`6'h24`|Pmod A．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Pmod Aの8ピンに結線．|
|`6'h25`|Pmod B．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Pmod Bの8ピンに結線．|
|`6'h26`|AR8～AR13．下位6ビットを使用し，上位26ビットは0固定．書き込み専用で使用している．Arduinoデジタル入出力8～13番ピンに結線(0ビット目=AR8～5ビット目=AR13)．|
|`6'h27`|A(AREF)，AR_SDA，AR_SCL．下位3ビットを使用し，上位29ビットは0固定．書き込み専用で使用している(2ビット目=A，1ビット目=AR_SDA，0ビット目=AR_SCL)．Arduinoの単体デジタルピン・I2C用SDA/SCLピンに結線．|
|`6'h28`|AR0～AR7．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Arduinoデジタル入出力0～7番ピンに結線(0ビット目=AR0～7ビット目=AR7)．|
|`6'h29`|AR_RST．下位1ビットを使用し，上位31ビットは0固定．書き込み禁止．現在は物理ピンと未結線．|
|`6'h2a`|AR_MISO，AR_SCK，AR_MOSI，AR_SS．下位4ビットを使用し，上位28ビットは0固定(3ビット目=MISO，2ビット目=SCK，1ビット目=MOSI，0ビット目=SS)．Arduino SPIの4ピンに結線．MISOビットは外部ピンの値を毎サイクル反映するミラーで書き込んでも意味を持たず，他の3ビットは書き込んだ値がそのまま出力される．MISOは非同期入力のため2段のフリップフロップで同期化してから取り込んでおり，ピンの値がこのビットへ現れるまで3サイクルかかる．SSはアクティブLowのため，初期値は非選択を表す1．|
|`6'h2b`|アナログピン．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2c`|XADC．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2d`|GPIO0(0ビット目)～GPIO7(7ビット目)．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．GPIO番号はRaspberry PiのGPIO番号(BCM番号)と一致する．GPIO0～7はPmod Aと同じ物理ピンであり，そのピンにはPmod Aのレジスタの値を出力している．このため，このレジスタに書き込んだ値は保持されるだけでピンには反映されない．|
|`6'h2e`|GPIO8(0ビット目)～GPIO15(7ビット目)．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．ラズパイヘッダーに結線．|
|`6'h2f`|GPIO16(0ビット目)～GPIO23(7ビット目)．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．ラズパイヘッダーに結線．|
|`6'h30`|GPIO24(0ビット目)～GPIO27(3ビット目)．下位4ビットを使用し，上位28ビットは0固定．書き込み専用で使用している．GPIO24～26はラズパイヘッダーに結線，GPIO27は対応する物理ピンが無いため未結線．|
|`6'h31`|標準入力データ．書き込み禁止．|
|`6'h32`|標準入力シグナル．`{29'h0, tlast, tvalid, tready}`．標準入力の信号線の状態を毎サイクル写し取るだけのレジスタであり，信号線の駆動には関与しない．読み書き禁止．|
|`6'h33`|標準出力データ．読み込み禁止．|
|`6'h34`|標準出力シグナル．`{29'h0, tlast, tvalid, tready}`．標準出力の信号線の状態を毎サイクル写し取るだけのレジスタであり，信号線の駆動には関与しない．読み書き禁止．|

アドレスは6ビットで`6'h3f`まで表現できるが，`6'h35`～`6'h3f`には対応するレジスタが存在しない．

リセット時・停止時の初期値は，上表で初期値を示したもの以外は0である(停止の扱いは[halt.md](halt.md)を参照)．

本仕様では，レジスタ単位でのみ読み書き制限を設けており，ビット単位の読み書き制御機構は持たない．そのため，入出力の方向が信号ごとに異なるレジスタ(例: `6'h2a`)であっても，レジスタ全体としての読み書き制限は設けない．

# フラグについて
FLGは他のCPU内レジスタと同じく32ビットのレジスタであり，現在未実装であるためいずれの命令からも値が格納されることはない(詳細は[isa.md](isa.md)を参照)．4ビット目以降は未使用である．

フラグ内の使用ビット
|ビット|名前|機能|
|:-|:-|:-|
|0ビット目|CF|キャリーフラグ．桁あふれが生じたときtrue，生じなかったときfalse|
|1ビット目|ZF|ゼロフラグ．計算結果が0のときtrue，0以外のときfalse|
|2ビット目|OF|オーバーフローフラグ．符号付の数の足し算や引き算において，最上位ビットが不正に書き変えられてしまったときtrue，そうでないときfalse|
|3ビット目|SF|サインフラグ．計算結果の最上位ビットを保存する．最上位ビットが1(負)のときtrue，0(0または正)のときfalse|
