# 仕様
QurgeのCPUは，そのレジスタおよびpc，ボード上の各種スイッチに直接アクセスできる．各レジスタは32ビット幅である．

|アドレス|割り当て|
|:-|:-|
|`6'h00`～`6'h0f`|CPU内のレジスタ．|
|`6'h10`|SP．スタックポインタ．下記戻り先のうち，現在使用しているもの．読み書き禁止．|
|`6'h11`～`6'h1a`|呼び出し関数の戻り先を保存．読み書き禁止．|
|`6'h1b`|空き．レジスタ割り当て時に単純に生じた空き番地であり，特に理由はない．読み書きとも不可．|
|`6'h1c`|FLG．フラグ．書き込み禁止．|
|`6'h1d`|RSI．現在いずれの命令からも使用されない未実装のレジスタである(実際の引数渡しは[compiler.md](compiler.md)の呼び出し規約を参照)．|
|`6'h1e`|RAX．演算結果．|
|`6'h1f`|PC．プログラムカウンタ．書き込み禁止．読み出すと，その命令自身の番地が返る．|
|`6'h20`|タクトスイッチ．下位4ビットを使用し，上位28ビットは0固定．書き込み禁止．|
|`6'h21`|DIPスイッチ．下位2ビットを使用し，上位30ビットは0固定．書き込み禁止．|
|`6'h22`|LED．下位4ビットを使用し，上位28ビットは0固定．読み込み禁止．|
|`6'h23`|RGB LED．下位6ビットを使用し，上位26ビットは0固定．読み込み禁止．|
|`6'h24`|Pmod A．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Pmod Aの8ピンに結線．|
|`6'h25`|Pmod B．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Pmod Bの8ピンに結線．|
|`6'h26`|AR8～AR13．下位6ビットを使用し，上位26ビットは0固定．書き込み専用で使用している．Arduinoデジタル入出力8～13番ピンに結線(0ビット目=AR8～5ビット目=AR13．AR番号にビット位置を合わせるため，本レジスタと`6'h28`のみ他のレジスタと逆順)．|
|`6'h27`|A(AREF)，AR_SDA，AR_SCL．下位3ビットを使用し，上位29ビットは0固定．書き込み専用で使用している(2ビット目=A，1ビット目=AR_SDA，0ビット目=AR_SCL)．Arduinoの単体デジタルピン・I2C用SDA/SCLピンに結線．|
|`6'h28`|AR0～AR7．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．Arduinoデジタル入出力0～7番ピンに結線(0ビット目=AR0～7ビット目=AR7．AR番号にビット位置を合わせるため，本レジスタと`6'h26`のみ他のレジスタと逆順)．|
|`6'h29`|AR_RST．下位1ビットを使用し，上位31ビットは0固定．書き込み禁止．現在は物理ピンと未結線．|
|`6'h2a`|AR_MISO，AR_SCK，AR_MOSI，AR_SS．下位4ビットを使用し，上位28ビットは0固定(3ビット目=MISO，2ビット目=SCK，1ビット目=MOSI，0ビット目=SS)．Arduino SPIの4ピンに結線．MISOビットは外部ピンの値を毎サイクル反映するミラーで書き込んでも意味を持たず，他の3ビットは書き込んだ値がそのまま出力される．|
|`6'h2b`|アナログピン．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2c`|XADC．現時点でCPUで使用する予定がないため，いったんオミットしている．|
|`6'h2d`|GPIO0～GPIO7．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．GPIO0～5はPmod Aと物理ピンを共有するため未結線(常に0)，GPIO6・7のみラズパイヘッダーに結線．|
|`6'h2e`|GPIO8～GPIO15．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．ラズパイヘッダーに結線．|
|`6'h2f`|GPIO16～GPIO23．下位8ビットを使用し，上位24ビットは0固定．書き込み専用で使用している．ラズパイヘッダーに結線．|
|`6'h30`|GPIO24～GPIO27．下位4ビットを使用し，上位28ビットは0固定．書き込み専用で使用している．GPIO24のみラズパイヘッダーに結線，GPIO25～27は対応する物理ピンが無いため未結線(常に0)．|
|`6'h31`|標準入力データ．書き込み禁止．|
|`6'h32`|標準入力シグナル．`{29'h0, tlast, tvalid, tready}`．書き込み禁止．|
|`6'h33`|標準出力データ．読み込み禁止．|
|`6'h34`|標準出力シグナル．`{29'h0, tlast, tvalid, tready}`|

アドレスは6ビットで`6'h3f`まで表現できるが，`6'h35`～`6'h3f`には対応するレジスタが存在しない．

本仕様では，レジスタ単位でのみ読み書き制限を設けており，ビット単位の読み書き制御機構は持たない．そのため，入出力の方向が信号ごとに異なるレジスタ(例: `6'h2a`)であっても，レジスタ全体としての読み書き制限は設けない．

# フラグについて
FLGは他のCPU内レジスタと同じく32ビットのレジスタであり，現在未実装であるためいずれの命令からも値が格納されることはない(詳細は[isa.md](isa.md)を参照)．4ビット目以降は未使用である．

フラグ内の使用ビット
|ビット|名前|機能|
|:-|:-|:-|
|0ビット目|CF|キャリーフラグ．桁あふれが生じたときtrue，生じなかったときfalse|
|1ビット目|ZF|ゼロフラグ．計算結果が0のときtrue，0以外のときfalse|
|2ビット目|OF|オーバーフローフラグ．符号付の数の足し算や引き算において，最上位ビットが不正に書き変えられてしまったときtrue，そうでないときfalse|
|3ビット目|SF|サインフラグ．計算結果の最上位ビットを保存する．最上位ビットが1(負)のときtrue，0(0または正)のときfalse|
