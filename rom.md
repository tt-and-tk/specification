# 仕様
PCに対応する機械語を返す．
命令のビット数は64ビット(機械語32ビット+イミディエイトデータ32ビット)．詳細は[isa.md](isa.md)を参照．\
PCは命令単位のインデックスであり，imm使用の有無に関わらず全命令がこの64ビットを固定で占有する．通常の実行進行(ジャンプ・分岐以外)では常に+1される．

プログラムカウンタは，データバス幅(32ビット)で扱われる．\
ROMへ渡せるのはこのうち命令数の上限をちょうど表現できる幅の下位ビット(幅の値は[limitations.md](limitations.md#rom容量について)を参照)であり，この幅に収まる範囲がROMに対応する(下記「プログラムカウンタの空間」を参照)．

ROMの容量・命令数上限については[limitations.md](limitations.md#rom容量について)を参照．

# プログラムカウンタの空間
CPUは，ROMの命令に加えて，メインメモリのコード領域([memory.md](memory.md)を参照)に置いた命令も実行できる．PCの値によって，命令を取り出す場所が次のように決まる．

|PC|命令を取り出す場所|
|:-|:-|
|`0x0000`〜`0x3FFF`|ROM(ROMへ渡せる幅に収まる範囲)|
|`0x4000`〜`0x4FFF`|メインメモリのコード領域(`0x8000`〜`0xFFFF`)．PCに対応する番地は`0x8000` + (PC − `0x4000`) × 8|
|上記以外|不正な番地．折り返して有効な番地とみなすことはせず，命令を取り出そうとすると停止する(そのときの挙動は[halt.md](halt.md)を参照)|

コード領域の先頭(PCの`0x4000`)は固定である．JMP/CALLの飛び先や関数の番地は絶対番地のため，コード領域に置くプログラムは，先頭の命令のPCが`0x4000`になる前提で作る必要がある．

コード領域には，1命令を8バイトで置く．番地の小さい方の4バイトがイミディエイトデータ，大きい方の4バイトが機械語で，それぞれの4バイトはリトルエンディアンである．すなわち，64ビットの命令をリトルエンディアンで8バイトに並べた形になる．\
命令は，M系の命令でメインメモリへ書き込めば置ける(命令を置くための専用の書き込み方法はない)．\
コード領域にはROMのような命令数の範囲がない．PCがコード領域を指していれば，その位置の内容を，命令として書き込んだものかどうかによらず実行する．
