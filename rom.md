# 仕様
PCに対応する機械語を返す．
命令のビット数は64ビット(機械語32ビット+イミディエイトデータ32ビット)．詳細は[isa.md](isa.md)を参照．\
PCは命令単位のインデックスであり，imm使用の有無に関わらず全命令がこの64ビットを固定で占有する．通常の実行進行(ジャンプ・分岐以外)では常に+1される．

プログラムカウンタは，データバス幅(32ビット)で扱われる．\
ROMへ渡せるのはこのうち命令数の上限をちょうど表現できる幅の下位ビット(幅の値は[limitations.md](limitations.md#rom容量について)を参照)であり，PCがこの幅に収まる範囲(`0x0000`〜`0x3FFF`)の命令をROMから取り出す．\
それより上のPCのうち`0x4000`〜`0x4FFF`は，ROMではなくメインメモリの後半に置いた命令に対応する([memory.md](memory.md#メモリ上の命令の実行)を参照)．ROMにもメモリの後半にも対応しない番地は，折り返して有効な番地とみなすことはせず，不正な番地として扱う(そのときの挙動は[halt.md](halt.md)を参照)．

ROMの容量・命令数上限については[limitations.md](limitations.md#rom容量について)を参照．
