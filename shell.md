# シェル
自作パソコンのシェルは，CPU(PL)のROM上で動くプログラムである．標準入力から1行ずつコマンドを受け取り，対応する処理を実行する．\
PS(ARM)側はキー入力と画面出力を中継するだけで，コマンドの解釈には関与しない．

このファイルがシェルの仕様であり，実装(`qurge`の`mypc.srcs/pn/shell.pn`)はこれに従う．

# 起動時の動作
SDカードの初期化を試み，成功すればカード上のファイルテーブルを読み込んで前回の内容を復元する．\
初期化に失敗した場合は，ファイルが1件も無い状態で起動する．以降の操作は行えるが，カードが使えないため保存はされない．\
初期化に成功し，カードに読めるファイルテーブルが無かった場合も，ファイルが1件も無い状態で起動する．この場合は保存が行われるため，そのブロックにあった内容は次の保存で上書きされる．

# 入力の解析
プロンプト`> `を表示して1行を受け取り，半角空白で区切った先頭のトークンをコマンド名，以降を引数として扱う．

- 区切りとして扱うのは半角空白のみで，タブなど他の空白文字は引数の一部になる．コマンド名の手前と引数の間の空白は何文字でもよい
- コマンド名は下表のいずれかと完全に一致する必要がある(大文字・小文字を区別する)．一致しない場合と空行の場合はエラーになり，何も実行しない
- 1行の長さの上限とバックスペースの扱いは，入力に組み込み関数`scan`を使うためその仕様に従う([compiler.md](compiler.md)を参照)．シェルが渡す配列は64要素であり，63文字を超えた分は次の行として読み込まれる

# コマンド一覧
| 書式 | 動作 | エラーになる条件 |
|:-|:-|:-|
| `echo [文字列]` | 引数をそのまま出力する(引数が無ければ空行を出力する) | なし |
| `help` | 使用できるコマンド名の一覧を出力する | 引数を与えた |
| `ls` | 存在するファイルの名前を1行ずつ出力する | 引数を与えた |
| `touch <名前>` | 内容が空のファイルを1件作成する | 引数の過不足，ファイルが既に4件ある，同名のファイルが既にある |
| `write <名前> [内容]` | 既存ファイルの内容を，引数の残り全体で置き換える | 名前が無い，該当するファイルが無い |
| `cat <名前>` | ファイルの内容を出力する | 引数の過不足，該当するファイルが無い |
| `rm <名前>` | ファイルを削除する | 引数の過不足，該当するファイルが無い |
| `mv <旧名> <新名>` | ファイルの名前を変更する | 引数の過不足，該当するファイルが無い，新名が他のファイルと重複する |
| `sdinit` | SDカードを初期化し，カード上のファイルテーブルを読み込む | 引数を与えた，初期化に失敗した |

上表のエラーになる条件に当てはまる場合は，コマンド名を前置したエラーメッセージを出力し，そのコマンドは何も行わない．保存の失敗はこれに当たらず，メッセージは出るがコマンド自体は成立する(「保存」を参照)．

使用にあたって前提となる挙動:

- `write`は，名前の後ろを空白で区切らず丸ごと内容として扱うため，内容に空白を含められる(名前との間の空白だけが区切りとして取り除かれる)．内容を省くと空のファイルになる
- `sdinit`は，カードに読めるファイルテーブルがあった場合，その内容でメモリ上のファイルを置き換える．カードが使えない間に作ったファイルは保存されていないため，ここで失われる
- `ls`が出力する順序は，ファイルを作成した順序と一致するとは限らない(削除して空いた位置は，次に作ったファイルが使う)

# ファイルシステム
ファイルはディレクトリを持たず，すべてが1つの階層に並ぶ．

- ファイルは最大4件
- 名前は7文字まで．超えた分は切り捨てられる．参照する側の名前も同じ規則で切り詰めてから照合するため，8文字目以降は無視される
- 内容は31文字まで．超えた分は切り捨てられる
- 同じ名前のファイルを複数作ることはできない

# 保存
ファイルの増減・名前・内容が変わるコマンド(`touch`・`write`・`rm`・`mv`)は，実行のたびに全ファイルをまとめてSDカードへ書き出す．カードが使えない状態では書き出しを行わないため，電源を切った時点で内容は失われる．

- 書き出し先はカードの先頭ブロックであり，カードが他の用途で使っていた内容(PCで使うカードのMBR等)は上書きされる
- 扱えるカードはSDSC v2およびSDHC/SDXCで，SD v1には対応しない
- 書き出しに失敗した場合も，メモリ上の変更はそのまま残る(次に書き出しへ成功した時点でカードへ反映される)
