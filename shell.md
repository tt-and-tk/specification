# シェル
自作パソコンのシェルは，CPU(PL)のROM上で動くプログラムである．標準入力から1行ずつコマンドを受け取り，対応する処理を実行する．\
PS(ARM)側はキー入力と画面出力を中継するだけで，コマンドの解釈には関与しない．

このファイルがシェルの仕様であり，実装(`qurge`の`mypc.srcs/pn/shell.pn`と，そこから取り込むファイル)はこれに従う．

# 起動時の動作
SDカードを初期化し，カード上のFAT32ファイルシステムをマウントする(「ファイルシステム」を参照)．\
初期化やマウントに失敗した場合もシェルはそのまま起動し，失敗の理由を表示する．この場合，カードを差し直して`sdinit`を実行すればマウントし直せる．

# 入力の解析
プロンプト`> `を表示して1行を受け取り，半角空白で区切った先頭のトークンをコマンド名，以降を引数として扱う．

- 区切りとして扱うのは半角空白のみで，タブなど他の空白文字は引数の一部になる．コマンド名の手前と引数の間の空白は何文字でもよい
- コマンド名は下表のいずれかと完全に一致する必要がある(大文字・小文字を区別する)．一致しない場合と空行の場合はエラーになり，何も実行しない
- 1行の長さの上限とバックスペースの扱いは，入力に組み込み関数`scan`を使うためその仕様に従う([compiler.md](compiler.md)を参照)．シェルが渡す配列は64要素であり，63文字を超えた分は次の行として読み込まれる

# コマンド一覧
| 書式 | 動作 | エラーになる条件 |
|:-|:-|:-|
| `echo [文字列]` | 引数をそのまま出力する(引数が無ければ空行を出力する) | なし |
| `help` | 使用できるコマンド名の一覧を出力する | 引数を与えた |
| `ls` | ルートディレクトリにあるファイルとディレクトリの名前を1行ずつ出力する．ディレクトリは名前の末尾に`/`を付ける | 引数を与えた，ファイルシステムをマウントしていない，カードから読み出せない，ファイルシステムの記録が壊れている |
| `cat <名前>` | ルートディレクトリにあるファイルの内容を出力する | 引数の過不足，名前が8.3形式に収まらない，該当するファイルが無い，名前がディレクトリを指す，ファイルシステムをマウントしていない，カードから読み出せない，ファイルシステムの記録が壊れている |
| `sdinit` | SDカードを初期化し，カード上のFAT32ファイルシステムをマウントし直す | 引数を与えた，初期化に失敗した，カードから読み出せない，カード上にFAT32ファイルシステムが見つからない |

上表のエラーになる条件に当てはまる場合は，コマンド名を前置したエラーメッセージを出力し，そのコマンドは何も行わない．ただし次の2つは例外である．

- `sdinit`が失敗した場合は，それまでマウントしていたファイルシステムも外れる．以降は`sdinit`が成功するまで，`ls`・`cat`はマウントしていない旨のエラーになる
- `ls`・`cat`が出力の途中で読み出しに失敗した場合，それまでに出力した内容はそのまま残る

使用にあたって前提となる挙動:

- `cat`は，ファイルの内容が改行で終わっていなければ末尾に改行を補う．内容が空のファイルでは何も出力しない
- `ls`が出力する順序は，ディレクトリに記録された順序であり，名前順ではない

# ファイルシステム
PCでFAT32にフォーマットしたSDカードを読み出す．現時点では読み出しのみに対応し，ファイルの作成・書き込み・削除は行えない．

## 扱えるカード
- 扱えるカードはSDSC v2およびSDHC/SDXCで，SD v1には対応しない
- カードはFAT32でフォーマットされている必要がある．SDXCカードの標準のフォーマットであるexFATや，容量の小さいSDSCカードに多いFAT16には対応しないため，FAT32でフォーマットし直して使う
- カードの第1パーティションの種別がFAT32(`0x0B`または`0x0C`)であれば，そのパーティションをマウントする．パーティションを持たず，カードの先頭からFAT32ファイルシステムが始まるカードもマウントできる
- セクタ長が512バイトのファイルシステムのみ扱える

## 扱えるディレクトリとファイル名
- 扱えるのはルートディレクトリのみで，その中にあるディレクトリの中身は参照できない
- ファイル名は8.3形式(基本名1〜8文字，拡張子0〜3文字．いずれも空白を含まない)の短い名前で扱う．空白は引数の区切りになるため，空白を含む名前は指定できない
- 長いファイル名(LFN)は読まず，LFNを持つファイルも短い名前で表示・指定する．PCで8.3形式に収まらない名前や空白を含む名前を付けると，PCが空白を除いた短い名前を別に付ける(`LongFileName.txt`は`LONGFI~1.TXT`，`My File.txt`は`MYFILE~1.TXT`になる)
- `cat`に指定する名前は，大文字・小文字を区別せずに照合する．8.3形式に収まらない名前は，切り詰めて照合せずエラーになる(切り詰めると別のファイルの名前になりうるため)
- `ls`は，短い名前を基本名・拡張子ごとに次の規則で表示する．PCで付けた名前の基本名・拡張子がそれぞれ大文字のみ・小文字のみで書かれていれば，付けた名前のとおりに表示される
  - PCが小文字で表示する印を付けた部分(Windowsが全て小文字の基本名・拡張子に付ける)は小文字で表示する
  - それ以外は大文字で表示する(大文字・小文字が混ざった名前は，元の名前がLFNにしか記録されないため)
- 隠しファイル・システムファイルも区別せずに表示する(Windowsが作る`System Volume Information`は`SYSTEM~1/`と表示される)

## ファイルの内容
- 内容はバイト列をそのまま文字として出力する．文字コードの変換は行わない
- 内容の途中に値0のバイトがあると，そのバイトから512バイト単位の区切りまでは出力されない(テキストファイルを想定しており，バイナリファイルの表示には対応しない)
