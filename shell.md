# シェル
Pynthesisのシェルは，CPU(PL)のROM上で動くプログラムである．標準入力から1行ずつコマンドを受け取り，対応する処理を実行する．\
PS(ARM)側はキー入力と画面出力を中継するだけで，コマンドの解釈には関与しない．

このファイルがシェルの仕様であり，実装(`qurge`の`mypc.srcs/pn/shell.pn`と，そこから取り込むファイル)はこれに従う．

# 起動時の動作
SDカードを初期化し，カード上のFAT32ファイルシステムをマウントする(「ファイルシステム」を参照)．\
初期化やマウントに失敗した場合もシェルはそのまま起動し，失敗の理由を表示する．この場合，カードを差し直して`sdinit`を実行すればマウントし直せる．

# 入力の解析
プロンプト`> `を表示して1行を受け取り，半角空白で区切った先頭のトークンをコマンド名，以降を引数として扱う．

- 区切りとして扱うのは半角空白のみで，タブなど他の空白文字は引数の一部になる．コマンド名の手前と引数の間の空白は何文字でもよい
- コマンド名は下表のいずれかと完全に一致する必要がある(大文字・小文字を区別する)．一致しない場合と空行の場合はエラーになり，何も実行しない
- 1行の長さの上限とバックスペースの扱いは，入力に組み込み関数`scan`を使うためその仕様に従う([compiler.md](compiler.md)を参照)．シェルが渡す配列は64要素であり，63文字を超えた分は次の行として読み込まれる

# コマンド一覧
| 書式 | 動作 | エラーになる条件 |
|:-|:-|:-|
| `echo [文字列]` | 引数をそのまま出力する(引数が無ければ空行を出力する) | なし |
| `help` | 使用できるコマンド名の一覧を出力する | 引数を与えた |
| `ls` | 現在のディレクトリにあるファイルとディレクトリの名前を1行ずつ出力する．ディレクトリは名前の末尾に`/`を付ける | 引数を与えた，ファイル操作に共通する条件 |
| `cat <名前>` | ファイルの内容を出力する | 引数の過不足，名前が規則に合わない，該当するファイルが無い，名前がディレクトリを指す，ファイル操作に共通する条件 |
| `touch <名前>` | 内容が空のファイルを作成する | 引数の過不足，名前が規則に合わない，同じ名前のファイル・ディレクトリが既にある，カードに空きが無い，ファイル操作に共通する条件 |
| `write <名前> [内容]` | ファイルの内容を，名前の後ろの引数全体で置き換える．ファイルが無ければ作成する | 名前が無い，名前が規則に合わない，名前がディレクトリを指す，カードに空きが無い，ファイル操作に共通する条件 |
| `rm <名前>` | ファイルを削除する | 引数の過不足，名前が規則に合わない，該当するファイルが無い，名前がディレクトリを指す，ファイル操作に共通する条件 |
| `mv <旧名> <新名>` | ファイル・ディレクトリの名前を変更する | 引数の過不足，名前が規則に合わない，旧名に該当するものが無い，新名が他のファイル・ディレクトリと重複する，ファイル操作に共通する条件 |
| `mkdir <名前>` | 空のディレクトリを作成する | 引数の過不足，名前が規則に合わない，同じ名前のファイル・ディレクトリが既にある，カードに空きが無い，ファイル操作に共通する条件 |
| `rmdir <名前>` | 空のディレクトリを削除する | 引数の過不足，名前が規則に合わない，該当するディレクトリが無い，名前がファイルを指す，ディレクトリが空でない，ファイル操作に共通する条件 |
| `cd [名前]` | 現在のディレクトリを移す(「ディレクトリ」を参照) | 引数が多い，名前が規則に合わない，該当するディレクトリが無い，名前がファイルを指す，移動先のパスが長すぎる，ファイル操作に共通する条件 |
| `pwd` | 現在のディレクトリの絶対パスを出力する | 引数を与えた，ファイルシステムをマウントしていない |
| `sdinit` | SDカードを初期化し，カード上のFAT32ファイルシステムをマウントし直す．現在のディレクトリはルートディレクトリに戻る | 引数を与えた，初期化に失敗した，カードから読み出せない，カード上にFAT32ファイルシステムが見つからない |

名前を引数に取るコマンドは，いずれも現在のディレクトリの直下にあるファイル・ディレクトリを対象にする(「ディレクトリ」を参照)．\
表中の「ファイル操作に共通する条件」は，ファイルシステムをマウントしていない場合，カードを読み書きできない場合，ファイルシステムの記録が壊れている場合を指す．

上表のエラーになる条件に当てはまる場合は，コマンド名を前置したエラーメッセージを出力し，そのコマンドは何も行わない．ただし次のものは例外である．

- `sdinit`が失敗した場合は，それまでマウントしていたファイルシステムも外れる．以降は`sdinit`が成功するまで，ファイルを扱うコマンドはマウントしていない旨のエラーになる
- `ls`・`cat`が出力の途中で読み出しに失敗した場合，それまでに出力した内容はそのまま残る
- カードを書き換えるコマンドが途中でカードの読み書きに失敗した場合，それまでの書き換えはそのまま残る．ただし`write`は，新しい内容を元の内容とは別の空きへ書き終えてから差し替えるため，失敗しても(カードの空きが足りない場合を含む)ファイルは元の内容のまま残る．ファイルが無く新たに作成した場合は，内容が空のファイルが残る

使用にあたって前提となる挙動:

- `cat`は，ファイルの内容が改行で終わっていなければ末尾に改行を補う．内容が空のファイルでは何も出力しない
- `ls`が出力する順序は，ディレクトリに記録された順序であり，名前順ではない
- `write`は，名前の後ろを空白で区切らず丸ごと内容として扱うため，内容に空白を含められる(名前との間の空白だけが区切りとして取り除かれる)．内容を省くと空のファイルになる．内容の末尾に改行は付けない
- `write`で書ける内容の長さは，1行の長さの上限から，コマンド名・名前とその前後の空白を除いた分までである
- `touch`は，同じ名前のファイルが既にあればエラーになり，そのファイルには何もしない(Unixの`touch`のように，既存のファイルの更新日時を書き換えて成功する動作はしない)
- `mv`は，大文字・小文字だけが異なる名前へも変更できる．名前を変えるだけで，別のディレクトリへは移せない(新名に既存のディレクトリの名前を指定すると，Unixの`mv`のようにその中へ移すのではなく，名前の重複としてエラーになる)
- `rm`はファイルだけを削除する．ディレクトリは`rmdir`で削除する

# ファイルシステム
パソコン(Windowsなどが動く一般のパソコン)でFAT32にフォーマットしたSDカードを読み書きする．Pynthesisで作成・変更したファイル・ディレクトリは，カードをパソコンへ差せばそのまま読める．フォーマットは行えないため，パソコンで行う．

## 扱えるカード
- 扱えるカードはSDSC v2およびSDHC/SDXCで，SD v1には対応しない
- カードはFAT32でフォーマットされている必要がある．SDXCカードの標準のフォーマットであるexFATや，容量の小さいSDSCカードに多いFAT16には対応しないため，FAT32でフォーマットし直して使う
- カードの第1パーティションの種別がFAT32(`0x0B`または`0x0C`)であれば，そのパーティションをマウントする．パーティションを持たず，カードの先頭からFAT32ファイルシステムが始まるカードもマウントできる
- セクタ長が512バイトのファイルシステムのみ扱える

## ディレクトリ
- シェルは現在のディレクトリを1つ持つ．起動時と`sdinit`でマウントした時点ではルートディレクトリで，`cd`で移る
- 名前は，現在のディレクトリの直下にあるファイル・ディレクトリの名前だけを指定できる．`/`で区切ったパス(`/DOCS`・`DOCS/a.txt`など)は，`/`が名前に使えない文字のため，名前が規則に合わないエラーになる
- `cd`は次のように移る

  | 書式 | 動作 |
  |:-|:-|
  | `cd` | ルートディレクトリへ移る |
  | `cd <名前>` | 現在のディレクトリの直下にある，その名前のディレクトリへ移る |
  | `cd ..` | 親ディレクトリへ移る．ルートディレクトリでは何もしない |
  | `cd .` | 何もしない |
- `pwd`は，ルートディレクトリを`/`とし，ルートディレクトリから現在のディレクトリまでの各ディレクトリの名前を`/`に続けて並べた絶対パスを出力する(`/DOCS/sub`など)．各名前は`ls`と同じ形で表示する
- 現在のディレクトリの絶対パスは255文字まで保持できる．これを超えるディレクトリへの`cd`はエラーになる
- `.`・`..`は，ディレクトリ自身と親ディレクトリを表すエントリである．FAT32の決まりにより，ルート以外のディレクトリは先頭にこの2つを持ち，ルートディレクトリはどちらも持たない．`cd`はこれらのエントリを読まずに移るため，ルートディレクトリでも表のとおりに動く．`mkdir`はこの2つを作成する．`ls`はこれらを表示せず，`cd`以外のコマンドでは名前として指定できない(名前が規則に合わないエラーになる)
- `rmdir`は，`.`・`..`以外のエントリが無いディレクトリだけを削除する．中身ごと削除する機能は無い
- パソコンで作ったディレクトリも，Pynthesisで作ったディレクトリと同じように辿れる

## ファイル・ディレクトリの名前
- 名前は8.3形式(基本名1〜8文字，拡張子0〜3文字．いずれも空白を含まない)の短い名前で扱う．ディレクトリの名前もファイルと同じ規則に従い，拡張子を持てる．空白は引数の区切りになるため，空白を含む名前は指定できない
- 長いファイル名(LFN)は読まず，LFNを持つファイルも短い名前で表示・指定する．パソコンで8.3形式に収まらない名前や空白を含む名前を付けると，パソコンのOSが空白を除いた短い名前を作り，元の長いファイル名とは別に記録する(`LongFileName.txt`は`LONGFI~1.TXT`，`My File.txt`は`MYFILE~1.TXT`になる)．SDカード自体はセクタを読み書きするだけで，名前を作る機能は持たない
- 指定する名前は，大文字・小文字を区別せずに照合する．パソコンが作った短い名前(`~1`などの付いた名前)も，`ls`が表示したとおりに指定できる
- 規則に合わない名前はエラーになる．名前の規則は次のとおり
  - 8.3形式に収まること．収まらない名前は切り詰めて扱わない(切り詰めると別のファイルの名前になりうるため)
  - 制御文字・空白と，記号`" * + , / : ; < = > ? [ \ ] |`を含まないこと(FATの仕様で，短い名前に使えないと定められた文字のため)
- Pynthesisで作成・変更した名前は，カードには大文字で記録する．長いファイル名は作らないため，英字を大文字・小文字のどちらで表示するかは小文字で表示する印だけで決まる(数字や`( )`などの記号は変換されず，入力したとおりに表示される．パソコンで付けた名前の表示は後述)．基本名・拡張子のそれぞれについて，英小文字だけで書いた部分には，Windowsと同じく小文字で表示する印を付けるため，`ls`とパソコンで小文字のまま表示される．大文字・小文字が混ざった部分は大文字で表示される(`Hello.txt`は，大文字・小文字が混ざった基本名`Hello`が大文字になり，小文字だけの拡張子`txt`は小文字のまま残るため，`HELLO.txt`と表示される)
- パソコンで長いファイル名を付けたファイル・ディレクトリには，短い名前とは別に長いファイル名が記録されている．これを削除・名前の変更すると，記録されている長いファイル名もあわせて削除する．このため名前を変更したファイル・ディレクトリは，パソコンでも変更後の短い名前で表示される．内容を書き換えただけのファイルは，パソコンで元の長いファイル名のまま表示される
- `ls`は，短い名前を基本名・拡張子ごとに次の規則で表示する．パソコンで付けた名前の基本名・拡張子がそれぞれ大文字のみ・小文字のみで書かれていれば，付けた名前のとおりに表示される
  - パソコンが小文字で表示する印を付けた部分(Windowsが全て小文字の基本名・拡張子に付ける)は小文字で表示する
  - それ以外は大文字で表示する．パソコンは，大文字・小文字が混ざった名前や8.3形式に収まらない名前には長いファイル名を作り，元の名前をそちらにだけ記録する．このとき短い名前には印を付けないため，拡張子を含めて全て大文字で表示される
- ファイルの属性(隠しファイル・読み取り専用など)による区別は行わず，どのファイルも他のファイルと同じように表示・操作できる．Windowsが拒む読み取り専用のファイルへの書き込みや削除も行える(Windowsが作る`System Volume Information`は`SYSTEM~1/`と表示される)

## ファイルの内容
- 内容はバイト列をそのまま文字として出力する．文字コードの変換は行わない
- 内容の途中に値0のバイトがあると，そのバイトから512バイト単位の区切りまでは出力されない(テキストファイルを想定しており，バイナリファイルの表示には対応しない)

## 書き込みの前提
- 時計を持たないため，作成・変更したファイル・ディレクトリの日時は1980年1月1日0時0分(FATで表せる最も古い日時)になる
- ファイルシステムが記録している空きクラスタ数は，マウントしてから最初にカードを書き換える時点で「不明」を表す値にし，以降は更新しない．パソコンは次に差したときに数え直す
- 書き込みの途中で電源を切った場合やカードを抜いた場合の整合性は保証しない．壊れた記録はパソコンのディスク検査で修復する
